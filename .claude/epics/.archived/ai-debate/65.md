---
name: Agent infrastructure
status: closed
created: 2026-02-24T13:52:53Z
updated: 2026-02-24T13:58:02Z
github: https://github.com/jobu711/options_arena/issues/65
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Agent infrastructure

## Description

Create the foundational agent module files: `model_config.py` (Ollama model builder),
`_parsing.py` (DebateDeps and DebateResult dataclasses, context rendering), and
`__init__.py` (re-exports). These are the shared building blocks that all three agents
and the orchestrator depend on.

`agents/CLAUDE.md` is already created — do NOT recreate it.

## Acceptance Criteria

- [ ] `agents/model_config.py` created with `build_ollama_model(config) -> OllamaModel` and `_resolve_host(config) -> str`
- [ ] Host resolution priority: explicit config > `OLLAMA_HOST` env var > default `http://localhost:11434`
- [ ] `agents/_parsing.py` created with `DebateDeps` dataclass and `DebateResult` dataclass
- [ ] `DebateDeps` fields: `context: MarketContext`, `ticker_score: TickerScore`, `contracts: list[OptionContract]`, `opponent_argument: str | None`, `bull_response: AgentResponse | None`, `bear_response: AgentResponse | None`
- [ ] `DebateResult` fields: `context: MarketContext`, `bull_response: AgentResponse`, `bear_response: AgentResponse`, `thesis: TradeThesis`, `total_usage: RunUsage`, `duration_ms: int`, `is_fallback: bool`
- [ ] `render_context_block(ctx: MarketContext) -> str` function in `_parsing.py` for flat key-value text rendering
- [ ] `agents/__init__.py` created with placeholder re-exports (will be updated as agents/orchestrator are added)
- [ ] All type annotations complete — `mypy src/ --strict` passes
- [ ] `ruff check .` passes
- [ ] Existing 1,086 tests still pass

## Technical Details

### model_config.py (FR-D7)

```python
from pydantic_ai.models.ollama import OllamaModel

def build_ollama_model(config: DebateConfig) -> OllamaModel:
    host = _resolve_host(config)
    return OllamaModel(config.ollama_model, base_url=host)

def _resolve_host(config: DebateConfig) -> str:
    # explicit config > OLLAMA_HOST env > default
```

**Import path note**: `pydantic_ai.models.ollama.OllamaModel` — verify at implementation.
If import fails, fallback: `pydantic_ai.models.openai.OpenAIModel` with `base_url=host + "/v1"`.

### _parsing.py (FR-D3)

- `DebateDeps` and `DebateResult` are `@dataclass` (not Pydantic) because `RunUsage` is a plain dataclass.
- `render_context_block()` converts `MarketContext` to flat text for agent prompts (not JSON).

### __init__.py

Initial exports: `DebateDeps`, `DebateResult`, `build_ollama_model`, `render_context_block`.
Will add `run_debate`, `build_market_context` after #63.

### Files created

- `src/options_arena/agents/model_config.py`
- `src/options_arena/agents/_parsing.py`
- `src/options_arena/agents/__init__.py`

## Dependencies

- None (infrastructure task, no blocking dependencies)
- #64 provides `DebateConfig` but `_parsing.py` only needs the type import — can develop with the model definition

## Effort Estimate

- Size: S
- Parallel: true (can run alongside #64)
