---
name: Add shared prompt rules appendix and output validator helpers to _parsing.py
status: closed
created: 2026-02-25T14:23:09Z
updated: 2026-02-25T09:53:05Z
github: https://github.com/jobu711/options_arena/issues/77
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Add shared prompt rules appendix and output validator helpers to _parsing.py

## Description
Create the shared `PROMPT_RULES_APPENDIX` constant and two output validator helper functions
in `src/options_arena/agents/_parsing.py`. This is the foundation task — all three agent
updates depend on these additions.

## Acceptance Criteria
- [ ] `PROMPT_RULES_APPENDIX` constant added with `# VERSION: v2.0` header
- [ ] Appendix includes confidence calibration scale (5 tiers: 0.0-0.2 through 0.8-1.0)
- [ ] Appendix includes data anchors (composite score ↔ confidence constraints, RSI contradiction rule)
- [ ] Appendix includes data citation rules with WRONG/RIGHT examples
- [ ] `build_cleaned_agent_response(output: AgentResponse) -> AgentResponse` added
- [ ] `build_cleaned_trade_thesis(output: TradeThesis) -> TradeThesis` added
- [ ] Both helpers use existing `strip_think_tags()` function
- [ ] Both helpers short-circuit (return original) when no `<think>` tags found
- [ ] Full type annotations on all new code
- [ ] `ruff check` and `mypy --strict` pass

## Technical Details
- **File**: `src/options_arena/agents/_parsing.py`
- **Imports needed**: `AgentResponse` and `TradeThesis` are already imported
- Add `PROMPT_RULES_APPENDIX` after the existing `strip_think_tags()` function and before `DebateDeps`
- `build_cleaned_agent_response()` extracts the duplicated logic from bull.py lines 68-96 and bear.py lines 80-108
- `build_cleaned_trade_thesis()` extracts the duplicated logic from risk.py lines 98-122
- Both functions follow the same pattern: check fields for `<think>` tags, return original if clean, construct new frozen instance if dirty
- The exact code is specified in the epic deliverables section

## Dependencies
- [ ] None — this is the foundation task

## Effort Estimate
- Size: S
- Hours: 2
- Parallel: false (blocks all other tasks)

## Definition of Done
- [ ] Code implemented in `_parsing.py`
- [ ] `uv run ruff check src/options_arena/agents/_parsing.py --fix`
- [ ] `uv run mypy src/options_arena/agents/_parsing.py --strict`
- [ ] Existing tests still pass: `uv run pytest tests/ -v -k "parsing or _parsing"`
