---
name: Flow, risk & options indicators + Flow Agent
status: closed
created: 2026-02-28T15:36:34Z
updated: 2026-02-28T15:47:54Z
github: https://github.com/jobu711/options_arena/issues/157
depends_on: [154]
parallel: true
conflicts_with: []
---

# Task: Flow, risk & options indicators + Flow Agent

## Description

Build the options flow analysis and risk quantification layers. Implement GEX, unusual activity scoring, OI concentration, and max pain magnet. Add second-order Greeks (vanna, charm, vomma) via finite difference on existing pricing infrastructure. Extend options-specific indicators with PoP, optimal DTE, spread quality, and max loss ratio. Deploy the new Flow Agent.

Branch: `epic/dse-2-flow-risk`

## Acceptance Criteria

- [ ] `indicators/flow_analytics.py` created with 5 flow indicators: `compute_gex()`, `compute_oi_concentration()`, `compute_unusual_activity()`, `compute_max_pain_magnet()`, `compute_dollar_volume_trend()`
- [ ] `pricing/greeks_extended.py` created with 3 second-order Greeks: `compute_vanna()`, `compute_charm()`, `compute_vomma()` via central finite difference
- [ ] `indicators/options_specific.py` extended with 4 indicators: `compute_pop()`, `compute_optimal_dte()`, `compute_spread_quality()`, `compute_max_loss_ratio()`
- [ ] `agents/flow_agent.py` created — PydanticAI agent with `FlowThesis` output model, prompt organized by Gamma Positioning / Smart Money Activity / OI Analysis / Volume Confirmation
- [ ] Each indicator returns `float | None`, handles edge cases (empty chains, zero OI, etc.)
- [ ] Unit tests for all 12 indicators + 3 second-order Greeks
- [ ] `ruff check`, `ruff format`, `mypy --strict` pass
- [ ] All existing tests pass unchanged

## Technical Details

### Files Created
- `src/options_arena/indicators/flow_analytics.py` — 5 flow indicator functions
- `src/options_arena/pricing/greeks_extended.py` — vanna, charm, vomma via finite difference
- `src/options_arena/agents/flow_agent.py` — Flow Agent definition + prompt
- `tests/unit/indicators/test_flow_analytics.py` — flow indicator tests
- `tests/unit/pricing/test_greeks_extended.py` — second-order Greek tests
- `tests/unit/indicators/test_options_specific_ext.py` — extended options indicator tests

### Files Modified
- `src/options_arena/indicators/options_specific.py` — add PoP, optimal DTE, spread quality, max loss ratio

### Key Indicators
| # | Function | Formula | Priority |
|---|----------|---------|----------|
| 30 | `compute_gex` | `Σ(OI × gamma × 100 × spot)` net across strikes | P0 |
| 34 | `compute_oi_concentration` | `max_strike_OI / total_OI` | P0 |
| 35 | `compute_unusual_activity` | Volume/OI weighted by premium for strikes where vol > 2×OI | P0 |
| 39 | `compute_max_pain_magnet` | `1 - (\|spot - max_pain\| / spot)` | P1 |
| 47 | `compute_dollar_volume_trend` | 20-day slope of `close × volume` | P1 |
| 31 | `compute_vanna` | `d(delta)/d(IV)` finite difference, `dσ = 0.01` | P1 |
| 32 | `compute_charm` | `d(delta)/d(T)` finite difference, `dT = 1/365` | P1 |
| 33 | `compute_vomma` | `d(vega)/d(IV)` finite difference | P2 |
| 38 | `compute_pop` | `N(d2)` for calls, `N(-d2)` for puts | P1 |
| 40 | `compute_optimal_dte` | Theta-normalized expected value | P2 |
| 45 | `compute_spread_quality` | OI-weighted average bid-ask spread | P0 |
| 58 | `compute_max_loss_ratio` | `contract_cost / account_risk_budget` | P1 |

### Key Constraints
- Read `indicators/CLAUDE.md`, `pricing/CLAUDE.md`, `agents/CLAUDE.md` before modifying
- Second-order Greeks use `pricing/dispatch.py` only — never import `bsm` or `american` directly
- GEX computation: filter to ATM ± 10 strikes for performance (~20 strikes × 3 expirations)
- Flow Agent uses module-level `Agent[Deps, FlowThesis]` pattern, `model=None` at init
- Flow Agent prompt uses `PROMPT_RULES_APPENDIX` shared appendix
- PoP uses BSM `N(d2)` — import from `scipy.stats.norm.cdf`

## Dependencies

- [ ] Task 001 (Foundation) must be merged — provides `FlowThesis` model and `IndicatorSignals` fields
- [ ] Cross-epic: DSE-1 Expected Move (#36) needed by Optimal DTE (#40) — use `None` fallback until DSE-1 merges

## Effort Estimate

- Size: L
- Hours: 16-24
- Parallel: true (with Tasks 002, 004, 005, 006)

## Definition of Done

- [ ] Code implemented with full type annotations
- [ ] Tests written and passing (new + all existing)
- [ ] `ruff check`, `ruff format`, `mypy --strict` pass
- [ ] Branch `epic/dse-2-flow-risk` created, PR merged to master
