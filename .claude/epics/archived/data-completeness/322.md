---
name: Verification and CI gates
status: open
created: 2026-03-06T23:20:39Z
updated: 2026-03-06T23:24:32Z
github: https://github.com/jobu711/options_arena/issues/322
depends_on: [319, 320, 321]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Verification and CI gates

## Description

Run the full verification suite after all three features are implemented. Ensure lint, format, type checking, all Python tests (unit + integration), and frontend build all pass cleanly.

## Acceptance Criteria

- [ ] `uv run ruff check . --fix` — no errors
- [ ] `uv run ruff format .` — no changes needed
- [ ] `uv run mypy src/ --strict` — no errors
- [ ] `uv run pytest tests/ -v` — all tests pass (3,921+ existing + ~18 new)
- [ ] `cd web && npm run build` — no TypeScript errors
- [ ] No regressions in existing test suite

## Technical Details

### Verification commands (run in order):
```bash
uv run ruff check . --fix && uv run ruff format .
uv run mypy src/ --strict
uv run pytest tests/ -v
cd web && npm run build
```

### Common issues to watch for:
- Missing `import math` in model files (for `isfinite` validators)
- Missing type annotations on new parameters (mypy --strict)
- `OptionsDataService` import cycle (outcome_collector importing from services — should be fine since both are in `services/`)
- Frontend `Tag` severity type mismatch (must be exact PrimeVue severity literal)
- Existing test fixtures constructing `TickerInfo` without new fields — should work due to `None` defaults

### If failures occur:
- Fix lint/format issues inline
- Fix type errors (usually missing annotations or wrong types)
- Fix test failures by investigating root cause, not by modifying test expectations
- Re-run full suite after each fix

## Test Plan

### Test Cases
This task IS the test execution — no additional tests to write.

Run full suite and verify:
1. All new tests from Tasks 319 + 320 pass
2. All 3,921 existing tests still pass
3. No new warnings or deprecations introduced

### Edge Cases
- 2 pre-existing test failures in `test_expanded_context.py` (NaN/Inf validators) — these are on master, not our fault. Do not try to fix.

## Dependencies
- [ ] Task 319: Short interest end-to-end (must be complete)
- [ ] Task 320: Active-contract P&L (must be complete)
- [ ] Task 321: Indicator signal labels (must be complete)

## Effort Estimate
- Size: XS
- Hours: 0.5-1
- Parallel: false (depends on all other tasks)

## Definition of Done
- [ ] All 4 verification commands pass
- [ ] No regressions
- [ ] Epic ready for commit
