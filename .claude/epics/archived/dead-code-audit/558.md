---
name: "Legacy debate removal (Wave 1)"
status: closed
created: 2026-03-16T22:34:20Z
updated: 2026-03-16T22:53:34Z
github: https://github.com/jobu711/options_arena/issues/558
depends_on: []
parallel: false
conflicts_with: [559, 560]
test_files:
  - tests/unit/agents/test_bull.py
  - tests/unit/agents/test_bear.py
  - tests/unit/agents/test_prompt_enhancements.py
  - tests/unit/agents/test_orchestrator.py
  - tests/unit/agents/test_model_config.py
  - tests/unit/agents/test_parsing.py
  - tests/unit/api/test_schemas.py
  - tests/unit/models/test_openbb_models.py
  - tests/unit/models/test_analysis.py
---

# Task: Legacy debate removal (Wave 1)

## Description

Delete the orphaned bull/bear 2-agent debate code, dead DebateDeps fields, dead agent prompt blocks, unused API DI providers, dead config fields, and dead model classes. Pure subtraction — zero behavioral change.

## Acceptance Criteria

- [ ] `agents/bull.py` and `agents/bear.py` deleted
- [ ] `agents/prompts/bull.py` and `agents/prompts/bear.py` deleted
- [ ] 6 dead fields removed from `DebateDeps`: `opponent_argument`, `bear_counter_argument`, `bull_response`, `bear_response`, `bull_rebuttal`, `vol_response`
- [ ] `spread_analysis` and `constraint_warnings` preserved on `DebateDeps` (LIVE — research correction)
- [ ] Dead `if ctx.deps.bull_response` blocks removed from volatility, flow, fundamental agents
- [ ] `_opposite_direction()` deleted from orchestrator
- [ ] `_resolve_api_key()` deleted from model_config
- [ ] `build_cleaned_trade_thesis()` deleted from _parsing.py
- [ ] Dead API providers (`get_financial_datasets`, `get_intelligence`, `get_openbb`) deleted from deps.py
- [ ] `SectorInfo` deleted from schemas.py
- [ ] 3 dead `MarketContext` fields removed: `ml_regime`, `ml_regime_confidence`, `flow_anomaly_score`
- [ ] 4 dead config fields removed: `enable_clustering`, `contract_n_clusters`, `enable_flow_anomaly`, `enable_regime_weights` (+ `contract_n_clusters` validator)
- [ ] `OpenBBHealthStatus` deleted from openbb.py and removed from `models/__init__.py` re-exports
- [ ] All corresponding tests deleted or updated
- [ ] `ruff check`, `mypy --strict`, `pytest` all pass

## Technical Details

### Research Corrections (CRITICAL — read before starting)
1. `spread_analysis` is LIVE — set by orchestrator (lines 1440, 1514-1520, 1623), read in phase_options. Do NOT remove.
2. `constraint_warnings` is LIVE — set via `render_constraint_warnings()`, injected into all agent prompts. Do NOT remove.
3. `DebateResult` fields (`bull_response`, `bear_response`, `bull_rebuttal`, `vol_response`) are on a DIFFERENT model from `DebateDeps` — preserve on `DebateResult` for DB backward compat. Only remove from `DebateDeps`.

### Files affected (source)
| File | Change |
|------|--------|
| `agents/bull.py` | Delete entire file (69 lines) |
| `agents/bear.py` | Delete entire file (57 lines) |
| `agents/prompts/bull.py` | Delete entire file |
| `agents/prompts/bear.py` | Delete entire file |
| `agents/prompts/__init__.py` | Remove BULL/BEAR imports (lines 3-4) and __all__ entries (lines 13-14) |
| `agents/_parsing.py` | Remove 6 DebateDeps fields (lines 322-327). Delete `build_cleaned_trade_thesis()` (line 123) |
| `agents/orchestrator.py` | Delete `_opposite_direction()` (line 778). Remove kwargs setting dead DebateDeps fields |
| `agents/model_config.py` | Delete `_resolve_api_key()` (line 286) |
| `agents/volatility.py` | Delete dead `if ctx.deps.bull_response/bear_response` block (lines 44-51) |
| `agents/flow_agent.py` | Delete dead `if ctx.deps.bull_response/bear_response` block (lines 45-52) |
| `agents/fundamental_agent.py` | Delete dead `if ctx.deps.bull_response/bear_response` block (lines 49-56) |
| `api/deps.py` | Delete `get_financial_datasets()` (line 66), `get_intelligence()` (line 61), `get_openbb()` (line 56) |
| `api/schemas.py` | Delete `SectorInfo` class (line 685) |
| `models/analysis.py` | Remove `ml_regime` (line 208), `ml_regime_confidence` (line 209), `flow_anomaly_score` (line 205) from MarketContext |
| `models/config.py` | Remove `enable_clustering` (line 51), `contract_n_clusters` (line 57), `enable_flow_anomaly` (line 50) from MLConfig. Remove `enable_regime_weights` (line 294) from DebateConfig. Remove `contract_n_clusters` validator (lines 85-91) |
| `models/openbb.py` | Delete `OpenBBHealthStatus` class (line 179) |
| `models/__init__.py` | Remove `OpenBBHealthStatus` from re-exports (line 132, line 280) |
| `agents/__init__.py` | Remove bull/bear re-exports if any |

## Execution

### Read First
- `.claude/epics/dead-code-audit/research.md` — critical corrections to PRD
- `src/options_arena/agents/_parsing.py` — understand DebateDeps fields
- `src/options_arena/agents/orchestrator.py` — find kwarg sites and _opposite_direction
- `src/options_arena/agents/CLAUDE.md` — module conventions

### Action
1. Delete `src/options_arena/agents/bull.py` and `src/options_arena/agents/bear.py`
   - WHY: orphaned from pipeline — not imported in orchestrator or __init__
2. Delete `src/options_arena/agents/prompts/bull.py` and `prompts/bear.py`
   - WHY: only consumers were the deleted bull/bear agents
3. In `agents/prompts/__init__.py`, remove BULL_SYSTEM_PROMPT and BEAR_SYSTEM_PROMPT imports and __all__ entries
   - WHY: source files deleted
4. In `agents/_parsing.py`, remove 6 DebateDeps fields: `opponent_argument`, `bear_counter_argument`, `bull_response`, `bear_response`, `bull_rebuttal`, `vol_response`. Delete `build_cleaned_trade_thesis()` function
   - WHY: fields never set in 6-agent pipeline. Keep `spread_analysis` and `constraint_warnings` (LIVE)
5. In `agents/orchestrator.py`, delete `_opposite_direction()` (line 778). Grep for and remove any kwargs setting the 6 dead fields
   - WHY: zero call sites, dead legacy helper
6. In `agents/model_config.py`, delete `_resolve_api_key()` (line 286)
   - WHY: deprecated shim, no production callers
7. In `agents/volatility.py` (lines 44-51), `flow_agent.py` (lines 45-52), `fundamental_agent.py` (lines 49-56): delete dead `if ctx.deps.bull_response` and `if ctx.deps.bear_response` blocks
   - WHY: always False — fields never set on DebateDeps in pipeline
8. In `api/deps.py`, delete `get_financial_datasets()`, `get_intelligence()`, `get_openbb()` functions
   - WHY: DI providers never used in any route's `Depends()`
9. In `api/schemas.py`, delete `SectorInfo` class (line 685)
   - WHY: not imported in any route or other module
10. In `models/analysis.py`, remove `ml_regime`, `ml_regime_confidence`, `flow_anomaly_score` from MarketContext
    - WHY: never written to on MarketContext (note: same-named fields on IndicatorSignals in scan.py are LIVE — different model)
11. In `models/config.py`, remove `enable_clustering`, `contract_n_clusters`, `enable_flow_anomaly` from MLConfig. Remove `enable_regime_weights` from DebateConfig. Remove `contract_n_clusters` validator
    - WHY: no code path checks these fields to gate any feature
12. In `models/openbb.py`, delete `OpenBBHealthStatus` class. Remove from `models/__init__.py` re-exports
    - WHY: re-exported but never imported outside models package
13. Delete tests: `test_bull.py` (203 lines), `test_bear.py` (186 lines). In `test_prompt_enhancements.py` remove `TestBuildCleanedTradeThesis` (lines 180-220). In `test_orchestrator.py` remove `TestOppositeDirection` (lines 305-320). In `test_model_config.py` remove `TestResolveApiKey` (lines 32-55). In `test_schemas.py` remove `SectorInfo` tests (lines 216-233). In `test_openbb_models.py` remove `TestOpenBBHealthStatus` (lines 580-654). In `test_analysis.py` remove tests for dead MarketContext fields. Update `test_parsing.py` `TestDebateDeps` class to reflect removed fields
    - WHY: tests for deleted code

### Verify
```bash
uv run ruff check src/options_arena/agents/ src/options_arena/api/ src/options_arena/models/ --fix
uv run ruff format src/options_arena/agents/ src/options_arena/api/ src/options_arena/models/
uv run mypy src/options_arena/agents/ src/options_arena/api/ src/options_arena/models/ --strict
uv run pytest tests/unit/agents/ tests/unit/api/ tests/unit/models/ -v --timeout=60
uv run pytest -m critical -q
```

### Done
- `agents/bull.py`, `agents/bear.py`, `agents/prompts/bull.py`, `agents/prompts/bear.py` do not exist
- `DebateDeps` has `spread_analysis` and `constraint_warnings` but NOT `opponent_argument`, `bear_counter_argument`, `bull_response`, `bear_response`, `bull_rebuttal`, `vol_response`
- `DebateResult` still has `bull_response`/`bear_response`/`bull_rebuttal`/`vol_response` (DB compat preserved)
- No lint/type/test errors in modified files

## Test Plan

### Test Files
- `tests/unit/agents/test_bull.py` — DELETE entirely (203 lines)
- `tests/unit/agents/test_bear.py` — DELETE entirely (186 lines)
- `tests/unit/agents/test_prompt_enhancements.py` — remove `TestBuildCleanedTradeThesis` (lines 180-220)
- `tests/unit/agents/test_orchestrator.py` — remove `TestOppositeDirection` (lines 305-320)
- `tests/unit/agents/test_model_config.py` — remove `TestResolveApiKey` (lines 32-55)
- `tests/unit/agents/test_parsing.py` — update `TestDebateDeps` (lines 53-203) for removed fields
- `tests/unit/api/test_schemas.py` — remove `SectorInfo` tests (lines 216-233)
- `tests/unit/models/test_openbb_models.py` — remove `TestOpenBBHealthStatus` (lines 580-654)
- `tests/unit/models/test_analysis.py` — remove dead MarketContext field tests
- `tests/integration/test_openbb_integration.py` — remove bull/bear agent imports (lines 20-21)

### Test Cases
```python
# No new tests needed — this is pure deletion
# Existing tests should pass after cleanup
```

### Edge Cases
- `DebateResult` backward compat: fields must NOT be removed from DebateResult model
- `flow_anomaly_score` on `IndicatorSignals` (scan.py) is LIVE — only remove from `MarketContext`
- `ml_regime_confidence` on `IndicatorSignals` (scan.py) is LIVE — only remove from `MarketContext`

### Mocking Strategy
- No new mocks needed. Update existing `DebateDeps` fixtures in `tests/unit/agents/conftest.py` to remove dead fields

## Dependencies
- None — first task in epic

## Effort Estimate
- Size: L
- Hours: 4-6
- Parallel: false (touches many shared __init__.py files)

## Definition of Done
- [ ] All 4 agent files deleted
- [ ] DebateDeps cleaned (6 fields removed, 2 preserved)
- [ ] All dead helpers/functions deleted
- [ ] Dead config/model fields removed
- [ ] ~700 lines source + ~500 lines tests removed
- [ ] `ruff check`, `mypy --strict`, `pytest` all green
