---
name: Remove 5 dead analytics API endpoints
status: open
created: 2026-03-23T13:34:29Z
updated: 2026-03-23T13:51:04Z
github: https://github.com/jobu711/options_arena/issues/726
depends_on: []
parallel: true
conflicts_with: [735, 714]
test_files: []
---

# Task: Remove 5 dead analytics API endpoints

## Description

Remove 5 dead endpoints from `api/routes/analytics.py` that have zero frontend callers.
Also remove the `RecommendationCostSummary` schema from `api/schemas.py` that is only
used by the deleted `/api/analytics/recommendation-costs` endpoint.

## Acceptance Criteria

- [ ] `GET /api/analytics/indicator-attribution/{indicator}` removed (~line 68-80)
- [ ] `GET /api/analytics/risk-metrics` removed (~line 235-244)
- [ ] `GET /api/analytics/correlation` removed (~line 246-299)
- [ ] `GET /api/analytics/recommendation-costs` removed (~line 301-328)
- [ ] `GET /api/analytics/scan/{scan_id}/contracts` removed (~line 141-150)
- [ ] `RecommendationCostSummary` schema removed from `api/schemas.py` (~lines 954-964)
- [ ] Related test cases removed or updated
- [ ] Remaining analytics endpoints still function

## Technical Details

- These 5 endpoints have zero frontend callers (confirmed by Vue SPA codebase audit)
- The CLI commands providing equivalent functionality are NOT affected
- Other analytics endpoints (win-rate, score-calibration, holding-period, delta-performance, summary,
  collect-outcomes, backtest/*) remain active
- `RecommendationCostSummary` in `schemas.py` is only imported by the recommendation-costs endpoint

## Execution

### Read First
Files that MUST be read before starting this task:
- `src/options_arena/api/routes/analytics.py` — identify exact line ranges for each endpoint
- `src/options_arena/api/schemas.py` — locate RecommendationCostSummary
- `tests/unit/api/test_analytics_routes.py` — identify test cases for dead endpoints

### Action
1. In `src/options_arena/api/routes/analytics.py`, remove the 5 endpoint functions:
   - `GET /api/analytics/indicator-attribution/{indicator}` (~lines 68-80)
   - `GET /api/analytics/scan/{scan_id}/contracts` (~lines 141-150)
   - `GET /api/analytics/risk-metrics` (~lines 235-244)
   - `GET /api/analytics/correlation` (~lines 246-299)
   - `GET /api/analytics/recommendation-costs` (~lines 301-328)
   - WHY: Zero frontend callers; CLI equivalents exist
2. Remove any imports in `analytics.py` that become unused after endpoint deletion
   - WHY: Unused imports fail ruff lint
3. In `src/options_arena/api/schemas.py`, remove `RecommendationCostSummary` class (~lines 954-964)
   - WHY: Only consumer was deleted endpoint
4. In `tests/unit/api/test_analytics_routes.py`, remove test cases for the 5 deleted endpoints
   - WHY: Tests for deleted endpoints serve no purpose
5. Verify remaining analytics endpoints still have their tests
   - WHY: Don't accidentally delete tests for live endpoints

### Verify
```
uv run python -c "from options_arena.api.routes.analytics import router; print(f'{len(router.routes)} routes OK')"
uv run ruff check src/options_arena/api/routes/analytics.py src/options_arena/api/schemas.py
uv run mypy src/options_arena/api/routes/analytics.py --strict
uv run pytest tests/unit/api/test_analytics_routes.py -v
```

### Done
- `analytics.py` has 5 fewer route handlers
- `RecommendationCostSummary` no longer exists in schemas.py
- Remaining analytics endpoints still pass tests
- No lint or type errors in modified files

## Test Plan

### Test Files
- `tests/unit/api/test_analytics_routes.py` — remove dead endpoint tests, keep live ones

### Test Cases
```python
# Verify remaining endpoints still work:
# - GET /api/analytics/win-rate
# - GET /api/analytics/score-calibration
# - GET /api/analytics/holding-period
# - etc.
# Run: uv run pytest tests/unit/api/test_analytics_routes.py -v
```

### Edge Cases
- Shared imports: if deleted endpoints share imports with live endpoints, only remove unused ones
- Schema references: verify RecommendationCostSummary isn't imported elsewhere
- Route ordering: deleting routes shouldn't affect other route matching

### Mocking Strategy
- No mocking needed — pure deletion task

## Dependencies
- None

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: true (conflicts with 735/714 on schemas.py — coordinate edits)
