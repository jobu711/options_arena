---
name: Document neural pricing modules as experimental
status: open
created: 2026-03-23T13:34:29Z
updated: 2026-03-23T13:51:04Z
github: https://github.com/jobu711/options_arena/issues/742
depends_on: []
parallel: true
conflicts_with: []
test_files: []
---

# Task: Document neural pricing modules as experimental

## Description

Add `# EXPERIMENTAL: requires [neural] extra` header comments to `pricing/trajectory.py`
(407 lines) and `pricing/neural_surface.py` (325 lines). These modules are well-tested,
behind guarded imports, and don't affect users who don't install `[neural]`. Keeping them
avoids losing 732 lines of working code with no runtime cost.

Decision: **KEEP with documentation** (per epic FR-3.4 recommendation).

## Acceptance Criteria

- [ ] `pricing/trajectory.py` has experimental header comment
- [ ] `pricing/neural_surface.py` has experimental header comment
- [ ] Module docstrings updated to note `[neural]` dependency
- [ ] No functional changes to either file

## Technical Details

- Both files use guarded `try/except ImportError` for PyTorch/Lightning imports
- The `[neural]` optional extra in `pyproject.toml` lists PyTorch + Lightning deps
- These modules are NOT shipped in the default install — only with `pip install .[neural]`
- The code is well-tested with existing test coverage

## Execution

### Read First
Files that MUST be read before starting this task:
- `src/options_arena/pricing/trajectory.py` — read first ~10 lines for current docstring
- `src/options_arena/pricing/neural_surface.py` — read first ~10 lines for current docstring

### Action
1. In `src/options_arena/pricing/trajectory.py`, add header after any existing module docstring:
   ```python
   # EXPERIMENTAL: requires [neural] extra (PyTorch + Lightning)
   # Install with: uv pip install ".[neural]"
   # This module is behind guarded imports and has no effect without the extra.
   ```
   - WHY: Documents the optional/experimental nature for future developers
2. In `src/options_arena/pricing/neural_surface.py`, add the same header
   - WHY: Same rationale — documents experimental status

### Verify
```
uv run ruff check src/options_arena/pricing/trajectory.py src/options_arena/pricing/neural_surface.py
uv run mypy src/options_arena/pricing/trajectory.py src/options_arena/pricing/neural_surface.py --strict
```

### Done
- Both files have `# EXPERIMENTAL` header comments
- No functional changes (same behavior, same tests pass)
- Ruff and mypy clean

## Test Plan

### Test Files
- No new test files — documentation-only change

### Test Cases
```python
# Verify no functional changes:
# uv run pytest tests/unit/pricing/ -v
```

### Edge Cases
- Ensure header comment doesn't interfere with existing module docstring
- Ensure ruff doesn't flag comment placement

### Mocking Strategy
- No mocking needed — documentation-only task

## Dependencies
- None

## Effort Estimate
- Size: XS
- Hours: 0.1
- Parallel: true
