---
name: Verification — lint + typecheck + tests + docs regen
status: open
created: 2026-03-23T13:34:29Z
updated: 2026-03-23T13:51:04Z
github: https://github.com/jobu711/options_arena/issues/743
depends_on: [713, 718, 726, 735, 739, 714, 720, 733, 742]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Verification — lint + typecheck + tests + docs regen

## Description

Final verification pass after all deletion tasks complete. Run the full lint, typecheck,
and test suite to confirm no broken imports, dangling references, or regressions. Regenerate
documentation and TLDR cache. Verify line count reduction meets epic target (~2,351 lines).

## Acceptance Criteria

- [ ] `ruff check` passes with zero errors
- [ ] `ruff format` produces no changes
- [ ] `mypy --strict` passes with zero errors
- [ ] Full test suite passes (excluding deleted test files)
- [ ] `python tools/docgen.py` regenerates successfully
- [ ] `python tools/tldr_analyzer.py` refreshes TLDR cache
- [ ] No grep hits for deleted symbols in production code
- [ ] Line count reduction documented

## Technical Details

Expected line reductions:
- IntelligenceService + models: ~997 lines
- 14 dead API endpoints: ~330 lines
- Eval harness framework: ~1,024 lines (evals/ + models/eval.py + data/_eval.py + cli/eval.py)
- Total target: ~2,351 production lines + associated test lines

Symbols that should NOT appear in production code after cleanup:
- `IntelligenceService`, `IntelligenceConfig`, `IntelligencePackage`
- `AnalystSnapshot`, `InsiderSnapshot`, `InstitutionalSnapshot`
- `EvalConfig`, `EvalMixin`, `EvalDefinition`, `EvalRun`, `EvalReport`
- `eval_router`, `learning_router`
- `MetadataStats`, `IndexStarted`, `RecommendationCostSummary`

## Execution

### Read First
Files that MUST be read before starting this task:
- Epic file `.claude/epics/dead-code-cleanup-orphans/epic.md` — review success criteria

### Action
1. Run full lint + format:
   ```
   uv run ruff check . --fix && uv run ruff format .
   ```
   - WHY: Catch unused imports, formatting issues from deletions
2. Run strict type checking:
   ```
   uv run mypy src/ --strict
   ```
   - WHY: Catch type errors from removed symbols
3. Run full test suite:
   ```
   uv run pytest tests/ -v --tb=short
   ```
   - WHY: Confirm no regressions from deletions
4. Grep for deleted symbols in production code:
   ```
   grep -r "IntelligenceService\|IntelligenceConfig\|IntelligencePackage" src/
   grep -r "EvalConfig\|EvalMixin\|EvalDefinition\|EvalRun" src/
   grep -r "eval_router\|learning_router" src/
   grep -r "MetadataStats\|IndexStarted\|RecommendationCostSummary" src/
   ```
   - WHY: Confirm complete removal (grep should return nothing)
5. Regenerate documentation:
   ```
   python tools/docgen.py
   python tools/tldr_analyzer.py
   ```
   - WHY: Docs must reflect current codebase state
6. Count lines removed:
   ```
   git diff --stat master...HEAD
   ```
   - WHY: Verify we meet the epic's target of ~2,351 lines removed

### Verify
```
uv run ruff check . && uv run ruff format --check . && uv run mypy src/ --strict && uv run pytest tests/ -v
```

### Done
- All 4 gates pass: lint, format, typecheck, tests
- Zero grep hits for deleted symbols in `src/`
- Documentation regenerated
- `git diff --stat` shows >= 2,000 lines removed

## Test Plan

### Test Files
- No new test files — this is a verification task

### Test Cases
```python
# Full suite verification:
# uv run pytest tests/ -v --tb=short
# Expected: all tests pass with 0 errors, 0 failures
```

### Edge Cases
- Deleted test fixtures referenced by surviving tests (conftest.py cleanup)
- `__init__.py` re-exports that reference deleted modules
- Migration files referencing deleted models (acceptable — migrations are append-only)

### Mocking Strategy
- No mocking needed — verification task

## Dependencies
- [ ] All tasks 713-742 must complete first

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: false (final gate — depends on all other tasks)
