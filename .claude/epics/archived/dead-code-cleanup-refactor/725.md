---
name: Delete 6 dead context renderers from _parsing.py + remove re-exports
status: open
created: 2026-03-23T13:34:26Z
updated: 2026-03-23T13:50:12Z
github: https://github.com/jobu711/options_arena/issues/725
depends_on: []
parallel: true
conflicts_with: [721]
test_files: []
---

# Task: Delete 6 dead context renderers from _parsing.py + remove re-exports

## Description
Delete 6 dead domain-specific context renderer functions from `agents/_parsing.py` that
belonged to the old debate system where agents received pre-partitioned context. The new
recommendation system uses `render_context_block()` instead. Also delete helper functions
only used by the dead renderers, and remove re-exports from `agents/__init__.py`.

## Acceptance Criteria
- [ ] 6 dead renderer functions deleted from `_parsing.py`
- [ ] Helper functions used only by dead renderers deleted (`_render_optional`, `_render_regime_label`, `_format_dollars`, `_render_identity_block`)
- [ ] Dead re-exports removed from `agents/__init__.py`
- [ ] Live functions (`render_context_block`, `compute_citation_density`, `strip_think_tags`, etc.) unchanged
- [ ] `ruff check` and `mypy --strict` pass

## Technical Details

### Functions to delete from `_parsing.py`:
| Function | Lines | Purpose (dead) |
|----------|-------|----------------|
| `_render_optional` | 165-169 | Helper for dead renderers only |
| `_render_regime_label` | 172-177 | Helper for dead renderers only |
| `_format_dollars` | 180-189 | Helper for dead renderers only |
| `_render_identity_block` | 192-227 | Identity block for dead renderers |
| `render_trend_context` | 230-270 | Old trend agent context partitioning |
| `render_volatility_context` | 273-393 | Old volatility agent context partitioning |
| `render_flow_context` | 396-440 | Old flow agent context partitioning |
| `render_fundamental_context` | 443-578 | Old fundamental agent context partitioning |
| `render_macro_context` | 581-608 | Old macro context block |

Total: ~443 lines of dead code.

### Functions to KEEP:
- `strip_think_tags` (52-65) — used by agents
- `build_cleaned_domain_assessment` (109-129) — used by agents
- `DebateResult` class (132-163) — backward compat for old data
- `_render_neural_context` (611-621) — helper for live `render_context_block`
- `render_context_block` (624-927) — LIVE, used by recommendation orchestrator
- `compute_citation_density` (934-950) — LIVE
- `TOOL_RESPONSE_FORMAT` (68-78) — LIVE constant
- `PROMPT_RULES_APPENDIX` (80-106) — LIVE constant

### Re-exports to remove from `agents/__init__.py`:
- `render_trend_context`
- `render_volatility_context`
- `render_flow_context`
- `render_fundamental_context`
- `render_macro_context`

### Re-exports to KEEP:
- `render_context_block` — LIVE

## Execution

### Read First
- `src/options_arena/agents/_parsing.py` — verify line ranges and confirm helpers are dead-only
- `src/options_arena/agents/__init__.py` — find re-export lines to remove
- `src/options_arena/agents/CLAUDE.md` — module conventions

### Action
1. Read `_parsing.py` to confirm exact line ranges of all 9 dead functions/helpers
   - WHY: Line numbers may have shifted; verify before bulk delete
2. Verify `_render_optional`, `_render_regime_label`, `_format_dollars` have NO callers in live code
   - WHY: These helpers might be used by `render_context_block`; grep to confirm dead-only
3. Delete all 9 functions (lines ~165-608)
   - WHY: Zero production callers; old debate context partitioning replaced by `render_context_block`
4. Clean up any imports that become unused after deletion
   - WHY: Deleted renderers may have imported types not used by remaining code
5. Remove 5 dead re-exports from `agents/__init__.py`
   - WHY: These symbols no longer exist in `_parsing.py`
6. Run `ruff check` to catch any remaining issues
   - WHY: Automated detection of missed cleanup

### Verify
```
uv run ruff check src/options_arena/agents/_parsing.py --fix
uv run ruff format src/options_arena/agents/_parsing.py
uv run mypy src/options_arena/agents/_parsing.py --strict
uv run ruff check src/options_arena/agents/__init__.py --fix
```

### Done
- `_parsing.py` reduced by ~443 lines
- Zero references to deleted functions remain in `agents/` package
- `render_context_block` and all other live functions work unchanged
- `ruff check` and `mypy --strict` pass

## Test Plan

### Test Files
- No new test files — pure deletion

### Test Cases
Live function `render_context_block` verified by existing tests (not in the deleted set).

### Edge Cases
- Verify `_render_neural_context` does NOT call any deleted helpers
- Verify no `__all__` lists reference deleted functions
- Grep for string-based references to deleted function names

### Mocking Strategy
- N/A — pure deletion task

## Dependencies
- None

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: true (but conflicts_with 721 on `_parsing.py`)
