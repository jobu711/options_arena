---
name: Verification — lint + typecheck + tests + docs regen
status: open
created: 2026-03-23T13:34:26Z
updated: 2026-03-23T13:50:12Z
github: https://github.com/jobu711/options_arena/issues/729
depends_on: [712, 717, 725, 715, 719, 728, 716, 721]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Verification — lint + typecheck + tests + docs regen

## Description
Final verification gate after all 8 preceding tasks are complete. Run lint, format,
type checking, full test suite, and regenerate documentation. This ensures the combined
effect of all changes leaves the codebase green.

## Acceptance Criteria
- [ ] `ruff check .` passes with zero errors
- [ ] `ruff format .` reports no changes needed
- [ ] `mypy src/ --strict` passes with zero errors
- [ ] `pytest -m "not exhaustive" -n auto -q` passes (full standard suite)
- [ ] `pytest -m critical -q` passes (critical tier)
- [ ] `python tools/docgen.py` succeeds (docs regenerated)
- [ ] `python tools/tldr_analyzer.py` succeeds (TLDR cache refreshed)
- [ ] No new `__init__.py` re-exports referencing deleted symbols
- [ ] Git diff confirms net line reduction >= 1,000 lines

## Technical Details

### Verification sequence:
1. Lint + format (fast, catches import errors immediately)
2. Type check (catches missing types, wrong signatures)
3. Critical tests (fast sanity check)
4. Full standard test suite (comprehensive)
5. Docs regen (ensures technical reference is current)

### Expected outcomes:
- `rendering.py`: ~1,019 → ~492 lines (-527)
- `_parsing.py`: reduced by ~443 lines
- 6 test files deleted (~1,690 lines)
- Cache duplication eliminated
- Health check boilerplate consolidated (~80 lines saved)
- Config validators deduplicated (~54 lines saved)
- Enrichment ratio dead path removed
- **Net reduction: ~1,800+ lines**

## Execution

### Read First
- `CLAUDE.md` — verification commands section

### Action
1. Run `uv run ruff check . --fix && uv run ruff format .`
   - WHY: Fix any remaining lint/format issues from combined changes
2. Run `uv run mypy src/ --strict`
   - WHY: Verify type correctness across all modified modules
3. Run `uv run pytest -m critical -q`
   - WHY: Fast sanity check before full suite
4. Run `uv run pytest -m "not exhaustive" -n auto -q`
   - WHY: Full standard test suite validates no regressions
5. Run `python tools/docgen.py`
   - WHY: Regenerate technical reference after public API changes
6. Run `python tools/tldr_analyzer.py`
   - WHY: Refresh TLDR cache for modified files
7. Verify net line reduction with `git diff --stat`
   - WHY: Confirm epic success criteria (>= 1,000 lines removed)
8. Verify no stale `__init__.py` re-exports with targeted grep
   - WHY: Deleted functions must not be referenced anywhere

### Verify
```
uv run ruff check . --fix && uv run ruff format .
uv run mypy src/ --strict
uv run pytest -m critical -q
uv run pytest -m "not exhaustive" -n auto -q
python tools/docgen.py
python tools/tldr_analyzer.py
git diff --stat HEAD~$(git rev-list --count epic/dead-code-cleanup-refactor ^master)
```

### Done
- All 4 CI gates green (lint, typecheck, tests, frontend)
- Technical reference regenerated
- Git diff confirms >= 1,000 net lines removed
- Zero stale references to deleted functions

## Test Plan

### Test Files
- No new tests — this task runs the full suite

### Test Cases
Full test suite execution validates all changes from Tasks 712-721.

### Edge Cases
- N/A — this is a verification task

### Mocking Strategy
- N/A — runs existing test infrastructure

## Dependencies
- [712, 717, 725, 715, 719, 728, 716, 721] — ALL previous tasks must be complete

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: false (final gate)
