---
name: (DEFERRED) Plan data migration for ai_theses sunset
status: deferred
created: 2026-03-23T13:36:04Z
updated: 2026-03-23T13:50:24Z
github: https://github.com/jobu711/options_arena/issues/736
depends_on: []
parallel: true
conflicts_with: []
test_files:
  - tests/unit/data/test_debate_migration.py
---

# Task: (DEFERRED) Plan data migration script for ai_theses → recommendation_results

## Description

**This task is intentionally DEFERRED.** It documents the plan for FR-4.2
(sunset old debate backward-compat paths) but does NOT execute it. The
prerequisite — a data migration that transforms `ai_theses` rows into
`recommendation_results` format — must be built and validated first.

This task creates a planning document and migration script skeleton that a
future epic can pick up. No production code changes.

## Acceptance Criteria

- [ ] Migration plan document written to `docs/plans/debate-sunset-migration.md`
- [ ] Documents: source schema (`ai_theses`), target schema (`recommendation_results`), field mapping, data transformation rules
- [ ] Documents: ~297 lines of API code to remove after migration
- [ ] Documents: ~350 lines of model classes to remove after migration
- [ ] Documents: ~260 lines of reporting code to remove after migration
- [ ] Migration script skeleton in `tools/migrate_theses.py` (NOT executed)
- [ ] Estimated total lines removable: ~907

## Technical Details

### What gets sunset (FR-4.2 scope — NOT executed now)

**API routes** (~297 lines):
- `api/routes/debate.py`: Dual-table lookup logic in `GET /api/debate/{id}`
  that checks both `ai_theses` and `recommendation_results` tables
- `api/routes/export.py`: Old debate export path

**Model classes** (~350 lines in `models/analysis.py`):
- `AgentResponse`, `TradeThesis`, `VolatilityThesis`, `FlowThesis`,
  `RiskAssessment`, `FundamentalThesis`, `ContrarianThesis`,
  `ExtendedTradeThesis`

**Agent compat** (~misc lines):
- `DebateResult` from `agents/_parsing.py` and `_context.py`

**Reporting** (~260 lines in `reporting/debate_export.py`):
- Old debate sub-renderers: `_render_agent_response()`,
  `_render_risk_assessment()`, `_render_thesis_details()`, etc.

### Migration field mapping (to document)

| `ai_theses` field | → | `recommendation_results` field |
|-------------------|---|-------------------------------|
| `ticker` | → | `ticker` |
| `direction` | → | `direction` |
| `confidence` | → | `confidence` |
| `summary` | → | `rationale` |
| `bull_thesis` | → | assessment JSON (TrendAssessment) |
| `bear_thesis` | → | assessment JSON (ContrarianAssessment) |
| `risk_assessment` | → | assessment JSON (RiskDeskAssessment) |
| ... | → | ... (full mapping in plan doc) |

### Prerequisites before execution

1. Migration script must handle: NULL fields, old enum values, JSON parsing
2. All `ai_theses` rows must be successfully migrated
3. API endpoint must serve historical debates from new table
4. Integration test must verify old debate IDs return correct data
5. Rollback plan must exist

## Execution

### Read First
Files that MUST be read before starting this task:
- `src/options_arena/api/routes/debate.py` — dual-table lookup logic
- `src/options_arena/models/analysis.py` — old thesis model classes
- `src/options_arena/reporting/debate_export.py` — old debate renderers
- `src/options_arena/agents/_context.py` — `DebateResult` backward compat
- `data/migrations/` — understand migration numbering

### Action
1. Create `docs/plans/debate-sunset-migration.md` with full migration plan
   - WHY: documents the deferred work for a future epic to pick up
2. Create `tools/migrate_theses.py` skeleton with:
   - Schema documentation
   - Field mapping constants
   - Placeholder functions: `migrate_row()`, `verify_migration()`, `rollback()`
   - WHY: gives future developer a head start
3. Document line counts and file list for removal
   - WHY: quantifies the value of executing the sunset

### Verify
```
# No production code changes — just verify docs are well-formed
python -c "import ast; ast.parse(open('tools/migrate_theses.py').read())"
```

### Done
- Migration plan document exists with full field mapping
- Migration script skeleton parses without errors
- Line count estimate documented (~907 lines removable)
- Future epic can pick this up without re-analysis

## Test Plan

### Test Files
- `tests/unit/data/test_debate_migration.py` (skeleton only — tests the
  migration script's `migrate_row()` once it's implemented)

### Test Cases
```python
class TestDebateMigration:
    def test_migrate_row_placeholder(self) -> None:
        """Placeholder — will test row transformation when implemented."""
        ...

    def test_field_mapping_completeness(self) -> None:
        """Verify every ai_theses column has a mapping target."""
        ...

    def test_null_field_handling(self) -> None:
        """Verify NULL fields in ai_theses produce valid output."""
        ...
```

### Edge Cases
- `ai_theses` rows with NULL confidence (old data before validation)
- Old direction enum values that may not match new `SignalDirection`
- JSON-encoded thesis fields that may be malformed

### Mocking Strategy
- Use in-memory SQLite with sample `ai_theses` rows

## Dependencies
- None — this is a planning task, not blocked by anything

## Effort Estimate
- Size: S
- Hours: 0.5
- Parallel: true (independent of all other tasks)

## Definition of Done
- [ ] Migration plan document written
- [ ] Migration script skeleton created
- [ ] Line removal estimate documented
- [ ] No production code changed
