---
name: Write tests for bull rebuttal feature
status: open
created: 2026-02-25T10:56:30Z
updated: 2026-02-25T11:01:42Z
github: https://github.com/jobu711/options_arena/issues/99
depends_on: [94, 95, 96, 97, 98]
parallel: false
conflicts_with: []
---

# Task: Write tests for bull rebuttal feature

## Description
Add ~10 tests covering the bull rebuttal feature across all modified modules:
bull agent prompt switching, orchestrator rebuttal phase, risk prompt injection,
and CLI rendering. Uses TestModel for agent tests (no real LLM calls).

## Acceptance Criteria
- [ ] Bull rebuttal prompt: `bear_counter_argument` set -> prompt includes `<<<BEAR_COUNTER>>>`
- [ ] Bull initial prompt: `bear_counter_argument` is None -> prompt does NOT include rebuttal instructions
- [ ] Orchestrator: `enable_rebuttal=True` runs 4+ agents (bull, bear, bull-rebuttal, [vol], risk)
- [ ] Orchestrator: `enable_rebuttal=False` (default) skips rebuttal phase entirely
- [ ] Orchestrator: rebuttal output is valid `AgentResponse` with `agent_name="bull"`
- [ ] Orchestrator: `total_usage` includes rebuttal agent usage when rebuttal enabled
- [ ] Orchestrator: `DebateResult.bull_rebuttal` is populated when rebuttal enabled
- [ ] Orchestrator: `DebateResult.bull_rebuttal` is None when rebuttal disabled
- [ ] Risk prompt: includes `<<<BULL_REBUTTAL>>>` when `bull_rebuttal` is set on deps
- [ ] Risk prompt: omits rebuttal block when `bull_rebuttal` is None
- [ ] Rendering: rebuttal panel rendered when `bull_rebuttal` is not None on DebateResult

## Technical Details
- Files:
  - `tests/unit/agents/test_bull.py` — add tests for dynamic prompt switching
  - `tests/unit/agents/test_orchestrator.py` — add tests for rebuttal phase
  - `tests/unit/agents/test_risk.py` — add tests for rebuttal injection in prompt
  - `tests/unit/cli/test_rendering.py` — add test for rebuttal panel rendering
- Use `TestModel` for agent override tests (Context7-verified pattern)
- Use `models.ALLOW_MODEL_REQUESTS = False` at module level
- Use existing `conftest.py` fixtures: `mock_debate_deps`, `mock_agent_response`, etc.
- For orchestrator tests: override all agents with `TestModel`, set `enable_rebuttal=True`
  on config, verify result structure
- For prompt tests: create deps with/without `bear_counter_argument`, call prompt function
  directly, assert on string content
- Token savings test: verify only `key_points` are injected (check `bear_counter_argument`
  is a bullet list, not the full `argument` text)

## Dependencies
- [ ] Tasks #94-#98 — all implementation must be complete

## Effort Estimate
- Size: M
- Hours: 3
- Parallel: false (depends on all implementation tasks)

## Definition of Done
- [ ] ~10 tests added across test modules
- [ ] All tests pass: `uv run pytest tests/ -v`
- [ ] No regressions in existing test suite
- [ ] `uv run ruff check . --fix && uv run ruff format .` passes
- [ ] `uv run mypy src/ --strict` passes
