---
name: Full test suite + lint + typecheck verification
status: open
created: 2026-03-07T22:34:25Z
updated: 2026-03-07T22:39:52Z
github: https://github.com/jobu711/options_arena/issues/350
depends_on: [348, 349]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Full test suite + lint + typecheck verification

## Description

Run the complete verification gauntlet after all code tasks (346-349) are complete. This is the quality gate before merge — ensures no regressions, all new tests pass, lint is clean, and type checking passes.

## Acceptance Criteria

- [ ] `uv run ruff check . --fix && uv run ruff format .` — zero violations
- [ ] `uv run mypy src/ --strict` — zero errors
- [ ] `uv run pytest tests/unit -n auto -q` — all unit tests pass (3,921+ existing + new)
- [ ] `uv run pytest tests/integration -v` — all integration tests pass
- [ ] All 38 E2E Playwright tests pass
- [ ] No test regressions from existing suite
- [ ] New tests from Tasks 346-349 all pass
- [ ] Domain renderers produce shorter context than full render (NFR-1)
- [ ] Log-odds pooling handles all edge cases (NFR-7)

## Technical Details

### Verification Steps (in order)
1. `uv run ruff check . --fix && uv run ruff format .` — lint + format
2. `uv run mypy src/ --strict` — type checking
3. `uv run pytest tests/unit -n auto -q` — parallel unit tests
4. `uv run pytest tests/unit/agents/test_domain_renderers.py -v` — Task 1 tests
5. `uv run pytest tests/unit/agents/test_log_odds_pool.py tests/unit/agents/test_orchestrator_wiring.py -v` — Task 2 tests
6. `uv run pytest tests/unit/models/test_ensemble_fields.py tests/unit/agents/test_vote_entropy.py tests/unit/agents/test_volatility_direction.py -v` — Task 3 tests
7. `uv run pytest tests/unit/data/test_agent_predictions.py tests/unit/models/test_agent_prediction_model.py -v` — Task 4 tests
8. `uv run pytest tests/ -v` — full suite verbose (catch any interaction bugs)
9. E2E tests: `cd web && npx playwright test`

### Key Considerations
- If any test fails, fix the issue in the relevant code (not the test) unless the test expectation is wrong
- 2 pre-existing failures in `test_expanded_context.py` (NaN/Inf validators) are known — on master, not our fault
- Watch for import cycle issues from new re-exports
- Watch for typing issues from new model fields

## Test Plan

This IS the test task — no separate test files. The tests from Tasks 346-349 are verified here.

### Test Coverage Summary
From Tasks 346-349:
- `tests/unit/agents/test_domain_renderers.py` — 15+ cases (renderers, exclusions, edge cases)
- `tests/unit/agents/test_log_odds_pool.py` — 6+ cases (pooling math)
- `tests/unit/agents/test_orchestrator_wiring.py` — 4+ cases (context routing)
- `tests/unit/models/test_ensemble_fields.py` — 5+ cases (entropy field)
- `tests/unit/agents/test_vote_entropy.py` — 5+ cases (entropy math)
- `tests/unit/agents/test_volatility_direction.py` — 3+ cases (direction voting)
- `tests/unit/data/test_agent_predictions.py` — 5+ cases (persistence)
- `tests/unit/models/test_agent_prediction_model.py` — 5+ cases (model validators)

### Edge Cases
- Test interaction between partitioned context and existing full-context tests
- Test backward compatibility with existing serialized data

### Mocking Strategy
- N/A — this task runs existing tests, doesn't create new mocks

## Dependencies
- [ ] Task 348 (ensemble diversity)
- [ ] Task 349 (agent prediction persistence)

## Effort Estimate
- Size: S
- Hours: 2-3
- Parallel: false (must wait for all code tasks)

## Definition of Done
- [ ] All verification commands pass with zero errors
- [ ] No test regressions
- [ ] All new tests pass
- [ ] Ready for merge
