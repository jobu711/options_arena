---
name: Update Bull agent — integrate shared appendix and deduplicate validator
status: closed
created: 2026-02-25T14:23:09Z
updated: 2026-02-25T09:53:05Z
github: https://github.com/jobu711/options_arena/issues/78
depends_on: [77]
parallel: true
conflicts_with: []
---

# Task: Update Bull agent — integrate shared appendix and deduplicate validator

## Description
Update `src/options_arena/agents/bull.py` to append the shared `PROMPT_RULES_APPENDIX` to
its system prompt and replace the 29-line duplicated output validator with a 2-line
delegation to `build_cleaned_agent_response()`.

## Acceptance Criteria
- [ ] `PROMPT_RULES_APPENDIX` imported from `_parsing.py`
- [ ] `build_cleaned_agent_response` imported from `_parsing.py`
- [ ] `BULL_SYSTEM_PROMPT` appends `PROMPT_RULES_APPENDIX` (version bumped to v2.0)
- [ ] `strip_think_tags` import removed (no longer used directly)
- [ ] Output validator reduced to 2-line delegation:
      ```python
      @bull_agent.output_validator
      async def clean_think_tags(ctx: RunContext[DebateDeps], output: AgentResponse) -> AgentResponse:
          return build_cleaned_agent_response(output)
      ```
- [ ] Net line reduction: ~27 lines removed
- [ ] All existing bull agent tests pass unchanged

## Technical Details
- **File**: `src/options_arena/agents/bull.py`
- **Current state**: 29-line validator (lines 68-96) duplicates logic now in `_parsing.py`
- **Import changes**:
  - Add: `PROMPT_RULES_APPENDIX`, `build_cleaned_agent_response` from `_parsing`
  - Remove: `strip_think_tags` (no longer used directly in bull.py)
- **Prompt change**: `BULL_SYSTEM_PROMPT` string should end with `\n\n` + `PROMPT_RULES_APPENDIX`
  - Option A: Concatenate at module level: `BULL_SYSTEM_PROMPT = """...""" + "\n\n" + PROMPT_RULES_APPENDIX`
  - Option B: Concatenate in the `bull_system_prompt()` function return
  - Either approach is valid; Option A is simpler
- Update version comment from `# VERSION: v1.0` to `# VERSION: v2.0`

## Dependencies
- [ ] Task 001 — `PROMPT_RULES_APPENDIX` and `build_cleaned_agent_response` must exist in `_parsing.py`

## Effort Estimate
- Size: XS
- Hours: 1
- Parallel: true (can run alongside Tasks 003, 004 after 001 completes)

## Definition of Done
- [ ] Code updated in `bull.py`
- [ ] `uv run ruff check src/options_arena/agents/bull.py --fix`
- [ ] `uv run mypy src/options_arena/agents/bull.py --strict`
- [ ] Existing bull agent tests pass: `uv run pytest tests/ -v -k "bull"`
