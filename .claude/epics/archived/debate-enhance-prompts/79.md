---
name: Update Bear agent — integrate shared appendix and deduplicate validator
status: closed
created: 2026-02-25T14:23:09Z
updated: 2026-02-25T09:53:05Z
github: https://github.com/jobu711/options_arena/issues/79
depends_on: [77]
parallel: true
conflicts_with: []
---

# Task: Update Bear agent — integrate shared appendix and deduplicate validator

## Description
Update `src/options_arena/agents/bear.py` to append the shared `PROMPT_RULES_APPENDIX` to
its system prompt and replace the 29-line duplicated output validator with a 2-line
delegation to `build_cleaned_agent_response()`.

## Acceptance Criteria
- [ ] `PROMPT_RULES_APPENDIX` imported from `_parsing.py`
- [ ] `build_cleaned_agent_response` imported from `_parsing.py`
- [ ] `BEAR_SYSTEM_PROMPT` appends `PROMPT_RULES_APPENDIX` (version bumped to v2.0)
- [ ] `strip_think_tags` import removed (no longer used directly)
- [ ] Output validator reduced to 2-line delegation:
      ```python
      @bear_agent.output_validator
      async def clean_think_tags(ctx: RunContext[DebateDeps], output: AgentResponse) -> AgentResponse:
          return build_cleaned_agent_response(output)
      ```
- [ ] Net line reduction: ~27 lines removed
- [ ] All existing bear agent tests pass unchanged
- [ ] Dynamic prompt still correctly injects bull argument via `<<<BULL_ARGUMENT>>>` delimiters

## Technical Details
- **File**: `src/options_arena/agents/bear.py`
- **Current state**: 29-line validator (lines 80-108) duplicates logic now in `_parsing.py`
- **Import changes**:
  - Add: `PROMPT_RULES_APPENDIX`, `build_cleaned_agent_response` from `_parsing`
  - Remove: `strip_think_tags` (no longer used directly in bear.py)
- **Prompt change**: Bear uses a dynamic prompt, so `PROMPT_RULES_APPENDIX` must be appended
  to `BEAR_SYSTEM_PROMPT` at the module level (before the dynamic `<<<BULL_ARGUMENT>>>` injection).
  The dynamic prompt function `bear_dynamic_prompt()` should NOT be modified — it already
  appends the opponent argument after the base prompt.
- Update version comment from `# VERSION: v1.0` to `# VERSION: v2.0`

## Dependencies
- [ ] Task 001 — `PROMPT_RULES_APPENDIX` and `build_cleaned_agent_response` must exist in `_parsing.py`

## Effort Estimate
- Size: XS
- Hours: 1
- Parallel: true (can run alongside Tasks 002, 004 after 001 completes)

## Definition of Done
- [ ] Code updated in `bear.py`
- [ ] `uv run ruff check src/options_arena/agents/bear.py --fix`
- [ ] `uv run mypy src/options_arena/agents/bear.py --strict`
- [ ] Existing bear agent tests pass: `uv run pytest tests/ -v -k "bear"`
