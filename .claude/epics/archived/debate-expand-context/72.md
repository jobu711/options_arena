---
name: Add indicator and Greeks fields to MarketContext
status: closed
created: 2026-02-25T13:15:27Z
updated: 2026-02-25T13:20:16Z
github: https://github.com/jobu711/options_arena/issues/72
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Add indicator and Greeks fields to MarketContext

## Description
Expand `MarketContext` in `models/analysis.py` with 13 new optional fields for scoring
context, key indicators, contract Greeks beyond delta, and contract mid price. All new
fields have defaults so existing `MarketContext(...)` calls remain backward compatible.

## Acceptance Criteria
- [ ] `MarketContext` has 2 new scoring fields: `composite_score` (float, default 0.0), `direction_signal` (SignalDirection, default NEUTRAL)
- [ ] `MarketContext` has 6 new indicator fields: `adx`, `sma_alignment`, `bb_width`, `atr_pct`, `stochastic_rsi`, `relative_volume` (all `float | None = None`)
- [ ] `MarketContext` has 4 new Greeks fields: `target_gamma`, `target_theta`, `target_vega`, `target_rho` (all `float | None = None`)
- [ ] `MarketContext` has 1 new pricing field: `contract_mid` (`Decimal | None = None`)
- [ ] `field_serializer` added for `contract_mid` (Decimal -> str to prevent JSON precision loss)
- [ ] Existing `MarketContext(...)` calls (in orchestrator, tests) still work without modification
- [ ] `SignalDirection` import added to `analysis.py` (already imported in the file)
- [ ] `ruff check`, `ruff format`, `mypy --strict` all pass

## Technical Details
- **File**: `src/options_arena/models/analysis.py`
- Add `SignalDirection` to existing imports from `enums` (already present)
- New fields go after `data_timestamp` (or logically grouped after existing fields)
- `contract_mid` needs a `field_serializer` — extend the existing `serialize_decimal` to include it, or add a separate serializer
- All new fields have defaults, so `MarketContext` remains non-breaking
- `composite_score` default `0.0` (not None) — required float, always available from scan
- `direction_signal` default `SignalDirection.NEUTRAL` — always available from scan
- Indicators default to `None` — may not be computed for all tickers

## Dependencies
- [ ] None — this is the foundation task

## Effort Estimate
- Size: S
- Hours: 1-2
- Parallel: false (foundation for tasks #73, #74)

## Definition of Done
- [ ] Code implemented in `models/analysis.py`
- [ ] Existing tests still pass (`uv run pytest tests/ -v`)
- [ ] `uv run ruff check . --fix && uv run ruff format .` passes
- [ ] `uv run mypy src/ --strict` passes
