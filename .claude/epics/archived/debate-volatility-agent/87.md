---
name: Add VolatilityThesis model and re-export
status: open
created: 2026-02-25T15:00:36Z
updated: 2026-02-25T15:06:08Z
github: https://github.com/jobu711/options_arena/issues/87
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Add VolatilityThesis model and re-export

## Description
Create the `VolatilityThesis` Pydantic model in `models/analysis.py` and re-export it from `models/__init__.py`. This is the structured output type for the new Volatility Agent — a frozen, confidence-validated model with domain-specific fields for IV assessment, recommended strategy, target IV levels, and suggested strikes.

## Acceptance Criteria
- [ ] `VolatilityThesis` added to `src/options_arena/models/analysis.py`
- [ ] Model is `frozen=True` via `ConfigDict`
- [ ] `confidence` field has `field_validator` constraining to `[0.0, 1.0]` with `math.isfinite()` check
- [ ] `recommended_strategy` uses `SpreadType | None` enum, not raw string
- [ ] `iv_assessment` is `str` (values: "overpriced", "underpriced", "fair")
- [ ] All fields match epic spec: `iv_assessment`, `iv_rank_interpretation`, `confidence`, `recommended_strategy`, `strategy_rationale`, `target_iv_entry`, `target_iv_exit`, `suggested_strikes`, `key_vol_factors`, `model_used`
- [ ] Re-exported from `models/__init__.py` with `__all__` entry
- [ ] JSON roundtrip works: `VolatilityThesis.model_validate_json(vt.model_dump_json()) == vt`

## Technical Details
- File: `src/options_arena/models/analysis.py` — add after `TradeThesis`
- File: `src/options_arena/models/__init__.py` — add import and `__all__` entry
- Follow exact pattern of `TradeThesis`: frozen, `ConfigDict`, `field_validator` on confidence
- `target_iv_entry` and `target_iv_exit` are `float | None` (optional)
- `suggested_strikes` and `key_vol_factors` are `list[str]`
- Import `SpreadType` from `enums.py` (already exists)

## Dependencies
- [ ] None — this is a pure model addition with no external deps

## Effort Estimate
- Size: XS
- Hours: 1
- Parallel: true (can run in parallel with any task that doesn't touch models/)

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing (construction, frozen, confidence validation, JSON roundtrip)
- [ ] `uv run ruff check . --fix && uv run ruff format .` passes
- [ ] `uv run mypy src/ --strict` passes
