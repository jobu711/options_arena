---
name: Remove watchlist Python backend + unit tests
status: open
created: 2026-03-07T14:08:06Z
updated: 2026-03-07T14:13:13Z
github: https://github.com/jobu711/options_arena/issues/330
depends_on: []
parallel: false
conflicts_with: [331]
test_files: []
---

# Task: Remove watchlist Python backend + unit tests

## Description

Delete all Python watchlist source files and their unit tests, then clean up all
imports, re-exports, router registrations, and repository methods that reference
the watchlist feature.

## Acceptance Criteria

- [ ] `src/options_arena/models/watchlist.py` deleted
- [ ] `src/options_arena/cli/watchlist.py` deleted
- [ ] `src/options_arena/api/routes/watchlist.py` deleted
- [ ] `tests/unit/models/test_watchlist.py` deleted
- [ ] `tests/unit/cli/test_watchlist_cli.py` deleted
- [ ] `tests/unit/api/test_watchlist_routes.py` deleted
- [ ] `tests/unit/data/test_repository_watchlist.py` deleted
- [ ] Watchlist imports removed from `models/__init__.py` (lines ~93-98 + `__all__` entries ~200-204)
- [ ] Watchlist import removed from `cli/__init__.py` (line ~11)
- [ ] Watchlist router removed from `api/app.py` (import + `include_router()`)
- [ ] 4 watchlist schema classes removed from `api/schemas.py` (~lines 375-425)
- [ ] 10 watchlist repository methods removed from `data/repository.py` (~lines 561-662+)
- [ ] `uv run pytest tests/ -v` passes
- [ ] `uv run mypy src/ --strict` passes
- [ ] `uv run ruff check .` passes

## Technical Details

### Files to Delete (7)
1. `src/options_arena/models/watchlist.py` — 4 Pydantic models
2. `src/options_arena/cli/watchlist.py` — 5 Typer subcommands
3. `src/options_arena/api/routes/watchlist.py` — 6 HTTP endpoints
4. `tests/unit/models/test_watchlist.py` — model tests
5. `tests/unit/cli/test_watchlist_cli.py` — CLI tests
6. `tests/unit/api/test_watchlist_routes.py` — API route tests
7. `tests/unit/data/test_repository_watchlist.py` — repository tests

### Files to Modify (5)
1. `src/options_arena/models/__init__.py` — remove 4 imports + 4 `__all__` entries
2. `src/options_arena/cli/__init__.py` — remove watchlist module import
3. `src/options_arena/api/app.py` — remove watchlist router import + `include_router()`
4. `src/options_arena/api/schemas.py` — remove `WatchlistCreateRequest`, `WatchlistCreateResponse`, `WatchlistTickerRequest`, `WatchlistTickerAddedResponse`
5. `src/options_arena/data/repository.py` — remove `create_watchlist()`, `delete_watchlist()`, `add_ticker_to_watchlist()`, `remove_ticker_from_watchlist()`, `get_watchlists()`, `get_watchlist_by_id()`, `get_watchlist_by_name()`, `get_tickers_for_watchlist()`, `_row_to_watchlist()`, `_row_to_watchlist_ticker()`

### Approach
1. Delete all 7 files first
2. Clean up imports/re-exports in the 5 modified files
3. Run `ruff check` to catch any remaining references
4. Run `mypy --strict` to verify type integrity
5. Run full test suite to confirm nothing else depended on watchlist

## Test Plan

### Test Files
No new tests. This task deletes 4 test files (~951 lines, ~68 test functions).

### Verification Strategy
- `grep -r "watchlist" src/ --include="*.py"` returns 0 matches after cleanup
- `uv run pytest tests/ -v` passes (fewer tests, all green)
- `uv run mypy src/ --strict` passes (no missing imports)

### Edge Cases
- Verify no conftest.py fixtures reference watchlist models
- Verify no other test files import watchlist builders or helpers
- Verify `api/deps.py` has no watchlist-related dependency providers

## Dependencies
- None — this is the first task

## Effort Estimate
- Size: S
- Hours: 1-2
- Parallel: false (must complete before Task 002)

## Definition of Done
- [ ] All 7 files deleted
- [ ] All 5 files cleaned of watchlist references
- [ ] `grep -r "watchlist" src/ --include="*.py"` returns 0
- [ ] `uv run pytest tests/ -v` green
- [ ] `uv run mypy src/ --strict` green
- [ ] `uv run ruff check .` green
