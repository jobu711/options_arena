---
name: Remove watchlist frontend + E2E tests
status: open
created: 2026-03-07T14:08:06Z
updated: 2026-03-07T14:13:13Z
github: https://github.com/jobu711/options_arena/issues/331
depends_on: [330]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Remove watchlist frontend + E2E tests

## Description

Delete all frontend watchlist files (Vue page, Pinia store, TypeScript types, E2E tests)
and clean up all references in shared components (router, App.vue nav, TickerDrawer,
ScanResultsPage, type re-exports).

## Acceptance Criteria

- [ ] `web/src/pages/WatchlistPage.vue` deleted
- [ ] `web/src/stores/watchlist.ts` deleted
- [ ] `web/src/types/watchlist.ts` deleted
- [ ] `web/e2e/suites/watchlist/watchlist.spec.ts` deleted
- [ ] `web/e2e/fixtures/pages/watchlist.page.ts` deleted
- [ ] `web/e2e/fixtures/builders/watchlist.builders.ts` deleted
- [ ] `/watchlist` route removed from `web/src/router/index.ts`
- [ ] "Watchlists" nav link removed from `web/src/App.vue`
- [ ] Watchlist store import + "Add to Watchlist" UI removed from `web/src/components/TickerDrawer.vue`
- [ ] Watchlist store import + quick-add logic removed from `web/src/pages/ScanResultsPage.vue`
- [ ] Watchlist type re-exports removed from `web/src/types/index.ts`
- [ ] `cd web && npm run build` succeeds
- [ ] `cd web && npx vue-tsc --noEmit` passes (if applicable)

## Technical Details

### Files to Delete (6)
1. `web/src/pages/WatchlistPage.vue` — full watchlist management page
2. `web/src/stores/watchlist.ts` — Pinia store (state, actions, getters)
3. `web/src/types/watchlist.ts` — TypeScript interfaces
4. `web/e2e/suites/watchlist/watchlist.spec.ts` — Playwright E2E suite
5. `web/e2e/fixtures/pages/watchlist.page.ts` — page object
6. `web/e2e/fixtures/builders/watchlist.builders.ts` — test data builders

### Files to Modify (5)
1. `web/src/router/index.ts` — remove `/watchlist` route object (lines ~25-29)
2. `web/src/App.vue` — remove `<RouterLink to="/watchlist">` nav item (line ~31)
3. `web/src/components/TickerDrawer.vue` — remove `useWatchlistStore` import, watchlist dropdown/button UI, and `onAddToWatchlist()` logic
4. `web/src/pages/ScanResultsPage.vue` — remove `useWatchlistStore` import and any quick-add integration
5. `web/src/types/index.ts` — remove watchlist type re-exports

### Approach
1. Delete all 6 files
2. Clean up router, nav, and type re-exports (simple line removals)
3. Clean up TickerDrawer — remove watchlist-specific template section, script imports, and reactive state
4. Clean up ScanResultsPage — remove watchlist store usage
5. Verify `DashboardPage.vue` has no watchlist references (research flagged this as open question)
6. Run `npm run build` to verify no broken imports
7. Check E2E fixture index files for watchlist re-exports

## Test Plan

### Test Files
No new tests. Deletes 3 E2E test/fixture files (~299 lines).

### Verification Strategy
- `grep -r "watchlist\|Watchlist" web/src/ --include="*.ts" --include="*.vue"` returns 0
- `grep -r "watchlist\|Watchlist" web/e2e/ --include="*.ts"` returns 0
- `cd web && npm run build` succeeds with no warnings about missing modules
- Frontend loads without console errors (manual check or E2E)

### Edge Cases
- TickerDrawer must remain fully functional for scan results display after watchlist removal
- E2E fixture index files (`web/e2e/fixtures/index.ts` or similar) may re-export watchlist builders
- Check if any E2E helper utilities reference watchlist page object

## Dependencies
- [ ] Task 001 must be complete (backend watchlist API removed — frontend would error on API calls)

## Effort Estimate
- Size: S
- Hours: 1-2
- Parallel: false (depends on Task 001)

## Definition of Done
- [ ] All 6 files deleted
- [ ] All 5 files cleaned of watchlist references
- [ ] `grep -r "watchlist\|Watchlist" web/src/ web/e2e/ --include="*.ts" --include="*.vue"` returns 0
- [ ] `cd web && npm run build` succeeds
- [ ] No console errors on page load
