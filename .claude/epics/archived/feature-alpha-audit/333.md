---
name: Remove PDF export path
status: open
created: 2026-03-07T14:08:06Z
updated: 2026-03-07T14:13:13Z
github: https://github.com/jobu711/options_arena/issues/333
depends_on: []
parallel: true
conflicts_with: []
test_files: []
---

# Task: Remove PDF export path

## Description

Remove all PDF/weasyprint export code from the debate export pipeline. The `--export`
flag keeps `"md"` as its only valid value. Remove `_render_pdf()`, PDF blocks in CLI
validation and API route, and the PDF test case.

## Acceptance Criteria

- [ ] `_render_pdf()` function removed from `reporting/debate_export.py`
- [ ] `export_debate_to_file()` simplified to handle `fmt="md"` only
- [ ] CLI `--export` validation changed from `("md", "pdf")` to `("md",)`
- [ ] CLI weasyprint error message removed
- [ ] API `export.py` PDF rendering block (lines ~196-213) removed
- [ ] API format validation updated to `("md",)` only
- [ ] Docstrings updated to remove PDF references
- [ ] `test_export_pdf_raises_import_error_without_weasyprint` test removed
- [ ] `uv run pytest tests/unit/reporting/ tests/unit/cli/ tests/unit/api/ -v` passes
- [ ] `grep -r "weasyprint\|_render_pdf" src/` returns 0

## Technical Details

### Files to Modify (4)

1. **`src/options_arena/reporting/debate_export.py`**
   - Remove `_render_pdf()` function (~lines 450-474, ~25 lines)
   - Simplify `export_debate_to_file()` — remove `elif fmt == "pdf"` branch
   - Update function docstring to remove PDF mention

2. **`src/options_arena/cli/commands.py`**
   - Change `--export` help text from `"md or pdf"` to `"md"`
   - Change validation from `export not in ("md", "pdf")` to `export != "md"`
   - Remove weasyprint error handling (~lines 598-599)

3. **`src/options_arena/api/routes/export.py`**
   - Remove entire PDF rendering block (~lines 196-213, ~18 lines)
   - Change format validation from `("md", "pdf")` to `("md",)`
   - Update docstrings that mention PDF/weasyprint
   - Update module docstring (line 1-2)

4. **`tests/unit/reporting/test_debate_export.py`**
   - Remove `test_export_pdf_raises_import_error_without_weasyprint` (~lines 298-308)
   - Update module docstring PDF reference (line ~9)

### Approach
1. Modify `debate_export.py` first (remove `_render_pdf`, simplify caller)
2. Update CLI validation
3. Update API route
4. Remove test
5. Run verification suite

## Test Plan

### Test Files
No new tests. Removes 1 test case (~11 lines).

### Verification Strategy
- `grep -r "weasyprint\|_render_pdf\|pdf" src/ --include="*.py"` returns 0 production refs (only legitimate non-PDF uses of "pdf" like comments should remain)
- All existing markdown export tests still pass
- `uv run pytest tests/unit/reporting/ -v` green
- `uv run pytest tests/unit/cli/ -v` green
- `uv run pytest tests/unit/api/ -v` green

### Edge Cases
- Ensure `--export md` still works identically
- Ensure `--export pdf` now gives clear validation error (not a crash)
- API `?format=pdf` returns 400/422 (validation rejection), not 500
- No other code imports from `_render_pdf` (grep confirms)

## Dependencies
- None — PDF export is independent of watchlist removal

## Effort Estimate
- Size: XS
- Hours: 0.5-1
- Parallel: true (independent of Tasks 001-003)

## Definition of Done
- [ ] All 4 files modified
- [ ] `grep -r "weasyprint\|_render_pdf" src/` returns 0
- [ ] `uv run pytest tests/unit/reporting/ tests/unit/cli/ tests/unit/api/ -v` green
- [ ] `uv run mypy src/ --strict` green
- [ ] `uv run ruff check .` green
