---
name: Update existing test assertions for new weight count
status: open
created: 2026-03-08T14:53:33Z
updated: 2026-03-08T15:04:43Z
github: https://github.com/jobu711/options_arena/issues/383
depends_on: [379]
parallel: true
conflicts_with: []
test_files: []
---

# Task: Update existing test assertions for new weight count

## Description
Update any existing test assertions that hardcode the indicator count (19 → 21) or
specific weight values that changed during redistribution. These tests will fail after
Task 379 modifies the scoring tables.

## Acceptance Criteria
- [ ] `test_indicator_weights_has_19_entries` updated to assert 21 entries
- [ ] Any test asserting specific weight values for redistributed indicators updated
- [ ] Any test asserting `DOMAIN_BOUNDS` count updated (19 → 21)
- [ ] All existing tests still pass after updates
- [ ] No test logic changed beyond assertion values

## Technical Details
- Primary file: `tests/unit/scoring/test_weight_assertion.py`
- Search for: `19` in test assertions related to weights, bounds, indicators
- Search for: exact weight values (0.07, 0.06, 0.05) that changed
- Known assertions to update:
  - `assert len(INDICATOR_WEIGHTS) == 19` → `== 21`
  - Any per-weight value assertions for the 8 redistributed indicators
  - `assert len(DOMAIN_BOUNDS) == 19` → `== 21` (if exists)

## Test Plan

### Test Files
- No new test files — this task modifies existing test files

### Test Cases
```python
# Update existing test:
def test_indicator_weights_has_21_entries(self) -> None:
    """Verify INDICATOR_WEIGHTS has 21 entries (19 original + 2 liquidity)."""
    assert len(INDICATOR_WEIGHTS) == 21
```

### Edge Cases
- Weight values must match exactly (use `pytest.approx()` for float comparisons)

### Mocking Strategy
- No mocking needed — tests import constants directly

## Dependencies
- [ ] Task 379 — scoring tables must be updated first

## Effort Estimate
- Size: S
- Hours: 0.5
- Parallel: true (after Task 379, can run in parallel with other tests)

## Definition of Done
- [ ] Existing test assertions updated
- [ ] All existing tests pass (zero regression)
- [ ] No test logic changed beyond numerical assertions
