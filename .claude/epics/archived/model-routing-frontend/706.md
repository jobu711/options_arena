---
name: E2E tests + rebuild web/dist
status: closed
created: 2026-03-23T13:16:53Z
updated: 2026-03-23T20:08:19Z
github: https://github.com/jobu711/options_arena/issues/706
depends_on: [701, 705]
parallel: false
conflicts_with: []
test_files: [tests/e2e/model-routing.spec.ts]
---

# Task: E2E tests + rebuild web/dist

## Description
Playwright E2E tests for the model routing dashboard panel and cost table. Rebuild `web/dist/` for production serving. Final verification that all tests pass.

## Acceptance Criteria
- [ ] Playwright spec: toggle routing on/off
- [ ] Playwright spec: adjust thresholds and apply
- [ ] Playwright spec: reset to defaults
- [ ] Playwright spec: cost table renders with data
- [ ] Playwright spec: cost table empty state
- [ ] `web/dist/` rebuilt with `npm run build`
- [ ] `options-arena serve` serves the updated dashboard
- [ ] Full test suite passes (backend + E2E)

## Technical Details
- E2E tests use Playwright with API route interception (no real backend needed for UI tests)
- Mock `/api/config` to return routing config
- Mock `/api/analytics/recommendation-costs` to return cost data
- Test both populated and empty states
- Final `npm run build` regenerates `web/dist/` for production

Files affected:
- `tests/e2e/model-routing.spec.ts` (new)
- `web/dist/` (rebuilt)

## Execution

### Read First
- `tests/e2e/` — existing Playwright spec patterns
- `web/playwright.config.ts` — Playwright configuration

### Action
1. Create `tests/e2e/model-routing.spec.ts`:
   - Test: navigate to dashboard → model routing panel visible
   - Test: toggle routing → verify PUT called
   - Test: adjust thresholds → apply → verify PUT body
   - Test: reset to defaults → verify DELETE called
   - Test: cost table with data → verify rows render
   - Test: cost table empty → verify empty state message
   - WHY: verify user-facing functionality end-to-end
2. Run Playwright tests
3. Rebuild `web/dist/`: `cd web && npm run build`
   - WHY: production build must include new components
4. Run full test suite (backend + E2E) for final verification

### Verify
```
cd web && npx playwright test tests/e2e/model-routing.spec.ts
cd web && npm run build
uv run pytest tests/ -x -q --timeout=120
uv run ruff check . --fix && uv run ruff format .
uv run mypy src/ --strict
```

### Done
- All E2E tests pass
- `web/dist/` rebuilt successfully
- Full test suite passes
- Lint, format, and type checks clean

## Test Plan

### Test Files
- `tests/e2e/model-routing.spec.ts`

### Test Cases
```typescript
test.describe('Model Routing Dashboard', () => {
  test('routing panel is visible on dashboard', async ({ page }) => {
    // Navigate to / and verify Panel component renders
  })

  test('toggle routing sends PUT', async ({ page }) => {
    // Click ToggleSwitch, verify PUT /api/config/routing called
  })

  test('apply thresholds sends PUT with values', async ({ page }) => {
    // Adjust sliders, click Apply, verify PUT body
  })

  test('reset sends DELETE', async ({ page }) => {
    // Click Reset to Defaults, verify DELETE called
  })

  test('cost table renders with data', async ({ page }) => {
    // Mock cost endpoint with data, verify table rows
  })

  test('cost table shows empty state', async ({ page }) => {
    // Mock cost endpoint with empty array, verify empty message
  })
})
```

### Edge Cases
- Panel collapsed by default → user can expand
- Apply with invalid thresholds → client-side validation prevents submit

### Mocking Strategy
- Playwright `page.route()` to intercept API calls
- Return mock JSON responses for config and costs

## Dependencies
- [ ] Task 701 (backend API fully working)
- [ ] Task 705 (frontend components integrated)

## Effort Estimate
- Size: M
- Hours: 3-4
- Parallel: false (final task)

## Definition of Done
- [ ] All E2E tests pass
- [ ] `web/dist/` rebuilt
- [ ] Full backend test suite passes
- [ ] Lint, format, type checks all clean
- [ ] Dashboard shows routing panel and cost table
