---
name: Update existing tests for MACD integration
status: open
created: 2026-03-06T20:38:15Z
updated: 2026-03-06T20:43:14Z
github: https://github.com/jobu711/options_arena/issues/315
depends_on: [316, 313]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Update existing tests for MACD integration

## Description
Update existing tests across the codebase that are affected by the MACD changes: orchestrator tests that reference `_derive_macd_signal`, pipeline/scan tests that check indicator counts, composite scoring tests that check weight sums, and any snapshot/fixture data that includes `IndicatorSignals`.

## Acceptance Criteria
- [ ] All existing tests that reference `_derive_macd_signal` updated to use `classify_macd_signal`
- [ ] Tests checking `INDICATOR_REGISTRY` count updated from 14 to 15
- [ ] Tests checking `INDICATOR_WEIGHTS` updated for new weight values
- [ ] Any `IndicatorSignals` fixture/factory updated to include `macd` field
- [ ] Pipeline integration tests updated if they assert on indicator field counts
- [ ] Full test suite passes: `uv run pytest tests/ -v`

## Technical Details
- Search for `_derive_macd_signal` in tests — update all references
- Search for indicator count assertions (14 → 15)
- Search for weight sum tests or specific weight value assertions
- Search for `IndicatorSignals` construction in test fixtures — add `macd` where needed
- Check `conftest.py` files for shared fixtures that build `IndicatorSignals`

## Test Plan

### Test Files
- No new test files — this task updates existing test files

### Test Cases
- Run `uv run pytest tests/ -v` and fix all failures related to MACD changes
- Specifically check:
  - `tests/unit/agents/test_orchestrator.py`
  - `tests/unit/scan/test_indicators.py`
  - `tests/unit/scoring/test_composite.py`
  - `tests/unit/models/test_scan.py`

### Edge Cases
- Existing tests that construct `IndicatorSignals()` with positional args (unlikely but check)
- Tests that assert exact field counts on `IndicatorSignals`

### Mocking Strategy
- Update existing mocks that construct `IndicatorSignals` to include `macd` field

## Dependencies
- [ ] Task 316 (registry changes)
- [ ] Task 313 (orchestrator changes)

## Effort Estimate
- Size: M
- Hours: 1.5
- Parallel: false

## Definition of Done
- [ ] `uv run pytest tests/ -v` passes with 0 failures
- [ ] No references to `_derive_macd_signal` remain in test files
- [ ] All indicator count assertions updated
