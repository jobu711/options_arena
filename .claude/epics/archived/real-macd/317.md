---
name: Final validation and CLAUDE.md updates
status: open
created: 2026-03-06T20:38:15Z
updated: 2026-03-06T20:43:14Z
github: https://github.com/jobu711/options_arena/issues/317
depends_on: [315]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Final validation and CLAUDE.md updates

## Description
Run the full verification suite (lint, typecheck, tests), update module CLAUDE.md files to reflect the new MACD indicator, and verify that MACD can disagree with overall direction (key acceptance criterion from the PRD).

## Acceptance Criteria
- [ ] `uv run ruff check . --fix && uv run ruff format .` passes
- [ ] `uv run mypy src/ --strict` passes
- [ ] `uv run pytest tests/ -v` passes (all tests)
- [ ] `indicators/CLAUDE.md` updated: function list includes `macd()`, indicator count = 15
- [ ] `scoring/CLAUDE.md` updated if it references indicator counts or weight tables
- [ ] `_derive_macd_signal` does not appear anywhere in codebase (grep verification)
- [ ] Confirmed: MACD signal can disagree with overall scan direction

## Technical Details

### Verification commands
```bash
uv run ruff check . --fix && uv run ruff format .
uv run pytest tests/ -v
uv run mypy src/ --strict
```

### CLAUDE.md updates
- `src/options_arena/indicators/CLAUDE.md`: Add `macd()` to function list, update count from 14→15
- `src/options_arena/scoring/CLAUDE.md`: Update weight table if present
- `src/options_arena/scan/CLAUDE.md`: Update registry count if mentioned

### Grep verification
```bash
grep -r "_derive_macd_signal" src/ tests/
```
Should return no results.

## Test Plan

### Test Files
- No new test files — this is a validation task

### Test Cases
- Run full suite and verify 0 failures
- Run grep to confirm fake derivation is gone
- Manually verify CLAUDE.md accuracy

### Edge Cases
- N/A (validation task)

### Mocking Strategy
- N/A (validation task)

## Dependencies
- [ ] All previous tasks (312-315) must be complete

## Effort Estimate
- Size: S
- Hours: 0.5
- Parallel: false

## Definition of Done
- [ ] All three verification commands pass
- [ ] CLAUDE.md files updated
- [ ] No references to `_derive_macd_signal` in codebase
- [ ] Epic ready for merge
