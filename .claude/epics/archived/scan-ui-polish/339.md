---
name: Update ScanPage.vue layout + FilterSummaryChips integration
status: open
created: 2026-03-07T16:59:19Z
updated: 2026-03-07T17:07:43Z
github: https://github.com/jobu711/options_arena/issues/339
depends_on: [337, 338]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Update ScanPage.vue layout + FilterSummaryChips integration

## Description
Update `ScanPage.vue` to integrate the new components: add page header with subtitle, import FilterSummaryChips between filters and launch button, add template ref on PreScanFilters for chip-driven filter clearing, wrap the launch button in a styled card section with centered layout.

**Critical constraint**: All scan logic (runScan, handleCancel, viewResults, stores, WebSocket) must remain completely unchanged. Page must stay under 350 lines.

## Acceptance Criteria
- [ ] Page header: h1 title + subtitle description text
- [ ] FilterSummaryChips rendered between PreScanFilters and launch section
- [ ] FilterSummaryChips receives `currentFilters`, sector/industry counts
- [ ] Template ref on PreScanFilters component
- [ ] `clear-filter` event calls `preFiltersRef.clearFilter(key)`
- [ ] `clear-all` event calls `preFiltersRef.clearAll()`
- [ ] Launch button wrapped in styled card section, centered
- [ ] Button uses `size="large"` (or custom CSS equivalent)
- [ ] ProgressTracker replaces launch area when scanning
- [ ] Past Scans section has header with scan count
- [ ] Page stays under 350 lines
- [ ] All existing `data-testid` attributes preserved

## Technical Details

### File Modified
- `web/src/pages/ScanPage.vue` (246 → ~280 lines)

### Template Changes
```html
<!-- Page header -->
<div class="page-header">
  <h1 data-testid="scan-title">Scan</h1>
  <p class="page-subtitle">Configure and launch options universe scans</p>
</div>

<!-- Filters -->
<PreScanFilters ref="preFiltersRef" @update:filters="onFiltersUpdate" ... />

<!-- Filter summary + Launch -->
<div class="launch-section" v-if="!scanStore.isScanning">
  <FilterSummaryChips
    :filters="currentFilters"
    :sector-count="currentFilters.sectors?.length ?? 0"
    :industry-group-count="currentFilters.industryGroups?.length ?? 0"
    :disabled="opStore.inProgress"
    @clear-filter="handleClearFilter"
    @clear-all="handleClearAll"
  />
  <Button label="Run Scan" icon="pi pi-play" severity="success"
    size="large" :disabled="..." :loading="..." @click="runScan()"
    data-testid="start-scan-btn" />
</div>

<!-- Progress (replaces launch when scanning) -->
<ProgressTracker v-else ... />

<!-- Past Scans -->
<div class="past-scans-section">
  <h2>Past Scans <Badge :value="scanStore.scans.length" ... /></h2>
  <DataTable ... />
</div>
```

### New Script Logic
```typescript
import FilterSummaryChips from '@/components/scan/FilterSummaryChips.vue'

const preFiltersRef = ref<InstanceType<typeof PreScanFilters> | null>(null)

function handleClearFilter(key: string): void {
  preFiltersRef.value?.clearFilter(key)
}

function handleClearAll(): void {
  preFiltersRef.value?.clearAll()
}
```

### CSS Additions
```css
.page-header { margin-bottom: 1.5rem; }
.page-subtitle {
  color: var(--p-surface-400, #888);
  font-size: 0.9rem;
  margin: 0.25rem 0 0;
}
.launch-section {
  background: var(--p-surface-800, #1a1a1a);
  border: 1px solid var(--p-surface-700, #333);
  border-radius: 0.5rem;
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
  margin: 1rem 0;
}
```

## Test Plan

### Test Files
- No new test files — E2E coverage via existing scan-launch.spec.ts + updates in task 005

### Key Behaviors
- Filter chips appear when filters are non-default
- Clicking chip X clears corresponding filter
- "Clear All" resets all filters
- Launch button centered in styled card
- ProgressTracker replaces launch section during scan
- Past scans table renders with count badge

## Dependencies
- [ ] Task #337 (FilterSummaryChips.vue) must be complete
- [ ] Task #338 (PreScanFilters restructure + defineExpose) must be complete

## Effort Estimate
- Size: S
- Hours: 2
- Parallel: false

## Definition of Done
- [ ] Page header with subtitle renders
- [ ] FilterSummaryChips integrated with clear handlers
- [ ] Launch section styled as centered card
- [ ] Template ref + clearFilter/clearAll working end-to-end
- [ ] All existing scan logic unchanged
- [ ] Page under 350 lines
- [ ] No TypeScript errors
