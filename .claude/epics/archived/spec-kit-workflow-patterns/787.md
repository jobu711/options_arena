---
name: Enhance spec-analyzer with 11-category ambiguity taxonomy and scoring
status: closed
created: 2026-03-24T13:05:43Z
updated: 2026-03-24T13:21:28Z
github: https://github.com/jobu711/options_arena/issues/787
depends_on: []
parallel: true
conflicts_with: []
test_files: []
---

# Task: Enhance spec-analyzer with 11-category ambiguity taxonomy and scoring

## Description

Replace the spec-analyzer agent's Phase 3 10-point checklist with an 11-category taxonomy
(10 from spec-kit + 1 OA-specific) and add Impact x Uncertainty scoring. Update Phase 4
question format with category, recommended option, and alternatives. Cap prioritized
questions at 5 (spec-kit convention). Update output format with taxonomy coverage table.

## Acceptance Criteria

- [ ] Phase 3 uses 11-category taxonomy table instead of 10-point checklist
- [ ] Each gap has explicit Impact (1-3) and Uncertainty (1-3) scores
- [ ] Priority = Impact x Uncertainty, sorted descending
- [ ] Maximum 5 prioritized questions surfaced (remaining gaps in coverage table)
- [ ] Question format includes: Category, Context, Recommended option, Alternatives, Impact if unanswered
- [ ] Output includes taxonomy coverage table showing categories scanned and gap counts
- [ ] Phases 1, 2, 4 are preserved unchanged (backward compatible)
- [ ] Existing 10-point checklist items are subsumed by taxonomy categories (mapping documented)

## Technical Details

The spec-analyzer agent (`.claude/agents/spec-analyzer.md`, 142 lines) has a 4-phase protocol.
Only Phase 3 (lines 56-70) and the output format (lines 88-132) change. Phase 4's question
format (lines 71-86) gets enhanced but the classification system (Blocking/Non-blocking/Deferred)
is preserved.

### 11-Category Taxonomy

| # | Category | Subsumes Old Checklist Item(s) |
|---|----------|-------------------------------|
| 1 | Functional Scope & Behavior | #1 Happy path, #4 State transitions |
| 2 | Domain & Data Model | (new) |
| 3 | Interaction & UX Flow | (new) |
| 4 | Non-Functional Quality | #5 Concurrency, #7 Observability |
| 5 | Integration & Dependencies | #9 Migration |
| 6 | Edge Cases & Failure Handling | #2 Error paths, #3 Boundaries, #6 Rollback |
| 7 | Constraints & Tradeoffs | (new) |
| 8 | Terminology & Consistency | (new) |
| 9 | Completion Signals | #8 Testability |
| 10 | Placeholders & TODOs | #10 Documentation |
| 11 | Priority & Sequencing (OA) | (new — OA-specific addition) |

### Scoring Formula

- **Impact** (1-3): 1 = cosmetic/deferred, 2 = affects implementation decisions, 3 = blocks correct implementation
- **Uncertainty** (1-3): 1 = reasonable default exists, 2 = multiple valid options, 3 = no basis for choosing
- Priority = Impact x Uncertainty (range 1-9). Top 5 surfaced as questions.

### Enhanced Question Format

```
[Blocking/Non-blocking/Deferred] Q{N} (Impact:{I} x Uncertainty:{U} = {P}): {question}
  Category: {taxonomy category name}
  Context: {why this matters in OA's architecture}
  Recommended: {best option} — {reasoning}
  Alternatives:
    A) {option} — {tradeoff}
    B) {option} — {tradeoff}
  Impact if unanswered: {what breaks or degrades}
```

## Execution

### Read First
- `.claude/agents/spec-analyzer.md` — current 4-phase protocol, exact Phase 3 content
- `.claude/prds/spec-kit-workflow-patterns.md` lines 145-197 — taxonomy spec from PRD
- `.claude/epics/spec-kit-workflow-patterns/research.md` — architecture decisions

### Action
1. In `.claude/agents/spec-analyzer.md`, replace Phase 3's 10-point checklist (lines 56-70) with the 11-category taxonomy table
   - WHY: structured scan replaces unstructured list; covers more categories (terminology, non-functional, completion signals)
2. Add scoring instructions after the taxonomy table: Impact (1-3) x Uncertainty (1-3), top 5 questions
   - WHY: prioritization ensures most dangerous ambiguities surface first, not most obvious
3. Update Phase 4's question format (lines 71-86) to include Category, Context, Recommended, Alternatives, Impact if unanswered
   - WHY: recommended options let reviewer accept-or-override rather than research from scratch
4. Add backward compatibility note documenting how old checklist items map to taxonomy categories
   - WHY: prevents confusion for anyone familiar with old format
5. Update output format section (lines 88-132) to include taxonomy coverage table
   - WHY: shows which categories were scanned and gap density per category

### Verify
```
# Verify file is valid markdown with no broken formatting
grep -c "Category" .claude/agents/spec-analyzer.md  # Should find taxonomy references
grep -c "Impact" .claude/agents/spec-analyzer.md     # Should find scoring references
grep "Phase 1" .claude/agents/spec-analyzer.md       # Phase 1 still present (unchanged)
grep "Phase 2" .claude/agents/spec-analyzer.md       # Phase 2 still present (unchanged)
grep "Phase 4" .claude/agents/spec-analyzer.md       # Phase 4 still present (enhanced)
```

### Done
- 11-category taxonomy table present in Phase 3 section
- Scoring formula (Impact x Uncertainty) documented with range 1-9
- Question format includes all 6 fields (classification, score, category, context, recommended, impact)
- Output format includes taxonomy coverage table template
- Phases 1, 2 are byte-identical to before (no changes)
- Old 10-point checklist no longer present (fully subsumed)

## Test Plan

### Test Files
- No automated tests (markdown-only change)

### Manual Verification
1. **Taxonomy completeness**: Count categories in Phase 3 — should be exactly 11
2. **Backward mapping**: Verify all 10 old checklist items appear in taxonomy mapping
3. **Question format**: Verify template has all 6 required fields
4. **Output format**: Verify taxonomy coverage table template is present
5. **Phase preservation**: Diff Phases 1, 2 against original — should be unchanged

### Edge Cases
- Empty category (zero gaps found): taxonomy table should show 0, not omit the row
- All gaps in one category: scoring still ranks by Impact x Uncertainty, not category spread

### Mocking Strategy
- N/A (no code, no mocks)

## Dependencies
- None — this task is fully independent

## Effort Estimate
- Size: S
- Hours: 2-3
- Parallel: true

## Definition of Done
- [ ] Phase 3 uses 11-category taxonomy (not old checklist)
- [ ] Scoring formula documented
- [ ] Question format has all 6 fields
- [ ] Output format includes taxonomy coverage table
- [ ] Phases 1, 2, 4 structure preserved
