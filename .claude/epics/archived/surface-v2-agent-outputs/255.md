---
name: Frontend — v2 types, 4 agent cards, protocol-aware page
status: open
created: 2026-03-04T09:52:05Z
updated: 2026-03-04T09:52:05Z
github: https://github.com/jobu711/options_arena/issues/255
depends_on: [254]
parallel: true
conflicts_with: []
test_files: []
---

# Task: Frontend — v2 types, 4 agent cards, protocol-aware page

## Description
Add 4 TypeScript interfaces for v2 agent outputs. Create 4 specialized Vue card components (FlowAgentCard, FundamentalAgentCard, RiskAgentCard, ContrarianAgentCard). Modify DebateResultPage.vue for protocol-aware rendering. Extend WebSocket types and debate store for v2 agents.

## Acceptance Criteria
- [ ] 4 TypeScript interfaces in `web/src/types/debate.ts`: `FlowThesis`, `FundamentalThesis`, `RiskAssessmentThesis`, `ContrarianThesis`
- [ ] `DebateResult` interface has 5 new optional fields: `flow_response?`, `fundamental_response?`, `risk_v2_response?`, `contrarian_response?`, `debate_protocol?`
- [ ] `FlowAgentCard.vue` — displays direction, confidence, GEX interpretation, smart money signal, OI analysis, volume confirmation, key factors. Color: #f97316 (orange)
- [ ] `FundamentalAgentCard.vue` — displays direction, confidence, catalyst impact, earnings assessment, IV crush risk, key factors. Color: #14b8a6 (teal)
- [ ] `RiskAgentCard.vue` — displays risk level, confidence, PoP estimate, max loss, key risks, mitigants, position size. Color: #3b82f6 (blue)
- [ ] `ContrarianAgentCard.vue` — displays dissent direction, confidence, primary challenge, overlooked risks, consensus weakness, alternative scenario. Color: #eab308 (yellow)
- [ ] `DebateResultPage.vue` detects `debate_protocol === 'v2'` and renders 6-card grid (trend, flow, fundamental, volatility, risk, contrarian)
- [ ] V2 page shows "Trend Analysis" label instead of "Bull Agent" for trend card
- [ ] V2 page omits synthetic bear card
- [ ] V1 page layout completely unchanged
- [ ] `DebateAgentEvent.name` in `ws.ts` extended with `'flow' | 'fundamental' | 'risk_v2' | 'contrarian'`
- [ ] Debate store initializes v2 agent names when appropriate
- [ ] All cards use existing `ConfidenceBadge` and `DirectionBadge` components
- [ ] `vue-tsc --noEmit` passes

## Technical Details
- Files to create:
  - `web/src/components/FlowAgentCard.vue`
  - `web/src/components/FundamentalAgentCard.vue`
  - `web/src/components/RiskAgentCard.vue`
  - `web/src/components/ContrarianAgentCard.vue`
- Files to modify:
  - `web/src/types/debate.ts` — 4 interfaces + DebateResult extension
  - `web/src/types/ws.ts` — extend agent name union
  - `web/src/pages/DebateResultPage.vue` — protocol detection + v2 grid
  - `web/src/stores/debate.ts` — v2 agent progress initialization
- Card pattern: `<script setup lang="ts">`, `defineProps<Props>()`, scoped CSS, `border-left: 4px solid var(--agent-color)`, `var(--p-surface-800)` background
- Protocol detection: `const isV2 = computed(() => debate.value?.debate_protocol === 'v2')`
- Use `v-if` guards on all v2 cards

## Test Plan

### Test Cases
- Manual verification: v2 debate renders 6 cards with correct colors and field layouts
- Manual verification: v1 debate renders identically to before
- TypeScript compilation: `vue-tsc --noEmit` passes
- E2E (optional future): extend debate.spec.ts with v2 protocol mock

### Edge Cases
- V2 debate with some agent outputs null (cards not rendered for missing agents)
- Optional fields (short_interest_analysis, dividend_impact, etc.) null — show "--" fallback
- Long text in agent fields doesn't break card layout

### Mocking Strategy
- No test mocking — this is UI work verified by TypeScript compilation and manual/E2E testing

## Dependencies
- [ ] Task 005: API returns v2 fields (frontend needs the API shape)

## Effort Estimate
- Size: L
- Hours: 5-6
- Parallel: true (after Task 005 API shape is known)

## Definition of Done
- [ ] 4 new Vue card components created
- [ ] DebateResultPage protocol-aware
- [ ] TypeScript types extended
- [ ] `vue-tsc --noEmit` passes
- [ ] Manual testing: v2 and v1 both render correctly
