---
name: Remove obsolete context files
status: open
created: 2026-03-23T17:19:06Z
updated: 2026-03-23T17:28:44Z
github: https://github.com/jobu711/options_arena/issues/748
depends_on: [751, 753, 755]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Remove Obsolete Context Files

## Description
Delete the 5 context files that have been consolidated into Tier 0 (CLAUDE.md) and
Tier 1 (architecture.md, product.md, algorithms.md). This is a cleanup task — only
proceed after replacement files are verified.

## Acceptance Criteria
- [ ] `.claude/context/system-patterns.md` deleted
- [ ] `.claude/context/system-patterns-reference.md` deleted
- [ ] `.claude/context/product-context.md` deleted
- [ ] `.claude/context/tech-context.md` deleted
- [ ] `.claude/context/module-summaries.md` deleted
- [ ] No remaining `@`-references to deleted files in any .md file
- [ ] Replacement files exist and are verified (architecture.md, product.md, algorithms.md)

## Technical Details
These files are being deleted because their content has been absorbed:
- `system-patterns.md` (257 lines) → architecture.md + CLAUDE.md patterns summary
- `system-patterns-reference.md` (74 lines) → algorithms.md
- `product-context.md` (139 lines) → product.md
- `tech-context.md` (65 lines) → CLAUDE.md identity section
- `module-summaries.md` (125 lines) → architecture.md

Pre-deletion verification:
- Confirm architecture.md exists and has content
- Confirm product.md exists and has content
- Confirm algorithms.md exists and has content
- Confirm CLAUDE.md has no @-references to any of these files
- Grep all .md files for references to deleted filenames

## Execution

### Read First
Files that MUST be read before starting this task:
- `.claude/context/architecture.md` — verify replacement exists
- `.claude/context/product.md` — verify replacement exists
- `.claude/context/algorithms.md` — verify replacement exists
- `CLAUDE.md` — verify no @-references to deleted files

### Action
1. Verify all 3 replacement Tier 1 files exist and have content
   - WHY: never delete source files before replacements are confirmed
2. Verify CLAUDE.md has zero @-references: `grep "^@" CLAUDE.md`
   - WHY: deleted files must not be referenced
3. Search for any remaining references to deleted filenames across all .md files
   - WHY: catch stale references in guides, agents, prompts, etc.
4. Delete the 5 obsolete files using git rm
   - WHY: git rm stages the deletion for commit tracking
5. Fix any stale references found in step 3
   - WHY: no broken references after cleanup

### Verify
```
ls .claude/context/system-patterns.md 2>/dev/null && echo "FAIL: still exists" || echo "OK: deleted"
ls .claude/context/system-patterns-reference.md 2>/dev/null && echo "FAIL" || echo "OK"
ls .claude/context/product-context.md 2>/dev/null && echo "FAIL" || echo "OK"
ls .claude/context/tech-context.md 2>/dev/null && echo "FAIL" || echo "OK"
ls .claude/context/module-summaries.md 2>/dev/null && echo "FAIL" || echo "OK"
ls .claude/context/architecture.md && echo "OK: replacement exists"
ls .claude/context/product.md && echo "OK: replacement exists"
ls .claude/context/algorithms.md && echo "OK: replacement exists"
```

### Done
- All 5 obsolete files deleted
- All 3 replacement files exist
- No stale references anywhere in the project

## Test Plan

### Validation
No pytest tests. Validation:
- 5 files confirmed deleted
- 3 replacement files confirmed present
- `grep -r "system-patterns\|product-context\|tech-context\|module-summaries" .claude/ --include="*.md"` finds no stale references (excluding archive/epics)

## Dependencies
- [ ] Task 751: architecture.md created (replaces system-patterns + module-summaries)
- [ ] Task 753: product.md + algorithms.md created (replaces product-context + reference)
- [ ] Task 755: progress.md trimmed (no longer needs @-reference)

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: false (must wait for all replacement files)

## Definition of Done
- [ ] 5 files deleted
- [ ] 3 replacements verified
- [ ] No stale references
