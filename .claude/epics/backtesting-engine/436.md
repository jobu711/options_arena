---
name: E2E tests
status: open
created: 2026-03-10T00:04:29Z
updated: 2026-03-10T00:25:13Z
github: https://github.com/jobu711/options_arena/issues/436
depends_on: [435]
parallel: false
conflicts_with: []
test_files: [tests/e2e/analytics-dashboard.spec.ts]
---

# Task: E2E tests

## Description
Write Playwright E2E tests for the analytics dashboard. Verify all 5 tabs render correctly with seeded data, tab switching works, filter controls update charts, and empty state is handled gracefully. Follow existing E2E test patterns (73 existing tests across 14 spec files, 4 parallel workers, isolated DBs).

## Acceptance Criteria
- [ ] New E2E spec file `tests/e2e/analytics-dashboard.spec.ts` created
- [ ] Tests verify `/analytics` route loads successfully
- [ ] All 5 tabs render without errors
- [ ] Charts display with seeded data
- [ ] Tab switching preserves state
- [ ] Empty state handled (no data scenario)
- [ ] Filter controls (direction, holding period) update displayed data
- [ ] Tests pass in CI (headless Chromium)
- [ ] Tests use isolated database (existing pattern)

## Technical Details

### E2E Test Structure
Follow existing Playwright patterns from the 14 existing spec files. Tests use:
- Isolated database per test (existing fixture)
- FastAPI test server (existing setup)
- `page.goto('/analytics')` to navigate
- PrimeVue component selectors for tabs, tables, charts

### Seeded Data
Need helper to seed `recommended_contracts` + `contract_outcomes` with realistic test data for chart rendering. Can extend existing seed helpers or create new ones.

### Key Selectors
- Tab panels: `.p-tabview-panel`, `.p-tabview-nav-link`
- Charts: `canvas` elements (Chart.js renders to canvas)
- Tables: `.p-datatable`, `.p-datatable-tbody tr`
- Summary cards: custom component selectors

### Files Created
- `tests/e2e/analytics-dashboard.spec.ts` — NEW FILE

## Test Plan

### Test Files
- `tests/e2e/analytics-dashboard.spec.ts`

### Test Cases
```typescript
test.describe('Analytics Dashboard', () => {
  test('page loads at /analytics', async ({ page }) => {
    await page.goto('/analytics');
    await expect(page.locator('h1')).toContainText('Analytics');
  });

  test('overview tab shows equity curve chart', async ({ page }) => {
    // Seed data, navigate, verify canvas renders
  });

  test('overview tab shows summary cards', async ({ page }) => {
    // Verify trade count, win rate, avg return cards
  });

  test('agents tab shows accuracy heatmap', async ({ page }) => {
    // Click Agents tab, verify heatmap renders
  });

  test('segments tab shows sector bars', async ({ page }) => {
    // Click Segments tab, verify bar chart renders
  });

  test('greeks tab shows stacked bar', async ({ page }) => {
    // Click Greeks tab, verify stacked bar renders
  });

  test('holding tab shows comparison table', async ({ page }) => {
    // Click Holding tab, verify table with data
  });

  test('tab switching preserves data', async ({ page }) => {
    // Switch away and back, verify data still displayed
  });

  test('empty state when no data', async ({ page }) => {
    // No seeded data, verify "No data" messages
  });

  test('direction filter updates equity curve', async ({ page }) => {
    // Select "bullish" filter, verify chart updates
  });

  test('holding days filter updates segments', async ({ page }) => {
    // Change holding days, verify segment charts update
  });
});
```

### Edge Cases
- Empty database: all tabs show appropriate "no data" state
- Slow API response: loading indicators displayed
- Large dataset: charts render without timeout
- Tab rapid switching: no race conditions or stale data

### Mocking Strategy
- No mocking — E2E tests use real FastAPI server + isolated SQLite DB
- Seed data via direct DB insertion in test setup
- Follow existing E2E fixture patterns for database isolation

## Dependencies
- [ ] Task 6: Vue dashboard must be built and functional

## Effort Estimate
- Size: S
- Hours: 8
- Parallel: false (depends on Task 6 dashboard)

## Definition of Done
- [ ] E2E spec file created with 10+ tests
- [ ] All tests pass locally (headless)
- [ ] All tests pass in CI
- [ ] Coverage: all 5 tabs + empty state + filters
- [ ] No flaky tests (deterministic with seeded data)
- [ ] Final `ruff check` + `ruff format` + `mypy --strict` + `pytest` all green
