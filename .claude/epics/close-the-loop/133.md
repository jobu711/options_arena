---
name: Foundation — Models, Migration, Schemas
status: open
created: 2026-02-27T21:31:08Z
updated: 2026-02-27T21:36:30Z
github: https://github.com/jobu711/options_arena/issues/133
depends_on: []
parallel: false
conflicts_with: []
zone: interface
---

# Task: Foundation — Models, Migration, Schemas

## Description

Create the shared foundation that all parallel streams depend on: database migration,
Pydantic models for watchlists and scan diffs, and API schema stubs. This task MUST
complete and merge to master before Tasks 2-4 can begin.

**Branch**: `feat/v2.1-foundation`

## Acceptance Criteria

- [ ] Migration `006_v2_1_features.sql` creates `recommended_contracts` table and adds
      `description` column to existing `watchlists` table
- [ ] `models/watchlist.py` defines `Watchlist`, `WatchlistTicker`, `WatchlistDetail`
      with `frozen=True`, UTC validators on datetime fields
- [ ] `models/diff.py` defines `ScoreChange`, `ScanDiffResult`, `DebateTrendPoint`
      with `frozen=True`, UTC validator on `created_at`, confidence validator on
      `DebateTrendPoint.confidence`
- [ ] `models/__init__.py` re-exports all 6 new symbols
- [ ] `api/schemas.py` has v2.1 request/response classes: `WatchlistCreateRequest`,
      `WatchlistUpdateRequest`, `WatchlistAddTickerRequest`, `ScanDiffRequest`,
      `DebateTrendResponse`
- [ ] ~30 tests pass: model construction, frozen enforcement, UTC validation,
      confidence bounds, JSON roundtrip, migration runs without error, re-exports
- [ ] `ruff check`, `ruff format`, `mypy --strict` all pass

## Technical Details

### Files to Create
- `data/migrations/006_v2_1_features.sql`
- `src/options_arena/models/watchlist.py`
- `src/options_arena/models/diff.py`
- `tests/unit/models/test_watchlist.py`
- `tests/unit/models/test_diff.py`

### Files to Modify
- `src/options_arena/models/__init__.py` — add re-exports
- `src/options_arena/api/schemas.py` — append v2.1 schema classes

### Key Patterns
- All models: `ConfigDict(frozen=True)`
- All datetime fields: `@field_validator` checking `v.tzinfo is None or v.utcoffset() != timedelta(0)`
- Confidence field on `DebateTrendPoint`: `@field_validator` checking `0.0 <= v <= 1.0`
- `ScoreChange.direction_changed`: plain `bool` field (not computed)
- Import `SignalDirection` from existing enums for direction fields

### Module CLAUDE.md to Read First
- `models/CLAUDE.md`
- `api/CLAUDE.md`
- `data/CLAUDE.md`

## Dependencies

- [ ] None — this is the foundation task

## Effort Estimate

- Size: S
- Hours: 2-3
- Parallel: false (blocking — must complete before Tasks 2-4)

## Definition of Done

- [ ] Code implemented following all project conventions
- [ ] ~30 tests written and passing
- [ ] `uv run ruff check . --fix && uv run ruff format .` — 0 errors
- [ ] `uv run mypy src/ --strict` — 0 errors
- [ ] `uv run pytest tests/ -v` — all existing + new tests pass
- [ ] Committed with `feat: add v2.1 foundation models, migration, and API schemas`
- [ ] PR created and merged to master
