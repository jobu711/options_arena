---
name: Sector & Market Cap Filters
status: open
created: 2026-02-27T21:31:08Z
updated: 2026-02-27T21:36:30Z
github: https://github.com/jobu711/options_arena/issues/136
depends_on: [133, 137]
parallel: false
conflicts_with: [137]
zone: interface
---

# Task: Sector & Market Cap Filters

## Description

Implement the stubbed `--sectors` CLI flag and add sector filtering to the scan pipeline,
API, and Web UI. Runs AFTER Task 3 (Watchlists) merges because both modify `pipeline.py`
Phase 1.

**Branch**: `feat/v2.1-filters`

## Acceptance Criteria

### Config
- [ ] `ScanConfig` gains `sectors: list[str] | None = None` field

### Pipeline
- [ ] Phase 1 `_phase_universe()`: after preset filter and after watchlist filter
      (from Task 3), apply sector filter using SP500 sector data
- [ ] Sector filter: `tickers = [t for t in tickers if sp500_sectors.get(t, "") in allowed_sectors]`

### API
- [ ] `ScanRequest` gains `sectors: list[str] | None = None`
- [ ] Scan handler passes sectors through to pipeline config
- [ ] `GET /api/universe/sectors` → list of unique GICS sector names from SP500

### CLI
- [ ] Remove the "not yet implemented" warning from `--sectors` flag
- [ ] Parse comma-separated string into list
- [ ] Pass to pipeline via `ScanConfig` override
- [ ] Verify: `options-arena scan --sectors "Technology,Healthcare"` scans only those sectors

### Web UI
- [ ] `ScanPage.vue`: PrimeVue MultiSelect for sectors (populated from `/api/universe/sectors`)
- [ ] Selected sectors shown as chips
- [ ] Sectors submitted as part of scan start request

### Tests
- [ ] ~20 tests: ScanConfig validation, pipeline sector filtering, API scan with
      sectors, universe/sectors endpoint, CLI parsing, Vue MultiSelect rendering

## Technical Details

### Files to Modify
- `src/options_arena/models/config.py` — add `sectors` field to `ScanConfig`
- `src/options_arena/scan/pipeline.py` — Phase 1 (~10 lines after watchlist filter)
- `src/options_arena/api/schemas.py` — extend `ScanRequest` with `sectors`
- `src/options_arena/api/routes/scan.py` — pass sectors to pipeline config
- `src/options_arena/api/routes/universe.py` — add `/sectors` endpoint
- `src/options_arena/cli/commands.py` — implement `--sectors` (remove warning)
- `web/src/pages/ScanPage.vue` — add sector MultiSelect

### Files to Create
- `tests/unit/scan/test_pipeline_filters.py`
- `tests/unit/api/test_scan_filters.py`

### Key Implementation Notes
- SP500 sector data is already fetched by `UniverseService` (Wikipedia scrape) and available
  as `dict[str, str]` mapping ticker → GICS sector. Pipeline already has access to this.
- Market cap filtering deferred — requires TickerInfo fetch which is expensive at Phase 1.
  PRD recommends `--cap-tier` at Phase 3 (post-liquidity). Not in scope for v2.1.
- Sector endpoint: `UniverseService` already has sector data cached. Just expose via GET.
- CLI: current stubbed flag is at line 81 of `commands.py`, warning at line 94-96.

### Module CLAUDE.md to Read First
- `scan/CLAUDE.md`, `api/CLAUDE.md`, `cli/CLAUDE.md`, `web/CLAUDE.md`, `models/CLAUDE.md`

## Dependencies

- [ ] Task 001 (Foundation) must be merged
- [ ] Task 003 (Watchlists) must be merged — both modify `pipeline.py` Phase 1

## Effort Estimate

- Size: S
- Hours: 3-4
- Parallel: false (must run after Task 3)

## Definition of Done

- [ ] Code implemented following all project conventions
- [ ] ~20 tests written and passing
- [ ] `--sectors` flag fully functional (no warning)
- [ ] `uv run ruff check . --fix && uv run ruff format .` — 0 errors
- [ ] `uv run mypy src/ --strict` — 0 errors
- [ ] `uv run pytest tests/ -v` — all existing + new tests pass
- [ ] Committed with `feat: implement sector filtering for scan pipeline, API, and Web UI`
- [ ] PR created targeting master
