---
name: Watchlists
status: open
created: 2026-02-27T21:31:08Z
updated: 2026-02-27T21:36:30Z
github: https://github.com/jobu711/options_arena/issues/137
depends_on: [133]
parallel: true
conflicts_with: [136]
zone: interface
---

# Task: Watchlists

## Description

Implement full watchlist CRUD using the existing (but unused) `watchlists` and
`watchlist_tickers` DB tables. Add repository methods, REST API router, CLI subcommands,
pipeline watchlist-mode scanning, and a Vue watchlist management page.

**Branch**: `feat/v2.1-watchlists`

## Acceptance Criteria

### Repository (9 methods)
- [ ] `create_watchlist(name, description) -> int`
- [ ] `get_watchlist_by_id(id) -> Watchlist | None`
- [ ] `get_watchlist_by_name(name) -> Watchlist | None`
- [ ] `get_all_watchlists() -> list[Watchlist]`
- [ ] `add_ticker_to_watchlist(watchlist_id, ticker) -> None`
- [ ] `remove_ticker_from_watchlist(watchlist_id, ticker) -> None`
- [ ] `get_tickers_for_watchlist(watchlist_id) -> list[WatchlistTicker]`
- [ ] `update_watchlist(watchlist_id, name, description) -> None`
- [ ] `delete_watchlist(watchlist_id) -> None`

### API (7 endpoints in new `routes/watchlist.py`)
- [ ] `POST /api/watchlists` → 201 + `Watchlist`
- [ ] `GET /api/watchlists` → `list[Watchlist]`
- [ ] `GET /api/watchlists/{id}` → `WatchlistDetail`
- [ ] `PUT /api/watchlists/{id}` → `Watchlist`
- [ ] `DELETE /api/watchlists/{id}` → 204
- [ ] `POST /api/watchlists/{id}/tickers` → 201 + `WatchlistTicker`
- [ ] `DELETE /api/watchlists/{id}/tickers/{ticker}` → 204
- [ ] `ScanRequest` gains `watchlist_id: int | None = None`

### Pipeline
- [ ] `ScanPipeline.run()` gains `watchlist_tickers: list[str] | None = None`
- [ ] When set, Phase 1 skips universe/preset fetch — uses watchlist tickers as universe
- [ ] Scan API resolves `watchlist_id` → ticker list from DB before calling pipeline

### CLI
- [ ] `options-arena watchlist create "Name" --description "..."`
- [ ] `options-arena watchlist list`
- [ ] `options-arena watchlist show "Name"`
- [ ] `options-arena watchlist add "Name" AAPL MSFT NVDA`
- [ ] `options-arena watchlist remove "Name" MSFT`
- [ ] `options-arena watchlist delete "Name"`
- [ ] `options-arena scan --watchlist "Name"` triggers watchlist-mode scan

### Web UI
- [ ] `WatchlistPage.vue` at route `/watchlists` — list, create dialog, detail view
- [ ] `WatchlistManager.vue` — ticker table with add/remove
- [ ] `watchlist.ts` Pinia store
- [ ] TickerDrawer: "Add to Watchlist" button
- [ ] ScanPage: watchlist scan option
- [ ] Router + nav link updated

### Tests
- [ ] ~45 tests: repository CRUD, API endpoints (404/409), pipeline watchlist mode,
      CLI commands, Vue component rendering

## Technical Details

### Files to Create
- `src/options_arena/api/routes/watchlist.py`
- `web/src/stores/watchlist.ts`
- `web/src/pages/WatchlistPage.vue`
- `web/src/components/WatchlistManager.vue`
- `tests/unit/data/test_repository_watchlist.py`
- `tests/unit/api/test_watchlist_routes.py`
- `tests/unit/cli/test_watchlist_commands.py`

### Files to Modify
- `src/options_arena/data/repository.py` — add 9 methods (append-only)
- `src/options_arena/scan/pipeline.py` — Phase 1 `_phase_universe()` (~20 lines at ~line 180)
- `src/options_arena/api/app.py` — include watchlist router (+2 lines)
- `src/options_arena/api/schemas.py` — extend `ScanRequest` with `watchlist_id`
- `src/options_arena/cli/commands.py` — add `watchlist` subcommand group
- `web/src/router/index.ts` — add `/watchlists` route
- `web/src/App.vue` — add nav link
- `web/src/pages/ScanPage.vue` — add watchlist scan option
- `web/src/components/TickerDrawer.vue` — add "Add to Watchlist" button

### Key Implementation Notes
- Watchlist tables already exist (migration 001) — `description` column added by Task 1's migration
- Pipeline watchlist mode: `if watchlist_tickers: tickers = watchlist_tickers` then skip universe fetch
- API scan handler: resolve `watchlist_id` → `repo.get_tickers_for_watchlist()` → extract ticker strings → pass to pipeline
- CLI `watchlist` uses Typer sub-app: `watchlist_app = typer.Typer(); app.add_typer(watchlist_app, name="watchlist")`
- Conflict with Task 5: both modify `pipeline.py` Phase 1. Task 5 MUST run after this task merges.

### Module CLAUDE.md to Read First
- `data/CLAUDE.md`, `scan/CLAUDE.md`, `api/CLAUDE.md`, `cli/CLAUDE.md`, `web/CLAUDE.md`

## Dependencies

- [ ] Task 001 (Foundation) must be merged — provides `Watchlist`, `WatchlistTicker`,
      `WatchlistDetail` models and API schemas

## Effort Estimate

- Size: L
- Hours: 5-6
- Parallel: true (can run alongside Tasks 2 and 4)

## Definition of Done

- [ ] Code implemented following all project conventions
- [ ] ~45 tests written and passing
- [ ] `uv run ruff check . --fix && uv run ruff format .` — 0 errors
- [ ] `uv run mypy src/ --strict` — 0 errors
- [ ] `uv run pytest tests/ -v` — all existing + new tests pass
- [ ] Committed with `feat: add watchlist CRUD, pipeline integration, and Web UI`
- [ ] PR created targeting master
