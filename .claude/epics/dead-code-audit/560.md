---
name: "Re-export cleanup and deduplication (Wave 3)"
status: closed
created: 2026-03-16T22:34:20Z
updated: 2026-03-16T22:53:34Z
github: https://github.com/jobu711/options_arena/issues/560
depends_on: [558, 559]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Re-export cleanup and deduplication (Wave 3)

## Description

Remove dead `__init__.py` re-exports across indicators, services, and analysis modules. Rename `test_stationarity()` to private. Extract triplicated `_UNLIMITED_SENTINEL` to a shared constant. Update stale `reporting/CLAUDE.md`.

## Acceptance Criteria

- [ ] Dead re-exports removed from `indicators/__init__.py`: `map_regime_label_to_market_regime`, `test_stationarity` (keep `put_call_ratio_oi` — wired in Task 5)
- [ ] `test_stationarity()` renamed to `_test_stationarity()` in vol_forecast.py
- [ ] Dead re-exports removed from `services/__init__.py`: `filter_by_sectors`, `filter_by_industry_groups`, `classify_market_cap` (keep `BatchOHLCVResult`, `TickerOHLCVResult` — research says safe but conservative)
- [ ] Granular valuation exports removed from `analysis/__init__.py` (keep `compute_composite_valuation`)
- [ ] `_UNLIMITED_SENTINEL` extracted to shared location, 3 duplication sites updated
- [ ] `reporting/CLAUDE.md` updated to reflect actual structure

## Technical Details

### Re-export removals
| File | Export to remove | Reason |
|------|-----------------|--------|
| `indicators/__init__.py` | `map_regime_label_to_market_regime` (line 28, 69) | Zero consumers in src/ |
| `indicators/__init__.py` | `test_stationarity` (line 34, 80) | Zero consumers; function renamed to private |
| `services/__init__.py` | `filter_by_sectors` (line 36, 66) | Zero importers outside services/ |
| `services/__init__.py` | `filter_by_industry_groups` (line 35, 65) | Zero importers outside services/ |
| `services/__init__.py` | `classify_market_cap` (line 34, 64) | Zero importers outside services/ |
| `analysis/__init__.py` | `compute_owner_earnings_dcf`, `compute_three_stage_dcf`, `compute_ev_ebitda_relative`, `compute_residual_income` (lines 9-16) | Keep only `compute_composite_valuation` as the public API |

### Sentinel constant deduplication
Three files define `_UNLIMITED_SENTINEL = "999999.99"` independently:
- `reporting/debate_export.py` line 245
- `api/schemas.py` line 331
- `cli/rendering.py` line 410

Extract to `src/options_arena/models/constants.py` (or add to existing constants location) and import from there.

### `put_call_ratio_oi` decision
Research says Wave 3 should NOT remove this export because Wave 4 (Task 5) wires it into the pipeline. Keep it in `indicators/__init__.py`.

### `BatchOHLCVResult`/`TickerOHLCVResult` decision
Research found no Python imports outside `services/`. However, being conservative — keep in `services/__init__.py` as they're part of the public API for the services package.

## Execution

### Read First
- `src/options_arena/indicators/__init__.py` — current re-exports
- `src/options_arena/services/__init__.py` — current re-exports
- `src/options_arena/analysis/__init__.py` — current re-exports
- `src/options_arena/indicators/vol_forecast.py` — `test_stationarity()` usage
- `src/options_arena/reporting/debate_export.py` — sentinel constant
- `src/options_arena/reporting/CLAUDE.md` — stale docs

### Action
1. In `indicators/__init__.py`, remove `map_regime_label_to_market_regime` (line 28, 69) and `test_stationarity` (line 34, 80) from imports and __all__. Keep `put_call_ratio_oi`
   - WHY: zero consumers; put_call_ratio_oi kept for Task 5 wiring
2. In `indicators/vol_forecast.py`, rename `test_stationarity()` (line 59) to `_test_stationarity()`. Update internal callers within the file
   - WHY: internal helper function, not part of public API
3. In `services/__init__.py`, remove `filter_by_sectors` (line 36, 66), `filter_by_industry_groups` (line 35, 65), `classify_market_cap` (line 34, 64) from imports and __all__
   - WHY: zero importers outside services package
4. In `analysis/__init__.py`, remove `compute_owner_earnings_dcf`, `compute_three_stage_dcf`, `compute_ev_ebitda_relative`, `compute_residual_income`, `FDData`, `compute_correlation_matrix`, `compute_position_size`, `compute_risk_adjusted_metrics` exports — keep only `compute_composite_valuation`
   - WHY: composite is the public API; individual functions are internal implementation details
5. Create `src/options_arena/models/constants.py` with `UNLIMITED_SENTINEL = "999999.99"`. Update `reporting/debate_export.py`, `api/schemas.py`, `cli/rendering.py` to import from it
   - WHY: eliminate triplication of magic constant
6. Update `src/options_arena/reporting/CLAUDE.md` to reflect actual structure (only `debate_export.py`, Markdown-only format)
   - WHY: stale documentation causes confusion

### Verify
```bash
uv run ruff check src/options_arena/indicators/ src/options_arena/services/ src/options_arena/analysis/ src/options_arena/models/ src/options_arena/reporting/ src/options_arena/api/ src/options_arena/cli/ --fix
uv run ruff format src/
uv run mypy src/ --strict
uv run pytest -m "not exhaustive" -n auto -q
```

### Done
- `grep -r "from options_arena.indicators import.*map_regime_label_to_market_regime" src/` returns zero
- `grep -r "from options_arena.indicators import.*test_stationarity" src/` returns zero
- `grep -rn "_UNLIMITED_SENTINEL" src/` shows single definition in constants.py + 3 imports
- No lint/type/test errors

## Test Plan

### Test Files
- No test files need deletion for this task
- Tests that import `map_regime_label_to_market_regime` or `test_stationarity` from indicators: update import path to private function

### Test Cases
```python
# No new tests needed — this is cleanup only
# Verify existing tests pass with updated imports
```

### Edge Cases
- `put_call_ratio_oi` must remain exported (used by Task 5)
- `BatchOHLCVResult`/`TickerOHLCVResult` kept (conservative)
- `test_stationarity` internal callers within `vol_forecast.py` must use new `_test_stationarity` name
- Tests may import `from options_arena.indicators import test_stationarity` — redirect to `from options_arena.indicators.vol_forecast import _test_stationarity`

### Mocking Strategy
- No new mocks needed

## Dependencies
- [ ] Task 558 and 559 must complete first (clean baseline for additive changes)

## Effort Estimate
- Size: S
- Hours: 1-2
- Parallel: false (overlapping __init__.py files with prior tasks)

## Definition of Done
- [ ] ~40 lines source removed
- [ ] Sentinel constant deduplicated (1 definition, 3 imports)
- [ ] `reporting/CLAUDE.md` accurate
- [ ] `ruff check`, `mypy --strict`, `pytest` all green
