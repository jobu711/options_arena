---
name: Remove 3 dead universe API endpoints
status: open
created: 2026-03-23T13:34:29Z
updated: 2026-03-23T13:51:04Z
github: https://github.com/jobu711/options_arena/issues/714
depends_on: []
parallel: true
conflicts_with: [726, 735]
test_files: []
---

# Task: Remove 3 dead universe API endpoints

## Description

Remove 3 dead endpoints from `api/routes/universe.py` that have zero frontend callers.
Also remove the `MetadataStats` and `IndexStarted` schemas from `api/schemas.py` that
are only used by the deleted endpoints. The CLI `universe` commands are NOT affected.

## Acceptance Criteria

- [ ] `POST /api/universe/refresh` removed (~lines 61-77)
- [ ] `GET /api/universe/metadata/stats` removed (~lines 303-317)
- [ ] `POST /api/universe/index` removed (~lines 319-355)
- [ ] `MetadataStats` schema removed from `api/schemas.py` (~lines 817-833)
- [ ] `IndexStarted` schema removed from `api/schemas.py` (~lines 835-839)
- [ ] Related test cases removed or updated
- [ ] Remaining universe endpoints still function

## Technical Details

- `POST /api/universe/refresh` — wrapper for `UniverseService.refresh()`, CLI `universe refresh` still works
- `GET /api/universe/metadata/stats` — metadata coverage stats, only used by this endpoint
- `POST /api/universe/index` — triggers background metadata indexing, CLI `universe index` still works
- Other universe endpoints remain: `GET /api/universe/stats`, `GET /api/universe/tickers`,
  `GET /api/universe/metadata` (GET, not stats)

## Execution

### Read First
Files that MUST be read before starting this task:
- `src/options_arena/api/routes/universe.py` — identify exact line ranges
- `src/options_arena/api/schemas.py` — locate MetadataStats and IndexStarted
- `tests/unit/api/test_universe_metadata.py` — identify tests to remove

### Action
1. In `src/options_arena/api/routes/universe.py`, remove 3 endpoint functions:
   - `POST /api/universe/refresh` (~lines 61-77)
   - `GET /api/universe/metadata/stats` (~lines 303-317)
   - `POST /api/universe/index` (~lines 319-355)
   - WHY: Zero frontend callers; CLI equivalents exist
2. Remove any imports in `universe.py` that become unused after endpoint deletion
   - WHY: Unused imports fail ruff lint
3. In `src/options_arena/api/schemas.py`, remove:
   - `MetadataStats` class (~lines 817-833)
   - `IndexStarted` class (~lines 835-839)
   - WHY: Only consumers were deleted endpoints
4. In `tests/unit/api/test_universe_metadata.py`, remove test cases for deleted endpoints
   - WHY: Tests for deleted endpoints serve no purpose
5. Verify remaining universe endpoints still have their tests
   - WHY: Don't accidentally delete tests for live endpoints

### Verify
```
uv run python -c "from options_arena.api.routes.universe import router; print(f'{len(router.routes)} routes OK')"
uv run ruff check src/options_arena/api/routes/universe.py src/options_arena/api/schemas.py
uv run mypy src/options_arena/api/routes/universe.py --strict
uv run pytest tests/unit/api/ -v -k "universe"
```

### Done
- `universe.py` has 3 fewer route handlers
- `MetadataStats` and `IndexStarted` no longer exist in schemas.py
- Remaining universe endpoints still pass tests
- No lint or type errors in modified files

## Test Plan

### Test Files
- `tests/unit/api/test_universe_metadata.py` — remove dead endpoint tests, keep live ones

### Test Cases
```python
# Verify remaining universe endpoints work:
# - GET /api/universe/stats
# - GET /api/universe/tickers
# Run: uv run pytest tests/unit/api/ -v -k "universe"
```

### Edge Cases
- Shared imports between deleted and live universe endpoints
- Schema references outside the deleted endpoints
- Background task cleanup for index endpoint

### Mocking Strategy
- No mocking needed — pure deletion task

## Dependencies
- None

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: true (conflicts with 726/735 on schemas.py)
