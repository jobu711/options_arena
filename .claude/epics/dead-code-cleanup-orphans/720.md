---
name: Delete eval harness framework (runner, graders, models)
status: open
created: 2026-03-23T13:34:29Z
updated: 2026-03-23T13:51:04Z
github: https://github.com/jobu711/options_arena/issues/720
depends_on: []
parallel: true
conflicts_with: []
test_files: []
---

# Task: Delete eval harness framework (runner, graders, models)

## Description

Delete the entire eval harness framework: `evals/` directory (879 lines — runner.py,
graders.py, __init__.py), `models/eval.py` (159 lines), and `data/_eval.py` (293 lines).
Zero eval definitions exist. The framework has never been used in production.

Migration 039 (`eval_runs` table) is LEFT IN PLACE — SQLite migrations are append-only.

## Acceptance Criteria

- [ ] `evals/runner.py` deleted (399 lines)
- [ ] `evals/graders.py` deleted (464 lines)
- [ ] `evals/__init__.py` deleted (16 lines)
- [ ] `models/eval.py` deleted (159 lines)
- [ ] `data/_eval.py` deleted (293 lines)
- [ ] All eval test files deleted
- [ ] Migration 039 left in place (append-only policy)

## Technical Details

- `evals/runner.py`: `run_eval_check()` — loads definitions, runs graders, computes pass@k
- `evals/graders.py`: 3 graders — `CodeGrader`, `ModelGrader`, `OutcomeGrader`
- `evals/__init__.py`: re-exports `CodeGrader`, `ModelGrader`, `OutcomeGrader`, `run_eval_check`
- `models/eval.py`: `EvalDefinition`, `EvalRun`, `EvalReport`, `EvalOutcome`, `EvalBaseline`
- `data/_eval.py`: `EvalMixin` with 7 CRUD methods for eval definitions and runs
- Test files: `tests/unit/evals/test_eval_models.py` (260 lines), `tests/unit/evals/test_eval_persistence.py` (244 lines)

## Execution

### Read First
Files that MUST be read before starting this task:
- `src/options_arena/evals/__init__.py` — confirm re-exports
- `src/options_arena/evals/runner.py` — confirm file to delete
- `src/options_arena/evals/graders.py` — confirm file to delete
- `src/options_arena/models/eval.py` — confirm models to delete
- `src/options_arena/data/_eval.py` — confirm mixin to delete

### Action
1. Delete entire `src/options_arena/evals/` directory (3 files, 879 lines)
   - WHY: Zero eval definitions exist — framework produces no value
2. Delete `src/options_arena/models/eval.py` (159 lines)
   - WHY: Models only used by deleted eval framework
3. Delete `src/options_arena/data/_eval.py` (293 lines)
   - WHY: Persistence mixin only used by deleted eval framework
4. Delete test files:
   - `tests/unit/evals/test_eval_models.py` (260 lines)
   - `tests/unit/evals/test_eval_persistence.py` (244 lines)
   - Delete `tests/unit/evals/` directory if empty after above deletions
   - WHY: Tests for deleted code
5. Leave `data/migrations/039_*.sql` in place
   - WHY: SQLite migrations are append-only; the table becomes unused but migration history stays intact
6. Grep for remaining `from options_arena.evals import` and `from options_arena.models.eval import`
   - WHY: Catch dangling imports

### Verify
```
uv run python -c "import options_arena; print('package OK')"
uv run ruff check src/options_arena/
uv run mypy src/options_arena/ --strict
```

### Done
- `evals/` directory no longer exists
- `models/eval.py` no longer exists
- `data/_eval.py` no longer exists
- `from options_arena.evals import run_eval_check` raises `ModuleNotFoundError`
- All test files in `tests/unit/evals/` deleted

## Test Plan

### Test Files
- Delete `tests/unit/evals/test_eval_models.py`
- Delete `tests/unit/evals/test_eval_persistence.py`

### Test Cases
```python
# No new tests — verify remaining suite passes:
# uv run pytest tests/ -v --ignore=tests/unit/evals/
```

### Edge Cases
- Conftest fixtures in `tests/unit/evals/conftest.py` (delete if exists)
- Imports from eval models in other test files
- `__init__.py` in `tests/unit/evals/` directory

### Mocking Strategy
- No mocking needed — pure deletion task

## Dependencies
- None

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: true
