---
name: Delete 10 dead debate rendering functions from rendering.py
status: open
created: 2026-03-23T13:34:26Z
updated: 2026-03-23T13:50:12Z
github: https://github.com/jobu711/options_arena/issues/712
depends_on: []
parallel: true
conflicts_with: []
test_files: []
---

# Task: Delete 10 dead debate rendering functions from rendering.py

## Description
Delete 10 dead debate rendering functions from `cli/rendering.py` that belonged to the
old debate system replaced by the unified recommendation system. These functions have zero
production callers. Also remove 7 imports that become unused after deletion.

## Acceptance Criteria
- [ ] All 10 dead functions deleted from `rendering.py`
- [ ] 7 unused imports removed
- [ ] File drops from ~1,019 to ~492 lines
- [ ] Remaining live functions unchanged and working
- [ ] `ruff check` and `mypy --strict` pass on modified file

## Technical Details

### Functions to delete (lines 183-727, ~527 lines):
| Function | Lines | Purpose (dead) |
|----------|-------|----------------|
| `render_volatility_panel` | 183-230 | Old debate volatility panel |
| `render_flow_panel` | 233-272 | Old debate flow panel |
| `render_fundamental_panel` | 275-319 | Old debate fundamental panel |
| `render_risk_panel` | 322-371 | Old debate risk panel |
| `render_contrarian_panel` | 374-413 | Old debate contrarian panel |
| `render_spread_panel` | 416-467 | Old debate spread panel |
| `render_debate_panels` | 470-550 | Old debate panel orchestrator |
| `_build_agent_panel_text` | 553-585 | Helper for old debate panels |
| `_build_verdict_panel_text` | 588-672 | Helper for old verdict rendering |
| `render_batch_summary_table` | 675-727 | Old batch debate summary |

### Imports to remove (only used by dead functions):
- `DebateResult` (from `agents._parsing`)
- `ContrarianThesis`
- `FlowThesis`
- `FundamentalThesis`
- `RiskAssessment`
- `SpreadAnalysis`
- `VolatilityThesis`

### Imports to KEEP (used by live `render_debate_history`):
- `TradeThesis`
- `ExtendedTradeThesis`
- `DebateRow`

### Functions to KEEP (live):
- `_safe_text` (63-68)
- `render_health_table` (71-93)
- `render_scan_table` (96-168)
- `render_debate_history` (730-789)
- `render_recommendation` (797-843)
- `_render_assessment_panel` (846-896)
- `_render_position_recommendation` (899-973)
- `render_recommendation_batch_summary` (976-1019)

## Execution

### Read First
- `src/options_arena/cli/rendering.py` — full file, verify line ranges before deletion
- `src/options_arena/cli/CLAUDE.md` — module conventions

### Action
1. Read `rendering.py` to confirm exact line ranges of 10 dead functions
   - WHY: Line numbers may have shifted since audit; verify before bulk delete
2. Delete lines 183-727 (all 10 dead functions)
   - WHY: Zero production callers confirmed by audit; old debate system replaced by recommendation
3. Remove 7 unused imports: `DebateResult`, `ContrarianThesis`, `FlowThesis`, `FundamentalThesis`, `RiskAssessment`, `SpreadAnalysis`, `VolatilityThesis`
   - WHY: These types were only used by the deleted functions
4. Verify `TradeThesis`, `ExtendedTradeThesis`, `DebateRow` imports remain (used by live `render_debate_history`)
   - WHY: Deleting these would break the live debate history renderer
5. Run `ruff check` to catch any remaining unused imports or references
   - WHY: Automated detection of any missed cleanup

### Verify
```
uv run ruff check src/options_arena/cli/rendering.py --fix
uv run ruff format src/options_arena/cli/rendering.py
uv run mypy src/options_arena/cli/rendering.py --strict
```

### Done
- `rendering.py` contains only 8 live functions (~492 lines)
- Zero references to deleted functions remain in `rendering.py`
- `ruff check` and `mypy --strict` pass with no errors
- `render_debate_history`, `render_recommendation`, and all other live functions unchanged

## Test Plan

### Test Files
- No new test files needed — this is pure deletion

### Test Cases
Verification that live functions still work is covered by existing tests in `test_rendering.py`
and `test_batch_debate.py` (modified in Task 717).

### Edge Cases
- Verify no dynamic string-based imports of deleted functions anywhere in codebase
- Verify no `__all__` lists reference deleted functions

### Mocking Strategy
- N/A — pure deletion task

## Dependencies
- None

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: true
