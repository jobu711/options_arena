---
name: Update/delete 5 context renderer test files
status: open
created: 2026-03-23T13:34:26Z
updated: 2026-03-23T13:50:12Z
github: https://github.com/jobu711/options_arena/issues/715
depends_on: [725]
parallel: false
conflicts_with: []
test_files: [tests/unit/agents/test_neural_context.py]
---

# Task: Update/delete 5 context renderer test files

## Description
Clean up 5 test files that exercise the 6 dead domain-specific context renderers deleted
in Task 725. Four files test only dead functions (delete entirely). One file is mixed
(tests both `_render_neural_context` which is live, and `render_context_block`).

## Acceptance Criteria
- [ ] `test_domain_renderers.py` deleted entirely (729 lines, tests dead renderers)
- [ ] `test_macro_rendering.py` deleted entirely (208 lines, tests dead `render_macro_context`)
- [ ] `test_vol_rendering.py` deleted entirely (144 lines, tests dead `render_volatility_context`)
- [ ] `test_volatility_ml_context.py` deleted entirely (120 lines, tests dead `render_volatility_context` ML fields)
- [ ] `test_neural_context.py` reviewed — keep tests for live `_render_neural_context`/`render_context_block`, remove any tests for dead functions
- [ ] All remaining tests pass

## Technical Details

### Files to DELETE entirely:
| File | Lines | Reason |
|------|-------|--------|
| `tests/unit/agents/test_domain_renderers.py` | ~729 | Tests all 6 dead domain renderers + `PROMPT_RULES_APPENDIX` |
| `tests/unit/agents/test_macro_rendering.py` | ~208 | Tests dead `render_macro_context` only |
| `tests/unit/agents/test_vol_rendering.py` | ~144 | Tests dead `render_volatility_context` surface mispricing |
| `tests/unit/agents/test_volatility_ml_context.py` | ~120 | Tests dead `render_volatility_context` ML forecast fields |

### Files to REVIEW (may need modification):
| File | Lines | Action |
|------|-------|--------|
| `tests/unit/agents/test_neural_context.py` | ~160 | Keep tests for `_render_neural_context` (live helper). Remove any tests that import dead functions. |

**Note on `test_domain_renderers.py`**: This file also tests `PROMPT_RULES_APPENDIX` which is LIVE.
If those tests exist, extract them to a separate file or `test_neural_context.py` before deletion.

## Execution

### Read First
- `tests/unit/agents/test_domain_renderers.py` — check if any tests cover live functions (PROMPT_RULES_APPENDIX)
- `tests/unit/agents/test_neural_context.py` — identify which tests are for live vs dead functions
- `tests/unit/agents/test_macro_rendering.py` — confirm 100% dead
- `tests/unit/agents/test_vol_rendering.py` — confirm 100% dead
- `tests/unit/agents/test_volatility_ml_context.py` — confirm 100% dead

### Action
1. Read `test_domain_renderers.py` to check for live `PROMPT_RULES_APPENDIX` tests
   - WHY: `PROMPT_RULES_APPENDIX` is live; tests for it must be preserved
2. If `PROMPT_RULES_APPENDIX` tests exist, move them to `test_neural_context.py` or a new file
   - WHY: Cannot delete tests for live constants
3. Delete `test_domain_renderers.py`, `test_macro_rendering.py`, `test_vol_rendering.py`, `test_volatility_ml_context.py`
   - WHY: All tests exercise deleted functions
4. Review `test_neural_context.py` — remove any imports of dead functions
   - WHY: Dead imports would cause ImportError
5. Clean up any orphaned fixtures in `tests/unit/agents/conftest.py`
   - WHY: Deleted test files may have been the sole consumers of certain fixtures

### Verify
```
uv run pytest tests/unit/agents/test_neural_context.py -v
uv run pytest tests/unit/agents/ -v --co -q 2>&1 | head -30
uv run ruff check tests/unit/agents/ --fix
```

### Done
- 4 test files deleted (~1,201 lines removed)
- `test_neural_context.py` passes with only live-function tests
- Any `PROMPT_RULES_APPENDIX` tests preserved
- No orphaned fixtures in conftest

## Test Plan

### Test Files
- `tests/unit/agents/test_neural_context.py` (modified, not new)

### Test Cases
Existing live tests to verify still pass:
```python
# In test_neural_context.py — these MUST survive:
class TestNeuralContext:
    def test_render_neural_context(self) -> None: ...
    def test_render_context_block_includes_neural(self) -> None: ...
```

### Edge Cases
- `test_domain_renderers.py` may contain `PROMPT_RULES_APPENDIX` tests — must preserve
- Conftest fixtures may become orphaned after deletion
- Verify no test collection errors after deletion

### Mocking Strategy
- N/A — test cleanup task

## Dependencies
- [725] Dead renderer functions must be deleted before their tests

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: false (depends on 725)
