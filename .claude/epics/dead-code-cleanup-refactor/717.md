---
name: Update/delete 4 rendering test files
status: open
created: 2026-03-23T13:34:26Z
updated: 2026-03-23T13:50:12Z
github: https://github.com/jobu711/options_arena/issues/717
depends_on: [712]
parallel: false
conflicts_with: []
test_files: [tests/unit/cli/test_rendering.py, tests/unit/cli/test_batch_debate.py]
---

# Task: Update/delete 4 rendering test files

## Description
Clean up 4 test files that exercise the 10 dead debate rendering functions deleted in
Task 712. Two files test only dead functions (delete entirely). Two files are mixed
(remove dead tests, keep live tests).

## Acceptance Criteria
- [ ] `test_rendering_v2.py` deleted entirely (339 lines, 100% dead tests)
- [ ] `test_spread_rendering.py` deleted entirely (150 lines, 100% dead tests)
- [ ] `test_rendering.py` has 3 dead tests removed, 5 live tests retained
- [ ] `test_batch_debate.py` has 1 dead test removed, 6 live tests retained
- [ ] All remaining tests pass

## Technical Details

### Files to DELETE entirely:
| File | Lines | Reason |
|------|-------|--------|
| `tests/unit/cli/test_rendering_v2.py` | ~339 | All 8+ tests exercise deleted debate panel functions |
| `tests/unit/cli/test_spread_rendering.py` | ~150 | All 10 tests exercise deleted `render_spread_panel` |

### Files to MODIFY:
| File | Remove | Keep | Details |
|------|--------|------|---------|
| `tests/unit/cli/test_rendering.py` | 3 tests | 5 tests | Remove: `test_render_volatility_panel_all_fields`, `test_render_volatility_panel_minimal`, `test_render_debate_panels_trend_heading` |
| `tests/unit/cli/test_batch_debate.py` | 1 test | 6 tests | Remove: `test_render_batch_summary_table_success_and_failure` |

### Imports to clean up:
After removing dead tests, clean up any imports of deleted functions/types (e.g.,
`render_volatility_panel`, `render_debate_panels`, `render_batch_summary_table`,
`VolatilityThesis`, `DebateResult`, etc.).

## Execution

### Read First
- `tests/unit/cli/test_rendering.py` — identify exact tests to remove vs keep
- `tests/unit/cli/test_rendering_v2.py` — confirm 100% dead tests
- `tests/unit/cli/test_spread_rendering.py` — confirm 100% dead tests
- `tests/unit/cli/test_batch_debate.py` — identify the 1 dead test to remove

### Action
1. Delete `tests/unit/cli/test_rendering_v2.py` entirely
   - WHY: All tests exercise deleted debate panel functions; zero live tests
2. Delete `tests/unit/cli/test_spread_rendering.py` entirely
   - WHY: All tests exercise deleted `render_spread_panel`; zero live tests
3. In `test_rendering.py`, delete 3 tests that reference deleted functions
   - WHY: Tests for functions that no longer exist
4. In `test_rendering.py`, clean up unused imports from deleted functions
   - WHY: ruff will flag unused imports
5. In `test_batch_debate.py`, delete `test_render_batch_summary_table_success_and_failure`
   - WHY: Tests deleted `render_batch_summary_table`
6. In `test_batch_debate.py`, clean up unused imports
   - WHY: ruff will flag unused imports
7. Run remaining tests to verify no breakage
   - WHY: Confirm live tests still pass after modifications

### Verify
```
uv run pytest tests/unit/cli/test_rendering.py -v
uv run pytest tests/unit/cli/test_batch_debate.py -v
uv run ruff check tests/unit/cli/ --fix
uv run ruff format tests/unit/cli/
```

### Done
- 2 test files deleted (489 lines removed)
- 4 dead tests removed from 2 remaining files
- All remaining tests in `test_rendering.py` and `test_batch_debate.py` pass
- No ruff errors in `tests/unit/cli/`

## Test Plan

### Test Files
- `tests/unit/cli/test_rendering.py` (modified, not new)
- `tests/unit/cli/test_batch_debate.py` (modified, not new)

### Test Cases
Existing live tests to verify still pass:
```python
# In test_rendering.py — these MUST survive:
class TestRendering:
    def test_render_health_table(self) -> None: ...
    def test_render_scan_table(self) -> None: ...
    # (3 more live tests)

# In test_batch_debate.py — these MUST survive:
class TestBatchDebate:
    def test_batch_debate_routing(self) -> None: ...
    # (5 more live tests)
```

### Edge Cases
- Verify conftest fixtures referenced by deleted tests aren't orphaned
- Verify no test imports reference deleted production functions

### Mocking Strategy
- N/A — test cleanup task

## Dependencies
- [712] Dead functions must be deleted before their tests

## Effort Estimate
- Size: S
- Hours: 0.25
- Parallel: false (depends on 712)
