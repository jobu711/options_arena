---
name: Final verification — lint, typecheck, full test suite
status: open
created: 2026-03-23T13:36:04Z
updated: 2026-03-23T13:50:24Z
github: https://github.com/jobu711/options_arena/issues/731
depends_on: [730]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Final verification — lint, typecheck, full test suite

## Description

Comprehensive verification pass after all helper extractions are complete.
Run the full project lint, type checker, and test suite to confirm zero
regressions. Fix any issues found. Regenerate docs. This is the gate before
the epic can be marked complete.

**No new features — verification and fix-up only.**

## Acceptance Criteria

- [ ] `ruff check .` passes with zero errors
- [ ] `ruff format .` reports no changes needed
- [ ] `mypy src/ --strict` passes with zero errors
- [ ] `pytest -m critical -q` passes (pre-commit tier)
- [ ] `pytest -m "not exhaustive" -n auto -q` passes (CI tier)
- [ ] Integration test `test_scan_pipeline.py` passes
- [ ] Line count of `process_ticker_options` confirmed <= 60 lines
- [ ] Total line count change documented (should be net ~0 — pure refactor)

## Technical Details

### Verification checklist

1. **Lint**: `uv run ruff check . --fix && uv run ruff format .`
2. **Type check**: `uv run mypy src/ --strict`
3. **Critical tests**: `uv run pytest -m critical -q`
4. **Full unit tests**: `uv run pytest tests/unit/ -n auto -q`
5. **Integration tests**: `uv run pytest tests/integration/ -v`
6. **Scan-specific tests**: `uv run pytest tests/unit/scan/ -v`
7. **Line count**: `grep -c '' <(sed -n '/^async def process_ticker_options/,/^[^ ]/p' src/options_arena/scan/phase_options.py)`
8. **Docs regeneration**: `python tools/docgen.py`

### Common issues to watch for

- Unused imports after helper extraction (ruff F401)
- Type narrowing issues with the new dataclass returns
- Missing `from __future__ import annotations` if new dataclasses need it
- mypy errors from `np.ndarray` fields (may need `type: ignore` for numpy)
- Test fixtures that assumed the old function structure

## Execution

### Read First
Files that MUST be read before starting this task:
- `src/options_arena/scan/phase_options.py` — final state after all extractions
- Test output from Tasks 1–4

### Action
1. Run `uv run ruff check . --fix && uv run ruff format .`
   - WHY: catch any formatting or lint issues from the refactor
2. Run `uv run mypy src/ --strict`
   - WHY: catch any type annotation issues
3. Run `uv run pytest -m critical -q`
   - WHY: fast pre-commit gate
4. Run `uv run pytest -m "not exhaustive" -n auto -q`
   - WHY: full CI-level validation
5. Fix any failures found in steps 1–4
   - WHY: epic cannot be marked complete with test failures
6. Run `python tools/docgen.py` to regenerate technical reference
   - WHY: CLAUDE.md verification requirements
7. Document final line counts: `process_ticker_options` and total `phase_options.py`
   - WHY: measurable success criterion from epic

### Verify
```
uv run ruff check . --fix && uv run ruff format .
uv run mypy src/ --strict
uv run pytest -m critical -q
uv run pytest -m "not exhaustive" -n auto -q
uv run pytest tests/integration/scan/test_scan_pipeline.py -v
python tools/docgen.py
```

### Done
- All lint, type, and test gates pass
- `process_ticker_options` confirmed <= 60 lines
- Docs regenerated
- Ready for commit

## Test Plan

### Test Files
No new test files — this task runs existing tests.

### Test Cases
N/A — verification task, not implementation.

### Edge Cases
N/A

### Mocking Strategy
N/A

## Dependencies
- [ ] Task #730 must be complete

## Effort Estimate
- Size: S
- Hours: 0.5
- Parallel: false

## Definition of Done
- [ ] All verification commands pass
- [ ] Any failures fixed
- [ ] Docs regenerated
- [ ] Line count documented
