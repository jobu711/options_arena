---
name: Write comprehensive tests for debate export
status: closed
created: 2026-02-25T13:14:31Z
updated: 2026-02-25T14:19:46Z
github: https://github.com/jobu711/options_arena/issues/113
depends_on: [109, 110, 111]
parallel: false
conflicts_with: []
---

# Task: Write comprehensive tests for debate export

## Description
Write ~8+ tests covering the full export feature as specified in the epic. Tests should cover
markdown generation, file writing, PDF error handling, directory creation, and batch export.
All tests use pytest with fixtures for `DebateResult` construction.

## Acceptance Criteria
- [ ] `export_debate_markdown()` output contains all section headers
- [ ] Markdown includes vol thesis section when `vol_response` is present
- [ ] Markdown includes rebuttal section when `bull_rebuttal` is present
- [ ] Markdown includes fallback warning when `is_fallback=True`
- [ ] `export_debate_to_file()` writes markdown file to temp path correctly
- [ ] PDF export raises `ImportError` when weasyprint is not installed
- [ ] Export directory is created automatically when it doesn't exist
- [ ] Batch export creates per-ticker files (when batch mode available)
- [ ] All tests pass with `uv run pytest tests/ -v`

## Technical Details
- **Test file**: `tests/test_reporting/test_debate_export.py` (or follow existing test layout)
- **Fixtures needed**:
  - `make_agent_response()` — factory for `AgentResponse` with defaults
  - `make_trade_thesis()` — factory for `TradeThesis` with defaults
  - `make_debate_result()` — factory for `DebateResult` with all fields populated
  - Use `tmp_path` pytest fixture for file write tests
- **Mock strategy**:
  - Mock `weasyprint` import for PDF tests (use `unittest.mock.patch`)
  - No need to mock anything for markdown tests (pure function)
- **Assertions**:
  - Use `in` checks for section headers in markdown output
  - Use `Path.exists()` and `Path.read_text()` for file tests
  - Use `pytest.raises(ImportError)` for PDF without weasyprint
- Follow existing test patterns in the project (check `tests/CLAUDE.md`)

## Dependencies
- [ ] Task 109 (markdown export function to test)
- [ ] Task 110 (file export + PDF to test)
- [ ] Task 111 (CLI integration to potentially test)

## Effort Estimate
- Size: M
- Hours: 3-5
- Parallel: false (needs implementations to test against)

## Definition of Done
- [ ] All ~8+ tests written and passing
- [ ] Tests cover all epic-specified scenarios
- [ ] Passes `ruff check`, `ruff format`, `mypy --strict`
- [ ] `uv run pytest tests/ -v` passes with no failures
