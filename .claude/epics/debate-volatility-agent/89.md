---
name: Update DebateDeps, DebateResult, and add parsing helper
status: open
created: 2026-02-25T15:00:36Z
updated: 2026-02-25T15:06:08Z
github: https://github.com/jobu711/options_arena/issues/89
depends_on: [87]
parallel: false
conflicts_with: []
---

# Task: Update DebateDeps, DebateResult, and add parsing helper

## Description
Extend the `DebateDeps` dataclass with a `vol_response` field so the Risk agent can access the Volatility Agent's output. Extend `DebateResult` with `vol_response` so the CLI can render it. Add `build_cleaned_volatility_thesis()` helper in `_parsing.py` that strips `<think>` tags from string fields, following the same pattern as `build_cleaned_agent_response()` and `build_cleaned_trade_thesis()`.

## Acceptance Criteria
- [ ] `DebateDeps.vol_response: VolatilityThesis | None = None` field added
- [ ] `DebateResult.vol_response: VolatilityThesis | None = None` field added
- [ ] `build_cleaned_volatility_thesis(thesis: VolatilityThesis) -> VolatilityThesis` function added
- [ ] Cleaning function strips `<think>` tags from all string fields: `iv_assessment`, `iv_rank_interpretation`, `strategy_rationale`, each item in `suggested_strikes`, each item in `key_vol_factors`, `model_used`
- [ ] Uses existing `strip_think_tags()` helper — no new stripping logic
- [ ] Returns a new frozen instance (since `VolatilityThesis` is frozen)
- [ ] Existing tests still pass — `DebateDeps` and `DebateResult` backward-compatible (new field is optional with default `None`)

## Technical Details
- File: `src/options_arena/agents/_parsing.py`
- Import `VolatilityThesis` from `options_arena.models`
- `DebateDeps` is a `@dataclass`, add `vol_response: VolatilityThesis | None = None` (must go after existing optional fields to maintain dataclass ordering)
- `DebateResult` is a `@dataclass`, add `vol_response: VolatilityThesis | None = None`
- Pattern for cleaning: construct new `VolatilityThesis(...)` with stripped string fields, pass through non-string fields unchanged. Use `model_construct()` or keyword args.

## Dependencies
- [ ] #87 — `VolatilityThesis` model must exist

## Effort Estimate
- Size: S
- Hours: 2
- Parallel: false (blocked by #87)

## Definition of Done
- [ ] Code implemented
- [ ] Tests written (cleaning with think tags, cleaning without think tags, empty string fallback)
- [ ] Existing agent tests still pass
- [ ] `uv run ruff check . --fix && uv run ruff format .` passes
- [ ] `uv run mypy src/ --strict` passes
