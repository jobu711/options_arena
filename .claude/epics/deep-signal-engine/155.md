---
name: IV & Volatility indicators + IV surface utilities
status: open
created: 2026-02-28T15:36:34Z
updated: 2026-02-28T15:47:54Z
github: https://github.com/jobu711/options_arena/issues/155
depends_on: [154]
parallel: true
conflicts_with: []
---

# Task: IV & Volatility indicators + IV surface utilities

## Description

Build the IV modeling layer: 13 indicators spanning IV term structure, skew, vol regime, expected moves, and HV forecasting. Create `pricing/iv_surface.py` for batch IV solving and ATM extraction. Extend the Volatility Agent prompt with rich IV context. All indicators are computable from yfinance chains + existing BSM/BAW infrastructure.

Branch: `epic/dse-1-volatility`

## Acceptance Criteria

- [ ] `pricing/iv_surface.py` created with: `extract_atm_iv()`, `extract_atm_iv_by_dte()`, `compute_iv_at_delta()`, `batch_iv_solve()`
- [ ] `indicators/iv_analytics.py` created with all 13 indicator functions (see Technical Details)
- [ ] `indicators/volatility.py` extended with `compute_hv_20d()` and `compute_ewma_vol_forecast()` helpers
- [ ] `agents/prompts/volatility_agent.py` updated with new IV indicator context sections (IV Regime, IV vs Realized, Term Structure, Skew, Expected Moves, Correlation)
- [ ] Each indicator function: takes typed inputs, returns `float | None` (or enum | None), handles insufficient data gracefully
- [ ] All `IndicatorSignals` fields for this epic populated correctly by indicator functions
- [ ] Unit tests for all 13 indicators (happy path, insufficient data, edge cases, boundary values)
- [ ] Unit tests for IV surface utilities (ATM extraction, delta-based IV, batch solve, missing data)
- [ ] `ruff check`, `ruff format`, `mypy --strict` pass
- [ ] All existing tests pass unchanged

## Technical Details

### Files Created
- `src/options_arena/indicators/iv_analytics.py` — 13 indicator functions
- `src/options_arena/pricing/iv_surface.py` — batch IV solving, ATM IV extraction, term structure utilities
- `tests/unit/indicators/test_iv_analytics.py` — full indicator test coverage
- `tests/unit/pricing/test_iv_surface.py` — IV surface utility tests

### Files Modified
- `src/options_arena/indicators/volatility.py` — add `compute_hv_20d()`, `compute_ewma_vol_forecast()`
- `src/options_arena/agents/prompts/volatility_agent.py` — updated prompt template

### Indicators (13 total)
| # | Function | Returns | Priority |
|---|----------|---------|----------|
| 19 | `compute_iv_hv_spread(atm_iv_30d, hv_20d)` | `float \| None` | P0 |
| 20 | `compute_hv_20d(close_series)` | `float \| None` | P0 |
| 21 | `compute_iv_term_slope(iv_60d, iv_30d)` | `float \| None` | P0 |
| 22 | `compute_iv_term_shape(slope)` | `IVTermStructureShape \| None` | P0 |
| 23 | `compute_put_skew(iv_25d_put, iv_atm)` | `float \| None` | P1 |
| 24 | `compute_call_skew(iv_25d_call, iv_atm)` | `float \| None` | P1 |
| 25 | `compute_skew_ratio(iv_25d_put, iv_25d_call)` | `float \| None` | P1 |
| 26 | `classify_vol_regime(iv_rank)` | `VolRegime \| None` | P0 |
| 27 | `compute_ewma_vol_forecast(returns, lambda_=0.94)` | `float \| None` | P1 |
| 28 | `compute_vol_cone_pctl(hv_20d, hv_history)` | `float \| None` | P2 |
| 29 | `compute_vix_correlation(ticker_returns, vix_changes)` | `float \| None` | P2 |
| 36 | `compute_expected_move(spot, atm_iv, dte)` | `float \| None` | P0 |
| 37 | `compute_expected_move_ratio(iv_em, avg_actual_move)` | `float \| None` | P0 |

### Key Constraints
- Read `indicators/CLAUDE.md` and `pricing/CLAUDE.md` before modifying
- `indicators/` takes pandas in, returns pandas/float out — no Pydantic models, no API calls
- `pricing/iv_surface.py` uses `pricing/dispatch.py` for IV solving — never import `bsm` or `american` directly
- IV surface functions are async (they call pricing dispatch which may involve BAW solver)
- All numeric returns guarded with `math.isfinite()` before returning
- Compute budget: <10s additional per full scan

## Dependencies

- [ ] Task 001 (Foundation) must be merged — provides `VolRegime`, `IVTermStructureShape` enums and `IndicatorSignals` fields

## Effort Estimate

- Size: L
- Hours: 16-24
- Parallel: true (with Tasks 003, 004, 005, 006)

## Definition of Done

- [ ] Code implemented with full type annotations
- [ ] Tests written and passing (new + all existing)
- [ ] `ruff check`, `ruff format`, `mypy --strict` pass
- [ ] Branch `epic/dse-1-volatility` created, PR merged to master
