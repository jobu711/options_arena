---
name: Package re-exports (__init__.py)
status: open
created: 2026-02-22T17:26:08Z
updated: 2026-02-22T17:32:40Z
github: https://github.com/jobu711/options_arena/issues/4
depends_on: [5, 7, 9, 11, 2]
parallel: false
conflicts_with: []
---

# Task: Package re-exports (__init__.py)

## Description
Create the `models/__init__.py` that re-exports all public models and enums from the package. Consumers import from the package (`from options_arena.models import OptionContract`), never from submodules.

## Acceptance Criteria
- [ ] `src/options_arena/models/__init__.py` re-exports all public symbols:
  - 9 enums from `enums.py`
  - 3 models from `market_data.py` (OHLCV, Quote, TickerInfo)
  - 4 models from `options.py` (OptionGreeks, OptionContract, SpreadLeg, OptionSpread)
  - 3 models from `analysis.py` (MarketContext, AgentResponse, TradeThesis)
  - 3 models from `scan.py` (IndicatorSignals, ScanRun, TickerScore)
  - 4 models from `config.py` (AppSettings, ScanConfig, PricingConfig, ServiceConfig)
  - 1 model from `health.py` (HealthStatus)
- [ ] `__all__` list includes all 27 exported names
- [ ] `from options_arena.models import OptionContract` works
- [ ] `from options_arena.models import AppSettings` works
- [ ] Also create/update `src/options_arena/utils/__init__.py` to re-export exceptions
- [ ] Passes `ruff check` and `mypy --strict`

## Technical Details
- Follow the exact re-export pattern from models/CLAUDE.md
- Alphabetical ordering within each section in `__all__`
- Use explicit imports, not `*` imports
- `utils/__init__.py` re-exports: `DataFetchError`, `TickerNotFoundError`, `InsufficientDataError`, `DataSourceUnavailableError`, `RateLimitExceededError`

## Dependencies
- Issues #5, #7, #9, #11, #2 (all model files must exist to import from them)

## Effort Estimate
- Size: XS
- Hours: 1
- Parallel: false — requires all model files to be complete
