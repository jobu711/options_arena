---
name: Package Integration & Verification Gate
status: open
created: 2026-02-23T11:31:08Z
updated: 2026-02-23T11:38:21Z
github: https://github.com/jobu711/options_arena/issues/34
depends_on: [30, 35, 36, 37, 31, 32, 33]
parallel: false
conflicts_with: []
---

# Task: Package Integration & Verification Gate

## Description
Final integration task: create package `__init__.py` with all public re-exports, add `fred_api_key` field to `ServiceConfig`, create `services/CLAUDE.md` module documentation, and run the full verification gate (ruff + pytest + mypy --strict).

## Acceptance Criteria
- [ ] `services/__init__.py` exports all 7 public classes with `__all__`
- [ ] `helpers.py` is NOT exported (internal to the package)
- [ ] `ServiceConfig` updated with `fred_api_key: str | None = None` (backward-compatible)
- [ ] `services/CLAUDE.md` created with module listing, patterns, and common mistakes
- [ ] All three verification commands pass:
  - `uv run ruff check . --fix && uv run ruff format .`
  - `uv run pytest tests/ -v`
  - `uv run mypy src/ --strict`
- [ ] Total test count target: ~810+ (708 existing + ~100+ new services tests)
- [ ] All imports resolve: `from options_arena.services import MarketDataService` works

## Technical Details
- **Files to create/modify**:
  - `src/options_arena/services/__init__.py` (create)
  - `src/options_arena/services/CLAUDE.md` (create)
  - `src/options_arena/models/config.py` (modify — add `fred_api_key` field)
- Re-export pattern matches existing packages (`models/__init__.py`, `scoring/__init__.py`)
- `CLAUDE.md` follows existing per-module pattern (see `models/CLAUDE.md`, `scoring/CLAUDE.md`)

## Dependencies
- [ ] All tasks 001–007 must be complete
- [ ] All tests from 001–007 must pass

## Effort Estimate
- Size: S
- Hours: 3
- Parallel: false (must wait for all other tasks)

## Definition of Done
- [ ] Package `__init__.py` with all re-exports
- [ ] `services/CLAUDE.md` with module documentation
- [ ] `ServiceConfig.fred_api_key` field added
- [ ] All three verification commands pass clean
- [ ] Total test count meets target (~810+)
- [ ] No regressions in existing 708 tests
