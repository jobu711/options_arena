---
name: Write 001_initial.sql migration
status: open
created: 2026-02-23T18:02:43Z
updated: 2026-02-23T18:08:27Z
github: https://github.com/jobu711/options_arena/issues/41
depends_on: [40]
parallel: false
conflicts_with: []
---

# Task: Write 001_initial.sql migration

## Description
Create the initial SQL migration file at `data/migrations/001_initial.sql`. This defines the full database schema: `scan_runs`, `ticker_scores`, `service_cache`, and v2 placeholder tables (`ai_theses`, `watchlists`, `watchlist_tickers`). The `schema_version` table is NOT in this file — it is created by `Database.connect()` before migrations run.

## Acceptance Criteria
- [ ] File exists at `data/migrations/001_initial.sql`
- [ ] `data/migrations/` directory created at project root (not inside `src/`)
- [ ] `scan_runs` table: id (PK AUTOINCREMENT), started_at (TEXT NOT NULL), completed_at (TEXT nullable), preset (TEXT NOT NULL), tickers_scanned (INTEGER NOT NULL), tickers_scored (INTEGER NOT NULL), recommendations (INTEGER NOT NULL)
- [ ] `ticker_scores` table: id (PK AUTOINCREMENT), scan_run_id (FK → scan_runs.id), ticker (TEXT NOT NULL), composite_score (REAL NOT NULL), direction (TEXT NOT NULL), signals_json (TEXT NOT NULL), UNIQUE(scan_run_id, ticker)
- [ ] Index `idx_ticker_scores_scan_id` on `ticker_scores(scan_run_id)`
- [ ] `service_cache` table: key (TEXT PK), value (BLOB), expires_at (REAL) — matches `services/cache.py` schema
- [ ] `ai_theses` table: v2 placeholder with scan_run_id FK, ticker, bull/bear/risk/verdict JSON, created_at
- [ ] `watchlists` table: v2 placeholder with unique name, created_at
- [ ] `watchlist_tickers` table: v2 placeholder with watchlist_id FK, ticker, added_at, UNIQUE(watchlist_id, ticker)
- [ ] All tables use `CREATE TABLE IF NOT EXISTS`
- [ ] All indexes use `CREATE INDEX IF NOT EXISTS`
- [ ] No `schema_version` table in this file (created by Database.connect())
- [ ] All datetime columns documented as ISO 8601 UTC in comments

## Technical Details
- Location: `data/migrations/001_initial.sql` (project root)
- All TEXT datetime columns store ISO 8601 format (`2026-02-23T17:55:14+00:00`)
- `signals_json` stores `IndicatorSignals.model_dump_json()` output
- `preset` stores `ScanPreset` string value (`"full"`, `"sp500"`, `"etfs"`)
- `direction` stores `SignalDirection` string value (`"bullish"`, `"bearish"`, `"neutral"`)
- `service_cache` schema must match what `ServiceCache.init_db()` creates in `services/cache.py`
- v2 tables are created now to avoid future migration when those features land

## Dependencies
- [ ] Task 40 (data/CLAUDE.md) — read before writing SQL

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: false — must exist before database.py can test migrations

## Definition of Done
- [ ] SQL file exists and is syntactically valid
- [ ] All 6 tables defined with correct column types
- [ ] Foreign keys and indexes in place
- [ ] Comments document column semantics
