---
name: Extract phase_options.py with helpers and constants
status: closed
created: 2026-03-09T19:11:03Z
updated: 2026-03-09T19:17:37Z
github: https://github.com/jobu711/options_arena/issues/425
depends_on: [424]
parallel: false
conflicts_with: [426, 427]
test_files: []
---

# Task: Extract phase_options.py with helpers and constants

## Description

Extract the largest phase (Phase 3 — options) from `ScanPipeline` into `scan/phase_options.py`.
This is the most complex extraction at ~560 LOC, including the main phase method, the per-ticker
helper, 5 module-level helper functions, and constants (`_PHASE3_FIELDS`, `_REGIME_*`).

## Acceptance Criteria

- [ ] `scan/phase_options.py` exists with all Phase 3 code (~560 LOC)
- [ ] Public functions: `run_options_phase()`, `process_ticker_options()`
- [ ] Private helpers: `_extract_mp_strike()`, `_merge_signals()`, `_normalize_phase3_signals()`,
      `_recompute_composite_scores()`, `_recompute_dimensional_scores()`
- [ ] Constants: `_PHASE3_FIELDS` tuple, `_REGIME_BULL`, `_REGIME_BEAR`, `_REGIME_NEUTRAL`
- [ ] All `self._*` references replaced with explicit parameters
- [ ] `run_options_phase()` signature matches PRD: `(scoring_result, universe_result, progress, *, fred, market_data, options_data, repository, scan_config, pricing_config)`
- [ ] `process_ticker_options()` extracted from `_process_ticker_options()` with explicit params
- [ ] Correct import block (stdlib, third-party, local — sorted per ruff I rules)
- [ ] `ruff check` + `ruff format` clean
- [ ] `mypy --strict` passes on the new file

## Technical Details

### Code Extraction Map

| Source (pipeline.py) | Target (phase_options.py) | Lines |
|---------------------|--------------------------|-------|
| `_phase_options()` | `run_options_phase()` | 545-720 |
| `_process_ticker_options()` | `process_ticker_options()` | 722-889 |
| `_PHASE3_FIELDS` | `_PHASE3_FIELDS` | 1122-1170 |
| `_extract_mp_strike()` | `_extract_mp_strike()` | 1173-1210 |
| `_merge_signals()` | `_merge_signals()` | 1213-1225 |
| `_normalize_phase3_signals()` | `_normalize_phase3_signals()` | 1228-1281 |
| `_recompute_composite_scores()` | `_recompute_composite_scores()` | 1284-1314 |
| `_REGIME_*` constants | `_REGIME_*` constants | 1319-1321 |
| `_recompute_dimensional_scores()` | `_recompute_dimensional_scores()` | 1324-1362 |

### self._* → Parameter Mapping

- `self._fred` → `fred: FredService`
- `self._market_data` → `market_data: MarketDataService`
- `self._options_data` → `options_data: OptionsDataService`
- `self._repository` → `repository: Repository`
- `self._settings.scan` → `scan_config: ScanConfig`
- `self._settings.pricing` → `pricing_config: PricingConfig`

### Important Notes

- `_process_ticker_options()` was an instance method calling `self._*` — convert to standalone
  function with explicit services/config parameters
- Module-level helpers (`_extract_mp_strike`, `_merge_signals`, etc.) are already standalone
  functions — they just move files with no parameter changes
- `_PHASE3_FIELDS` is a constant tuple — copy verbatim
- `_REGIME_*` constants — copy verbatim
- `run_options_phase()` mutates `ScoringResult.scores` in-place (merges Phase 3 fields,
  recomputes composites) — document this in the docstring but DO NOT change behavior

### Imports Needed

- stdlib: `asyncio`, `logging`, `math`, `datetime`, `decimal`, `zoneinfo`
- third-party: `numpy`, `pandas` (if used by helpers)
- local: models (many types), services (Fred, MarketData, Options), data (Repository),
  scoring (5+ functions), indicators.options_specific, scan.models, scan.progress, scan.indicators

### DO NOT

- Modify `pipeline.py` yet (Task 427)
- Modify any test files
- Change any logic — pure code relocation
- Add thin wrappers yet (Task 427)

## Test Plan

### Test Files

No new test files. Existing scan tests are the regression suite.

### Test Cases

```python
# No new tests — verification is ruff + mypy on the new file.
# Full test pass happens after Task 427 wires the wrappers.
```

### Edge Cases

- `_PHASE3_FIELDS` tuple must be copied exactly (used by normalization helpers)
- `_REGIME_*` constants must match exact values
- `_process_ticker_options` had `self` parameter — must be removed and replaced with explicit deps
- numpy/pandas imports only if the helpers actually use them (verify before adding)

### Mocking Strategy

N/A — no new tests in this task.

## Dependencies

- [ ] Task 424 (establishes the extraction pattern)

## Effort Estimate

- Size: L
- Hours: 3-4
- Parallel: false (largest extraction, benefits from pattern established in 424)

## Definition of Done

- [ ] `scan/phase_options.py` created with all functions, helpers, and constants
- [ ] `ruff check src/options_arena/scan/phase_options.py`
- [ ] `ruff format src/options_arena/scan/phase_options.py`
- [ ] `mypy src/options_arena/scan/phase_options.py --strict`
- [ ] No test files modified
