---
name: Full verification pass — lint, typecheck, test suite
status: open
created: 2026-03-04T17:18:28Z
updated: 2026-03-04T17:22:28Z
github: https://github.com/jobu711/options_arena/issues/262
depends_on: [258, 259, 260, 261]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Full verification pass — lint, typecheck, test suite

## Description
Final verification task after all fixes (FR-1 through FR-5) are implemented. Run the
full CI gate sequence: ruff lint + format, mypy strict, and the complete test suite.
Fix any issues discovered. Verify new test count meets the >= 10 target.

## Acceptance Criteria
- [ ] `uv run ruff check . --fix && uv run ruff format .` — clean
- [ ] `uv run mypy src/ --strict` — clean
- [ ] `uv run pytest tests/ -v` — all tests pass (3,799+ existing + new)
- [ ] New tests added: >= 10 covering all 5 FRs
- [ ] No raw `dict` fields remain in `DebateResultDetail`
- [ ] V2 export includes all 4 V2 agent sections
- [ ] No `date.today()` calls remain in outcome_collector.py

## Technical Details
- Run all three verification commands sequentially
- If ruff finds fixable issues, apply `--fix` and verify
- If mypy reports errors, fix type annotations in modified files
- If tests fail, identify root cause and fix
- Count new test functions across all modified/new test files

## Test Plan

### Test Files
- No new test files (this task runs existing + newly created tests)

### Test Cases
This task runs the full test suite — no new tests to write.

### Verification Checklist
- [ ] `ruff check .` exit code 0
- [ ] `ruff format --check .` exit code 0
- [ ] `mypy src/ --strict` exit code 0
- [ ] `pytest tests/ -v` exit code 0
- [ ] Count new test functions: `grep -r "def test_" tests/ --include="*.py"` in modified files

### Edge Cases
- N/A (verification task)

### Mocking Strategy
- N/A (verification task)

## Dependencies
- [ ] Task 258 — schema + debate route fixes
- [ ] Task 259 — export endpoint fix
- [ ] Task 260 — timezone fix
- [ ] Task 261 — migration

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: false (must wait for all other tasks)

## Definition of Done
- [ ] All 3 CI gates pass
- [ ] New test count verified >= 10
- [ ] No regressions in existing tests
- [ ] Ready for commit and PR
