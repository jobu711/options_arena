---
name: E2E tests for PreScanFilters interaction
status: open
created: 2026-03-06T14:22:09Z
updated: 2026-03-06T14:28:54Z
github: https://github.com/jobu711/options_arena/issues/283
depends_on: [282]
parallel: false
conflicts_with: []
test_files: [tests/e2e/test_prescan_filters.py]
---

# Task: E2E Tests for PreScanFilters Interaction

## Description
Write ~4 new Playwright E2E tests validating the PreScanFilters component: preset selection, price range controls, DTE range controls, and collapsible section behavior. Verify all existing 38+ E2E tests still pass.

## Acceptance Criteria
- [ ] ~4 new Playwright E2E tests in `tests/e2e/test_prescan_filters.py`
- [ ] Test 1: Preset selection changes visible ticker count
- [ ] Test 2: Price range min/max controls accept values and are sent in API request
- [ ] Test 3: DTE range min/max controls accept values and are sent in API request
- [ ] Test 4: Collapsible panels expand/collapse, filters persist across collapse
- [ ] All 38+ existing E2E tests pass unchanged
- [ ] Tests use isolated DB (project convention)

## Technical Details
- **`tests/e2e/test_prescan_filters.py`**: Follow existing E2E patterns in `tests/e2e/`.
- Use `page.locator()` with `data-testid` selectors (project convention).
- Each test navigates to scan page, interacts with PreScanFilters, verifies behavior.
- Mock or intercept API calls where needed (e.g., `page.route()` for preset-info endpoint).
- Use `expect(locator).to_have_text()` / `to_be_visible()` / `to_have_value()` assertions.
- Follow isolated DB pattern with `--database` fixture.

## Test Plan

### Test Files
- `tests/e2e/test_prescan_filters.py`

### Test Cases
```python
class TestPreScanFilters:
    async def test_preset_selection_shows_count(self, page: Page) -> None:
        """Select Nasdaq 100 preset, verify ticker count badge appears."""
        ...
    async def test_price_range_controls(self, page: Page) -> None:
        """Set min/max price, verify values sent in scan request payload."""
        ...
    async def test_dte_range_controls(self, page: Page) -> None:
        """Set min/max DTE, verify values sent in scan request payload."""
        ...
    async def test_panel_collapse_preserves_state(self, page: Page) -> None:
        """Collapse/expand panel, verify filter values persist."""
        ...
```

### Edge Cases
- Rapid preset switching → UI doesn't flash or error
- Invalid range (min > max in UI) → validation feedback shown
- Network error on preset-info fetch → graceful degradation

### Mocking Strategy
- `page.route()` to intercept API calls and return deterministic responses
- Isolated DB via project E2E fixture

## Dependencies
- [ ] Task 282 — frontend component must be built

## Effort Estimate
- Size: S
- Hours: 2-3
- Parallel: false (depends on Task 282)

## Definition of Done
- [ ] 4 new E2E tests in `tests/e2e/test_prescan_filters.py`
- [ ] All 4 new tests pass
- [ ] All 38+ existing E2E tests pass unchanged
- [ ] Tests follow project E2E conventions (isolated DB, data-testid selectors)
