---
name: Integration verification — full test suite + manual smoke test
status: open
created: 2026-03-06T14:22:09Z
updated: 2026-03-06T14:28:54Z
github: https://github.com/jobu711/options_arena/issues/284
depends_on: [285, 286, 287, 282, 283]
parallel: false
conflicts_with: []
test_files: []
---

# Task: Integration Verification — Full Test Suite + Manual Smoke Test

## Description
Final verification pass: run full Python test suite (3,921+ tests), all E2E tests (38+ existing + 4 new), linter, formatter, type checker. Manual smoke test of scan flow with new presets and filters. Verify ScanPage.vue line count < 150.

## Acceptance Criteria
- [ ] `uv run ruff check . --fix && uv run ruff format .` passes
- [ ] `uv run mypy src/ --strict` passes
- [ ] `uv run pytest tests/ -v` — all 3,921+ unit tests pass
- [ ] E2E tests — all 42+ tests pass (38 existing + 4 new)
- [ ] `ScanPage.vue` line count verified < 150
- [ ] `npm run type-check && npm run build && npm run lint` pass
- [ ] Manual smoke test: select each of 6 presets, verify scan launches
- [ ] Manual smoke test: set price range, verify filtering works
- [ ] Manual smoke test: set DTE range, verify contract selection respects it
- [ ] No regressions in existing functionality

## Technical Details
Run verification commands in sequence:
```bash
# Backend
uv run ruff check . --fix && uv run ruff format .
uv run mypy src/ --strict
uv run pytest tests/unit -v
uv run pytest tests/e2e -v

# Frontend
cd web && npm run type-check && npm run build && npm run lint

# Line count check
wc -l web/src/pages/ScanPage.vue  # Must be < 150
```

Manual smoke test checklist:
1. Start server: `options-arena serve`
2. Navigate to scan page
3. Verify 3 collapsible filter panels visible
4. Select each preset (FULL, SP500, ETFS, NASDAQ100, RUSSELL2000, MOST_ACTIVE) — verify count badge
5. Set price range (e.g., $50-$200) — verify scan respects filter
6. Set DTE range (e.g., 30-90) — verify only contracts in range appear
7. Collapse/expand panels — verify state preserved
8. Run full scan with Nasdaq 100 preset — verify completes successfully

## Test Plan

### Test Files
- No new test files — this is a verification task

### Test Cases
- Run all existing test suites
- Verify all acceptance criteria via commands above

### Edge Cases
- N/A (covered by individual task tests)

### Mocking Strategy
- N/A (integration verification uses full test suites)

## Dependencies
- [ ] All prior tasks (285-283) must be complete

## Effort Estimate
- Size: S
- Hours: 1-2
- Parallel: false (final gate)

## Definition of Done
- [ ] All automated checks pass (lint, format, typecheck, tests)
- [ ] Manual smoke test completed
- [ ] ScanPage.vue < 150 lines confirmed
- [ ] No regressions identified
- [ ] Epic ready for merge
