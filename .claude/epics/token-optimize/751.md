---
name: Create architecture.md — consolidate system-patterns + module-summaries
status: open
created: 2026-03-23T17:19:06Z
updated: 2026-03-23T17:28:44Z
github: https://github.com/jobu711/options_arena/issues/751
depends_on: [749]
parallel: true
conflicts_with: []
test_files: []
---

# Task: Create architecture.md

## Description
Create `.claude/context/architecture.md` (~200 lines) by consolidating content from
`system-patterns.md` (257 lines) and `module-summaries.md` (125 lines). Deduplicate
content that now lives in Tier 0 CLAUDE.md (boundary table, pattern one-liners).
This becomes the primary Tier 1 reference for cross-module work and audits.

## Acceptance Criteria
- [ ] `.claude/context/architecture.md` exists
- [ ] Contains all unique design patterns from system-patterns.md
- [ ] Contains all module constraints from module-summaries.md
- [ ] No duplication with Tier 0 CLAUDE.md content
- [ ] `wc -l .claude/context/architecture.md` <= 200 lines

## Technical Details
Content to INCLUDE (from system-patterns.md):
- All "### Pattern Name" sections with their descriptions
- Repository pattern, immutable models, re-export pattern
- NaN/Inf defense, service layer, PydanticAI agent pattern
- Desk agent, intent routing, learning, synthesis, scan pipeline patterns
- Web API patterns, analytics, backtesting, metadata, liquidity
- All newer patterns: ToolResponse, Model Routing, Eval Harness, Confidence Decay

Content to INCLUDE (from module-summaries.md):
- Per-module critical constraints and gotchas
- Field name mappings, boundary rules
- NOT the general patterns (already in system-patterns content)

Content to EXCLUDE (already in Tier 0):
- Boundary table (in CLAUDE.md)
- Pattern one-liner summaries (in CLAUDE.md)
- Anything that duplicates CLAUDE.md common mistakes list

Compression strategy:
- Remove code examples — keep constraint descriptions only
- Merge overlapping content between the two source files
- Use terse bullet points, not paragraphs

## Execution

### Read First
Files that MUST be read before starting this task:
- `CLAUDE.md` — know what's already in Tier 0 to avoid duplication
- `.claude/context/system-patterns.md` — primary source (257 lines)
- `.claude/context/module-summaries.md` — secondary source (125 lines)

### Action
1. Read both source files completely
   - WHY: need to identify overlapping content for deduplication
2. List all sections from system-patterns.md
   - WHY: ensure nothing is lost
3. Cross-reference with new CLAUDE.md to identify content already in Tier 0
   - WHY: avoid duplication — Tier 0 has pattern summaries and boundary table
4. Write architecture.md combining unique content from both sources
   - WHY: single Tier 1 file replaces two files
5. Verify line count: `wc -l .claude/context/architecture.md`
   - WHY: must be <= 200 lines

### Verify
```
wc -l .claude/context/architecture.md  # <= 200
grep -c "###" .claude/context/architecture.md  # pattern sections present
```

### Done
- `wc -l .claude/context/architecture.md` <= 200 lines
- All design patterns from system-patterns.md are represented
- All module constraints from module-summaries.md are represented
- No content duplicated with CLAUDE.md Tier 0

## Test Plan

### Validation
No pytest tests — config file only. Validation:
- Line count <= 200
- Every pattern section from system-patterns.md has corresponding content
- Every module from module-summaries.md has corresponding constraints
- No verbatim code blocks from source files

## Dependencies
- [ ] Task 749: CLAUDE.md rewrite (need to know what's in Tier 0 to avoid duplication)

## Effort Estimate
- Size: M
- Hours: 2
- Parallel: true (can run alongside Tasks 753, 755)

## Definition of Done
- [ ] architecture.md created at .claude/context/architecture.md
- [ ] Line count <= 200
- [ ] All patterns and module constraints preserved
- [ ] No Tier 0 duplication
