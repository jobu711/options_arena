---
name: After-measurement and validation smoke tests
status: open
created: 2026-03-23T17:19:06Z
updated: 2026-03-23T17:28:44Z
github: https://github.com/jobu711/options_arena/issues/756
depends_on: [749, 751, 753, 755, 748, 750, 752, 754]
parallel: false
conflicts_with: []
test_files: []
---

# Task: After-Measurement and Validation Smoke Tests

## Description
Measure all context sizes after optimization. Compare against baseline (Task 747).
Run representative smoke tests to verify Claude still finds correct context via the
router. Run full test suite to confirm no code breakage.

## Acceptance Criteria
- [ ] After-measurement documented alongside baseline in a comparison table
- [ ] Auto-loaded context < 420 lines (CLAUDE.md + rules)
- [ ] Tier 1 files total < 400 lines
- [ ] Module CLAUDE.md total < 2,000 lines
- [ ] Reduction percentage calculated and documented
- [ ] Full test suite passes (`uv run pytest tests/ -n auto -q`)
- [ ] Smoke tests demonstrate router working for 3+ task types

## Technical Details

**Measurements to take:**
- `wc -l CLAUDE.md` (target: < 260)
- `wc -l .claude/rules/*.md` (unchanged: ~156)
- `wc -l .claude/context/architecture.md .claude/context/product.md .claude/context/algorithms.md .claude/context/progress.md` (target: < 400)
- `find src/options_arena -name "CLAUDE.md" -exec wc -l {} +` (target: < 2,000)
- `wc -l .claude/agents/*.md` (target: 30% reduction from baseline)

**Comparison table format:**
| Category | Before | After | Reduction |
|----------|--------|-------|-----------|
| CLAUDE.md | 311 | ? | ?% |
| @-referenced context | 1,411 | 0 | 100% |
| Rules | 156 | 156 | 0% |
| Auto-loaded total | ~1,878 | ? | ?% |
| Tier 1 files | N/A | ? | — |
| Module CLAUDE.md | 3,972 | ? | ?% |
| Agent definitions | ? | ? | ?% |

**Smoke tests** (manual verification in new sessions):
1. Single-module bug fix: start session, ask "fix a bug in scoring/contracts.py"
   - Verify: Claude reads scoring/CLAUDE.md via router, not all context files
2. Cross-module feature: ask to add a feature touching agents/ and api/
   - Verify: Claude reads architecture.md + relevant module CLAUDE.md files
3. Audit: run `/full-audit`
   - Verify: audit agents load architecture.md, not product context

**Test suite:**
- `uv run pytest tests/ -n auto -q` — full suite, no code changes means no regressions

## Execution

### Read First
Files that MUST be read before starting this task:
- `.claude/epics/token-optimize/baseline.md` — before measurements
- `CLAUDE.md` — verify final state
- `.claude/context/architecture.md` — verify exists
- `.claude/context/product.md` — verify exists
- `.claude/context/algorithms.md` — verify exists

### Action
1. Run all measurement commands (same as Task 747)
   - WHY: need after-numbers for comparison
2. Read baseline.md and compute reduction percentages
   - WHY: validate we hit 80% target
3. Create comparison table in `.claude/epics/token-optimize/results.md`
   - WHY: permanent record of optimization results
4. Run full test suite: `uv run pytest tests/ -n auto -q`
   - WHY: confirm no code breakage (should pass — no code changes)
5. Document smoke test plan for manual verification
   - WHY: router effectiveness can only be tested in real sessions
6. Update epic status to completed if all criteria met
   - WHY: close out the epic

### Verify
```
wc -l CLAUDE.md  # < 260
wc -l .claude/rules/*.md | tail -1  # ~156
wc -l .claude/context/architecture.md .claude/context/product.md .claude/context/algorithms.md .claude/context/progress.md | tail -1  # < 400
find src/options_arena -name "CLAUDE.md" -exec wc -l {} + | tail -1  # < 2,000
uv run pytest tests/ -n auto -q  # all pass
```

### Done
- results.md created with before/after comparison table
- All line count targets met
- Test suite passes
- Reduction >= 70% on auto-loaded context documented
- Smoke test plan documented for manual post-merge verification

## Test Plan

### Validation
- Full pytest suite: `uv run pytest tests/ -n auto -q` (confirms no code breakage)
- Line count verification against all success criteria targets
- Manual smoke tests documented for post-merge verification

## Dependencies
- [ ] All tasks 747-754 must be complete

## Effort Estimate
- Size: S
- Hours: 1.5
- Parallel: false (final validation — depends on everything)

## Definition of Done
- [ ] results.md with before/after comparison
- [ ] All line count targets met
- [ ] Test suite passes
- [ ] Smoke test plan documented
- [ ] Epic marked completed
