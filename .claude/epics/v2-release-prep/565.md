---
name: Housekeeping & Stale Artifact Cleanup
status: open
created: 2026-03-17T03:21:32Z
updated: 2026-03-17T03:32:04Z
github: https://github.com/jobu711/options_arena/issues/565
depends_on: []
parallel: true
conflicts_with: []
test_files: []
---

# Task: Housekeeping & Stale Artifact Cleanup

## Description
Clean stale artifacts from the repository before running audits. Remove abandoned worktree directories, archive completed epic tracking files, and ensure clean working tree state on master.

## Acceptance Criteria
- [ ] All stale worktree directories in `.claude/worktrees/` removed
- [ ] Completed/merged epic directories archived (e.g., `scientific-ml-integration/`)
- [ ] `git status` shows clean working tree (no uncommitted changes)
- [ ] No stale "In Progress" items in `progress.md` (FinancialDatasets.ai status clarified)

## Technical Details
- Research identified 6 abandoned session directories in `.claude/worktrees/` — not real git worktrees, just leftover dirs
- `scientific-ml-integration/` epic is completed (all children merged) — archive to `.claude/epics/archived/`
- FinancialDatasets.ai epic listed as "In Progress" in progress.md but has 0 open GitHub issues — update status to reflect reality
- Git working tree was clean at research time, but verify before proceeding

## Execution

### Read First
Files that MUST be read before starting this task:
- `.claude/context/progress.md` — understand current "In Progress" items
- `.claude/epics/v2-release-prep/research.md` — stale artifacts section lists specific items

### Action
1. Run `ls .claude/worktrees/` to identify stale session directories
   - WHY: Research found 6 abandoned `agent-*` dirs that are not git worktrees
2. Remove stale worktree directories: `rm -rf .claude/worktrees/agent-*`
   - WHY: These are abandoned session dirs, not real worktrees — safe to delete
3. Check if `.claude/epics/scientific-ml-integration/` exists and all tasks are merged
   - WHY: Research says it's completed — verify before archiving
4. Move completed epics to `.claude/epics/archived/` (create dir if needed)
   - WHY: Keeps active epic directory clean
5. Update `.claude/context/progress.md`:
   - Clarify FinancialDatasets.ai status (move to "Future Work" or mark completed if archived)
   - Ensure no stale "In Progress" items remain
   - WHY: PRD requires no stale references before release
6. Run `git status` to verify clean working tree
   - WHY: Phase 1 gate requires clean starting state
7. Commit housekeeping changes: `chore: clean stale worktrees and archive completed epics`
   - WHY: Atomic commit for cleanup work

### Verify
```bash
ls .claude/worktrees/ 2>/dev/null || echo "No worktrees dir — clean"
git status
grep -c "In Progress" .claude/context/progress.md
```

### Done
- `.claude/worktrees/` is empty or doesn't exist
- `git status` shows clean working tree after commit
- `progress.md` has no stale "In Progress" items
- Completed epics archived under `.claude/epics/archived/`

## Test Plan

### Test Files
- No new test files — this is a cleanup task

### Test Cases
```python
# Verification is manual/command-based, not pytest:
# 1. Verify no stale worktree dirs exist
# 2. Verify git status is clean
# 3. Verify progress.md has accurate status for all items
```

### Edge Cases
- Worktrees directory doesn't exist: skip silently
- Some worktree dirs may be in use: check with `git worktree list` before deleting
- Epic archive directory doesn't exist: create it

### Mocking Strategy
- N/A — no code under test

## Dependencies
- [ ] None — this is the first task

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: true (can run independently)

## Definition of Done
- [ ] Stale worktree directories removed
- [ ] Completed epics archived
- [ ] progress.md updated with accurate statuses
- [ ] Clean git status after commit
