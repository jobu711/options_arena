---
name: Documentation Alignment & Release Tag
status: open
created: 2026-03-17T03:21:32Z
updated: 2026-03-17T03:32:04Z
github: https://github.com/jobu711/options_arena/issues/571
depends_on: [569]
parallel: false
conflicts_with: [569]
test_files: []
---

# Task: Documentation Alignment & Release Tag

## Description
Final documentation alignment, context budget verification, stale reference cleanup, and creation of the `v2.10.0` git tag. This is the last task — the release gate.

## Acceptance Criteria
- [ ] CLAUDE.md context budget within limits (verified by `wc -l`)
- [ ] No stale "In Progress" items in `progress.md`
- [ ] No stale epic references in context files
- [ ] `progress.md` updated with v2-release-prep as completed
- [ ] Clean `git status` on master
- [ ] Final commit: `chore: release v2.10.0`
- [ ] Git tag `v2.10.0` created on final commit

## Technical Details
- Context budget limits: CLAUDE.md ≤500 lines, @-referenced context ≤2,000 lines, rules ≤1,000 lines, grand total ≤3,500
- `progress.md` must reflect: v2-release-prep completed, FinancialDatasets.ai status accurate, no stale items
- Any remaining epic tracking files from this epic should be archived after tagging
- Tag should be lightweight (`git tag v2.10.0`) — no need for annotated tag unless user prefers

## Execution

### Read First
Files that MUST be read before starting this task:
- `.claude/context/progress.md` — verify current state after all prior tasks
- `CLAUDE.md` — check context budget section
- `.claude/context/tech-context.md` — verify version references

### Action
1. Verify context budget:
   ```bash
   wc -l CLAUDE.md .claude/context/*.md .claude/rules/*.md
   ```
   - WHY: CLAUDE.md policy requires staying within budget limits
2. Review `progress.md` for accuracy:
   - Move `v2-release-prep` from "In Progress" to "Recently Completed"
   - Verify FinancialDatasets.ai status was updated in Task 565
   - Verify version shows 2.10.0
   - WHY: Context files must be accurate at release time
3. Review `tech-context.md` for version references — update if any say 2.8.0
   - WHY: All context files must agree on version
4. Check for stale epic references in context files:
   ```bash
   grep -r "In Progress" .claude/context/
   ```
   - WHY: No stale references should exist at release
5. Final lint + type check + critical tests:
   ```bash
   uv run ruff check . --fix && uv run ruff format .
   uv run mypy src/ --strict
   uv run pytest -m critical -q
   ```
   - WHY: Final sanity check before tagging
6. Stage and commit all final changes: `chore: release v2.10.0`
   - WHY: Clean final commit with release message
7. Verify clean git status: `git status`
   - WHY: Tag should be on a clean commit
8. Create git tag: `git tag v2.10.0`
   - WHY: PRD success criterion — tagged release on master

### Verify
```bash
git tag -l "v2.10.0"
git status
wc -l CLAUDE.md .claude/context/*.md .claude/rules/*.md
grep "2.10.0" .claude/context/progress.md
```

### Done
- `git tag -l "v2.10.0"` shows the tag exists
- `git status` is clean (no uncommitted changes)
- Context budget within limits
- All context files reference version 2.10.0
- No stale "In Progress" items

## Test Plan

### Test Files
- No new test files — this is a documentation and release task

### Test Cases
```python
# Verification is command-based, not pytest:
# 1. git tag v2.10.0 exists
# 2. git status is clean
# 3. Context budget within limits (wc -l check)
```

### Edge Cases
- Context budget over limit: trim progress.md by archiving old "Recently Completed" items
- Stale references found: fix inline before final commit
- Tag already exists (re-run scenario): `git tag -d v2.10.0` then recreate

### Mocking Strategy
- N/A — no code under test

## Dependencies
- [ ] Task 569 (Version Bump & CHANGELOG) — version must be bumped before tagging

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: false (final task in sequence)

## Definition of Done
- [ ] Git tag `v2.10.0` exists on master
- [ ] Clean `git status`
- [ ] All context files aligned on v2.10.0
- [ ] Context budget verified within limits
- [ ] Release complete
